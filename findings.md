# Findings

## 2026-09-04 — Confirmed serverHold cause: WHOIS accuracy, not a keyword/foreign-registrant theory
**Finding:** Porkbun's actual compliance email states the NIXI hold reason is **"inaccurate WHOIS information."** The domain's RDAP registrant record had blank name/street/postal fields (only "California" in the region field) — `.in` domains cannot use third-party WHOIS privacy protection, and the registry requires the underlying record to be complete even though it masks display data itself. This supersedes the earlier "sensitive keyword + foreign registrant" theory, which was inference from cross-registrar timing patterns, not the stated cause.
**Context:** User forwarded the literal Porkbun email after the earlier RDAP-based diagnosis.
**Implication:** Fix is two steps: complete/accurate WHOIS contact info at the registrar (and disable any privacy add-on, not permitted for `.in`), then email `support@nixi.in` with KYC docs matching that corrected record. Memory file `project_domain_serverhold_outage.md` updated with this confirmed cause and the concrete remediation steps.

## 2026-09-04 — Emblems and Names (Prevention of Improper Use) Act, 1950 restricts "Prime Minister" in trade/business/trademark names
**Finding:** This Indian statute explicitly lists "the name or pictorial representation of the Prime Minister of India" among protected names, and prohibits using it for trade/profession/trademark/patent purposes without prior Central Government permission. Independent of domain TLD — it targets the business/product name itself, enforced by the Registrar of Companies / Trademark Registry, not by domain registrars.
**Context:** User asked to anticipate problems with `pradhanmantrielectionsgame.com` before purchasing it.
**Implication:** Not a blocker for an informal `.com`-hosted hobby project with no company/trademark filing. Becomes a real, unavoidable wall if this project is ever formally incorporated or trademarked in India under a name containing "Pradhan Mantri" — revisit then, not now.

## 2026-09-04 — Indian courts hold publicity rights subservient to free speech for public-domain use (Digital Collectibles v. Galactus Funware, Delhi HC 2023)
**Finding:** Delhi HC refused an injunction sought by real cricketers against a fantasy-sports NFT card game using their names/likeness/public stats, holding publicity rights are subservient to free speech including commercial speech, and that public-domain facts (name, performance data) can't be exclusively licensed. This is a closer fact pattern to a strategy game using real public figures than the Bachchan personality-rights precedent (which involved an actual lottery-scam app, not a legitimate game) — and political figures get an additional, stronger "core political speech" protection that pure entertainers don't.
**Context:** User pushed back on whether the personality-rights risk flagged earlier (re: politician roster) was overstated, prompting a deeper look at Indian case law rather than relying on the single Bachchan precedent.
**Implication:** Revised the roster risk assessment downward overall (most politicians = low risk, reinforced by existing unchallenged precedent apps). Bachchan and Rajinikanth remain the two highest-risk names specifically, since each has direct, on-point personality-rights case law naming them.

## 2026-09-04 — Rajinikanth never held political office; Bachchan and Hema Malini genuinely did/do
**Finding:** Rajinikanth announced political-party intent twice (2017, Dec 2020) via a precursor fan organization (Rajini Makkal Mandram) but withdrew before the party was ever formally launched or any election contested — no actual campaign or officeholding. By contrast, Amitabh Bachchan was an elected MP (Allahabad, Congress, 1984–87) and Hema Malini is a sitting BJP MP (Mathura, since 2014) despite both being primarily known as entertainers.
**Context:** User asked whether "launching a political party" makes someone a de facto politician, specifically re: Rajinikanth's roster entry.
**Implication:** Rajinikanth stays the single highest legal-risk name in the roster (pure celebrity personality-rights case, no political-speech protection to lean on) — the earlier "swap him out or keep deliberately" framing still stands. Bachchan and Hema Malini's real political history arguably strengthens, not weakens, their case for political-speech treatment.

## 2026-09-04 — No statute or case law found restricting private depiction of political party symbols outside the ECI's ballot-allocation process
**Finding:** The Election Symbols (Reservation and Allotment) Order, 1968 governs which symbol is allotted to which recognized party for ballot/candidacy use — an administrative allocation process among candidates, not a general-public-use restriction. Party symbols aren't conventional registered trademarks in India (they're ECI-allotted, a different legal category). No case law found of a party suing a private app/game over symbol depiction, unlike the Bachchan/Rajinikanth personality-rights pattern.
**Context:** User asked to look up the rules for using political party symbols (lotus, hand, etc.) in the game.
**Implication:** Lowest-risk item among everything raised this session — no changes needed to how the game currently depicts party symbols/colors. Absence of a found restriction isn't proof of zero risk, but nothing surfaced despite a reasonably thorough search, consistent with existing precedent apps using real party symbols unchallenged.

## 2026-09-03 — CLAUDE.md pre-simplification archive (full verbatim text, preserved before condensing)
**Finding:** `CLAUDE.md` had grown to ~135 lines of extremely dense prose (each bullet carrying its full "found on date X, user reported Y, measured Z, reverted W" backstory inline) because it was being used as both a rules file and a running decision log — the exact drift `findings.md`/`docs/wiki.html` were supposed to absorb instead. Condensed to terse, current-state-only rules; every fact, number, filename, and dated rationale below is preserved verbatim so nothing found via the old file is lost — just relocated here. Not itself a "finding" in the debugging sense; filed here because this is the project's designated dated-history location.
**Context:** User asked how to simplify `CLAUDE.md` without losing stored detail.
**Implication:** `CLAUDE.md` going forward is meant to be read top-to-bottom every session — keep new entries terse (rule + one-clause why) and put full narrative/measurement detail in a fresh dated entry here instead, the same discipline this cleanup applied retroactively.

**Archived section: UI conventions**
- Small UTs (Delhi, Chandigarh, Dadra & Nagar Haveli and Daman & Diu, Puducherry, Lakshadweep, Andaman & Nicobar Islands) are never directly tappable on the map — they're too small to hit reliably. Always route their interaction through the existing button-cluster pattern (`union-territories-container` in `index.html`, paired with the timer-pill), not direct map taps. J&K and Ladakh are large enough to tap directly and don't need this treatment.
- **`mobile/` (built 2026-07-23) is the real, playable build** — Booth Ink's markup/CSS wired to a full engine (`mobile/engine.js` pure redistribution/apportionment/starting-position math, `mobile/game.js` phase loop + all systems + AI opponent, `mobile/main.js` DOM glue) per `design/economy-status-map.md`. `design/prototypes/pme-mobile-sheet.html` remains the frozen design-reference prototype it was cloned from (demo-only logic, hardcoded to Modi's 4 agendas) — don't edit game logic there, edit `mobile/*.js`. `index.html`/`styles.css` at repo root is the old desktop-ported skin, superseded by both. Run `npm test` (engine self-check + a full-game simulation) and `npm run serve` (static server, then open `/mobile/index.html`) after changes here.
- **Update 2026-07-29 — `mobile/index.html` is now deprecated; `mobile/index-redesign-a.html` ("Refined Booth Ink") is the confirmed canonical mobile build.** Redesign A had been trending toward this since 2026-07-27 (a full-parity interactive prototype built to validate map tap-target/layout, alongside `index-redesign-c.html`'s dark-theme comparison), and is now confirmed as the real build going forward — not just a comparison prototype. It also carries a one-off addition `index.html` never had: an extra `#keralaBtn` quick-invest button (Kerala's narrow coastline is hard to tap reliably on the map), alongside `#delhiBtn`/`#goaBtn`/`#neBtn`/`#utsBtn`. **Update 2026-08-22 — reversed: `index-redesign-a.html` has been renamed back to `mobile/index.html`** (git-mv'd; `manifest-redesign-a.json` → `manifest.json`; `mobile/sw.js`'s cache version bumped `v2`→`v3`) because the dual-naming was pure confusion once redesign-a was confirmed as the permanent build — there's no other `index.html` left in `mobile/` to disambiguate from. All future file/element references for the mobile UI should target `mobile/index.html` again; `npm run serve` → open `/mobile/index.html`. `index-redesign-c.html`/`manifest-redesign-c.json` (the dark-theme comparison prototype) are untouched under their own names. `mobile/sw.js`'s precache list (`./index.html`, `./engine.js`, `./game.js`, `./main.js`, `./html2canvas.min.js`, `./manifest.json`) already matched this filename set with no further edits needed.
- **Update 2026-08-22 — the root-`index.html`-byte-copy deploy mechanism described below has been retired; root `index.html` no longer exists on this branch.** In its place, `scripts/deploy-mobile.js` builds a filtered, flattened copy of just the runtime files (`mobile/index.html`→`index.html`, `engine.js`, `game.js`, `main.js`, `sw.js`, `manifest.json`, `html2canvas.min.js`, plus the `assets/`, `data/`, `sounds/` folders and a short deploy-only `README.md`) into a local git worktree tracking the `mobile` remote's `main` branch, rewriting each copied file's `../assets/`, `../data/`, `../sounds/` references to drop the `../` (since these files become siblings of `assets/`/`data/`/`sounds/` at the fresh repo's root instead of nested a level below them) — then commits and pushes from there. This also drops `js/` (the legacy desktop code), `styles/`, `design/`, `docs/`, and dev-only scripts from what ships publicly, none of which the live mobile game needs. Run it whenever `mobile/index.html` (or its sibling `.js` files) changes and the live site needs to catch up — it always mirrors current source exactly, so the old "did I remember to re-copy" drift risk is gone. The original desktop-skin root `index.html` this whole mechanism replaced only still exists on `main`/`mobile-ui-overhaul` in the *other* repo (`pradhanmantrielectionsgame.github.io`, this project's `origin` remote) — that repo and its live desktop game are untouched by any of this.
- The mobile build is single-player-vs-AI only — live human matchmaking (ADR-0001/0002) needs a Firebase/Supabase backend, an external service account the user hasn't asked to stand up, so it's out of scope until requested. The AI (`mobile/game.js` `runAI`) is a greedy heuristic, not adversarially tuned. **A full implementation-ready plan for this already exists at `design/multiplayer-implementation-plan.md`** (written 2026-07-28) — event-sourced action-log sync over Firebase Realtime DB, reusing `game.js`'s action functions as-is since they're already pure/playerKey-agnostic. Point a future session at that file to resume/implement rather than re-deriving the architecture.
- 13 of 20 politicians' special-power numbers (`data/politicians-data.json` → `power` field) are a first-pass concrete assignment following the same conventions as the 6 the design doc explicitly reworked — not a substitute for a real balance/playtesting pass, which the design doc itself still marks open.
- **Update 2026-08-23: all 20/20 politician portrait images (`assets/images/*.png`) now exist** (confirmed via a direct file-existence check against every `image` path in `data/politicians-data.json`) — the earlier "11 of 20 missing, falls back to colored initial circle" note was stale and is retired; don't resurface portrait generation as a to-do without re-checking the actual files first. **Sound clips are a separate, still-real gap**: only 4 of 20 (`Narendra_Modi`, `Amitabh_Bachchan`, `Rajinikanth`, `Sachin_Tendulkar`) have a dedicated power-sound `.mp3` in `sounds/`; the other 16 fall back to the generic `fanfare` clip via `playPowerSound()`'s `.catch()` path (`mobile/main.js`).
- **The end-of-game "Share result" feature (added 2026-08-21) captures a real screenshot of the on-screen `.declare-card` via a locally-vendored `mobile/html2canvas.min.js` (v1.4.1) — not a hand-drawn canvas substitute.** An earlier hand-drawn compact card (headline + 2 ledger rows, Wordle/Duolingo-style) was built, tested on a real phone over an HTTPS tunnel, and rejected as showing only "half" the real result (missing the parliament chart and match stats); html2canvas instead screenshots the actual card so it always stays visually in sync with whatever that card currently shows, excluding just the Play again/Share buttons (`ignoreElements`). Don't remove `html2canvas.min.js` as unused or re-introduce a hand-drawn replacement without re-checking this history.
- **The politician-select carousel and end-of-game results screen use a "ballot card" visual identity — Booth Ink's own light-theme ink/brass/stamp/paper materials (tricolor stripe, brass-ringed seal, ticket-stub die-cut, decree box), not a literal Pokémon-style holo-foil card and not a dark theme.** Approved mockup and rationale: `design/prototypes/pol-card-mockup.html`. The app has no dark mode anywhere else, so this screen shouldn't invent one either — confirmed 2026-07-24 after a first dark-card-stock draft was rejected specifically for clashing with the rest of the app's light theme.
- **Don't reintroduce raw per-politician agenda bonus numbers (`data/politicians-data.json`'s `policies[].bonus`) in the select-screen UI.** They were deliberately removed 2026-07-24 as meaningless after the agenda-effect-system revamp; the replacement is a tap-to-expand region-effect breakdown (`agendaEffectChips()` in `mobile/main.js`, shared with the in-game agenda-info panel) reading real `policy-tags.json` data instead.
- **The app's real entry point is now a separate full-bleed welcome screen (`#welcomeOverlay` in `mobile/index.html`), not the politician-select screen.** Added 2026-07-25, reusing `assets/images/PradhanMantriBg.png` (an older poster-collage illustration, stylistically distinct from the rest of Booth Ink's flat-vector look) as a full-bleed `object-fit:contain` background with a dark scrim, a "Begin Campaign" button that hides `#welcomeOverlay` and reveals `#selectOverlay` (`mobile/main.js`). The select screen's own `<h1>` header is unchanged and still renders once revealed.
- **India's map SVG viewBox (`84 27 833 946` in `mobile/index.html`'s `<svg class="india-map">`) already tightly bounds the landmass — margins measured directly via Playwright `getBBox()` are only ~2-6% on any edge.** The large empty regions visible northeast and southeast of the landmass in the rendered HUD are ocean space within the landmass's own bounding rectangle (India's shape is roughly triangular, not rectangular) — not viewBox letterboxing, and not fixable by `transform:scale()`/`transform-origin` tuning. Confirmed 2026-07-27 after a scale-up attempt (1.03→1.35 with an offset origin, meant to "fill" the dead space) instead clipped the Northeast panhandle off-screen. Design future map-adjacent UI to deliberately occupy these NE/SE (and to a lesser extent SW) dead zones with floating panels/strips rather than trying to make the map itself expand to fill a rectangle it geometrically can't.

**Archived section: Game design principles**
- **`docs/wiki.html` is the single authoritative reference for finalized game design** — core loop, win condition, starting position, the redistribution rule, every mechanic category (investment, rally tokens, agendas, regional dominance, special powers), the politician roster, architecture, the AI, and testing. When the question is "how is X supposed to work," check it first. `design/economy-status-map.md` **was** this project's authoritative reference but was formally deprecated 2026-07-29 — it kept drifting out of sync with shipped decisions even while other sections of it were being actively edited (see `docs/wiki.html`'s divergence log, entries 8–11, for concrete examples: it still described the superseded hung-parliament rule and an already-fixed policy tag direction as current). It's kept only as a dated historical decision log now (the tag-cancellation audits, the funds-bump rationale, the seat-apportionment worked example) — `design/plan.md`, `CHANGELOG.md`, and `findings.md` remain dated history in the same sense, not current-state references, and neither is `economy-status-map.md` anymore.
- **`docs/wiki.html` itself needs the same discipline the design doc was held to: don't let it drift.** When a numeric constant or mechanic changes, update the wiki's relevant section in the same session, not "later" — recording the decision in this file is not enough to keep the wiki in sync, exactly the failure mode that got the design doc deprecated in the first place.
- Avoid click-fest mechanics — cap player actions per phase (a fixed number of moves/commitments) rather than allowing unlimited repeated clicking with diminishing returns. Every action should have real opportunity cost against the others available that phase.
- Regional/group dominance bonuses require **every single state** in the group to individually cross 50% popularity — not a seat-weighted average. Confirmed intentional in the desktop app's own logic; treat this as deliberate design, not a bug to loosen.
- **That AND-gate combined with a flat/never-exhausting investment cost lets a cheap group member become a permanently-free veto on the whole group's payout — check for this whenever group tagging or the investment-cost/decay formula changes.** Concrete break, found 2026-08-24 from a user report of dumping thousands of Cr into UTs without winning any group: small UTs (e.g. Puducherry, 1 seat) are deliberately tagged into big multi-state groups (`SouthIndia`, `CoastalIndia`) so they can't be ignored, but the old investment-boost curve (`investmentBoostBps` in `mobile/engine.js`) decayed for 20 taps then locked at a permanent 200bps floor forever — so re-suppressing that one cheap state cost a flat 10Cr/tap indefinitely, letting it deny a group worth hundreds of Cr at near-zero ongoing cost. Fixed by having the curve keep decaying geometrically past tap 20 instead of flattening (`boostDecayRate: 0.85` in `game-config.json`) — AI-vs-AI simulation (1,260-game full politician matrix) confirmed this closes the gap (UT-linked groups' win rate 4.1%→5.6%, now matching UT-free groups) without shifting overall win rate. This also explains why the AI appears to "camp" cheap UTs — `scoreInvestState`'s boost-per-crore scoring has no exhaustion, so a cheap-to-recontest state is its best score every tick forever; not deliberate denial logic, an emergent side effect of the greedy formula. See `docs/wiki.html`'s "Money, rallies & agendas" section for the current curve and worked numbers.
- **Added 2026-08-25: a group still held at the *start* of a phase now also pays a smaller, flat, recurring bonus** (`holdingBonusCrPerSeat: 5` in `game-config.json`'s `mobileEconomy.regionalDominance`, currently matching the instant one-time bonus 1:1) — thematically "sustained popularity keeps drawing fundraising," distinct from the existing instant/event-based bonus on first crossing 50%. Implemented as `dominanceHoldingPayoutCr()`/`applyGroupHoldingBonus()`, called once per phase boundary from `startPhase()` only — never from the shared `applyPayouts()` wrapper other mid-phase actions call, since this must fire once per phase, not on every action. **Deliberately flat, not growing the longer a group is held** — the user explicitly wanted no compounding/snowball risk. This raises the payoff for sniping an opponent's already-completed group (denying an ongoing income stream, not just a one-time bonus) — confirmed by the user as an intentional, distinct dynamic from the "spam a cheap UT forever" problem the decay-curve fix above addresses; a single well-timed snipe has real opportunity cost and isn't the same failure mode as unbounded repeat-tapping. **Balance-sim methodology note: a symmetric bonus (both players get it identically) won't move win rate — that's expected, not a sign the bonus does nothing.** Swept 2-6 Cr/seat (840-game full politician matrix each) and win rate stayed flat (~52-53%) throughout; the real signal was hung-parliament rate falling (80.4%→78.7%) and seat leads widening modestly (avg +5%, blowout games roughly doubling) as magnitude climbed — check *those* metrics, not win rate, when balance-testing any other symmetric (non-zero-sum-between-players) mechanic in the future. **That same symmetric-bonus blind spot has a sharper edge than the sweep above caught: the bonus is paid per-group, not deduplicated across a state that belongs to several groups at once** (15 groups exist, several overlapping and 200+ seats each), so an *asymmetric* capture — one side sweeping most groups, the other none — can mean 1,000+ "group-seats" worth of payout every phase, entirely one-sided. AI-vs-AI balance-sim can't surface this at all, since it requires one side to actually dominate group capture the way a human beating a passive/greedy AI does. Found 2026-08-26 from a user report of a 341-143 blowout after capturing 7 of 15 groups against an AI that captured 0 — dialed `holdingBonusCrPerSeat` down again, 2→1 (v1.2.7, shipped alongside the AI rally-cap fix), on the user's explicit direction rather than a fresh balance-sim sweep, since the failure mode this addresses only shows up in human-vs-AI asymmetric play, not the symmetric AI-vs-AI harness. **Update, same day: 1 Cr/seat still produced a blowout report — halved again to 0.5 (v1.3.1)**, again on direct user instruction ("halve it further"), no additional diagnosis requested. `dominanceHoldingPayoutCr()` in `mobile/engine.js` now rounds its result, since a fractional per-seat rate combined with several groups' odd seat counts would otherwise show a fractional Cr amount in the log/toast (e.g. "+₹50.5Cr"). If a report of blowout results persists even at 0.5 Cr/seat, the bonus magnitude is probably not the real lever left to pull — check instead whether `group-bonus-rusher` (the only AI profile that actively contests regional-dominance groups) is being picked often enough, since a human can rack up a 7-0 group split against any of the other three profiles regardless of this bonus's size.
- For game-economy/balance work (costs, bonuses, decay curves, dominance payouts), reference the **desktop app's real code** (`game-config.js`, `state-info.js`, `campaign-spending.js`, `home-state-bonus.js`, `group-rewards.js`, `state-groups.js`) as ground truth, not mobile's ported version — mobile's economy code is known broken/incomplete in several places (e.g. `checkRegionalDominanceBonuses()` still references retired region fields) and is being actively rewired.
- **Exception, decided 2026-07-21, numbers updated 2026-07-24:** mobile's base economy *scale* (starting funds 5,000, refresh 2,500/phase per player, cost = seats×10) is now independently canonical — it is **not** benchmarked against desktop's numbers (200/200/seats×1) and that comparison is closed, don't reopen it. The 2026-07-24 bump (from 2,500 starting/1,000 refresh) was a direct response to the user's own multi-round playtesting finding insufficient funds to reach a majority win against the AI. This exception is scale only; formula *shapes* (the redistribution rule, the agenda effect formula) still follow desktop's real code as ground truth unless a specific decision says otherwise. See `docs/wiki.html`'s "Money, rallies & agendas" section for the full current formula set and open items.
- **A policy's pull on a region is a per-(policy, region) magnitude, not one shared value for the whole policy** — e.g. National Defense pulls +12 in Eastern Border and −12 in Coastal India, different regions at different (even opposite-sign) strengths for the same policy. `data/policy-tags.json` encodes this directly as `tagEffects: { region: signedMagnitude }` (migrated 2026-07-22 — the old `tier`/`baseMagnitude`/`supportTags`/`opposeTags` shape is gone). Don't reintroduce a policy-wide constant; see `docs/wiki.html`'s "Money, rallies & agendas" section for the current schema and formula.
- **As of 2026-08-26, every `tagEffects` magnitude in the roster is one of exactly ±4/±8/±12** (a small set of prior ±6/±10 outliers were normalized onto this convention — see the same-state-cancellation update above for the tradeoffs that involved). **`tagEffects`'s region keys are exactly the same 15 strings as the regional-dominance groups' own `key` field** (confirmed 1:1 against `states_data.json`'s region columns) — so any UI that needs "how strongly does this policy pull on group G" should read `policy.tagEffects[G.key]` directly, never re-derive it by simulating a tap's actual redistribution outcome (which produces a much larger, redistribution-shaped number in the hundreds-to-thousands of bps, not the clean ±4/±8/±12 the data actually encodes — an early draft of the emoji-reaction feature below made exactly this mistake before being corrected). `mobile/game.js`'s `agendaGroupEffects(game, policyName)` is the shared helper for this.
- **Added 2026-08-26: agenda emoji reactions** — tapping an agenda the human player controls briefly pops an emoji over each affected region group's chip in the groups bar (`mobile/main.js`'s `spawnGroupEmojiReactions()`/`spawnGroupEmojiReaction()`, new `.fx-group-emoji` CSS in `mobile/index.html`), gradient-scaled by the tapped agenda's real per-region magnitude via `agendaGroupEffects()` above: 🙂/🙁 for ±4, 😄/😟 for ±8, 😍/😡 for ±12. AI (p2) agenda taps don't trigger this — human-only by design, confirmed with the user.
- **When a state carries several of a policy's tags at once (some supporting, some opposing), sum them into one net value first, then apply that single net through the redistribution engine exactly once — never apply each tag as its own separate transaction.** The 100%-cap and the proportional opponent/Others split are both non-linear per transaction, so sequential opposite-signed transactions don't cancel out the same way a single net value does, even when their magnitudes are equal on paper. See `docs/wiki.html`'s "The map & the 100% rule" section, "net first, apply once."
- **A special power's stated cost must be a genuine resource sacrifice — funds, popularity, or rally tokens — never a timing gate ("usable only in the final phase") or an eligibility condition ("only usable while below national-average popularity").** Both of those cost nothing whenever the power is actually usable, which breaks the "matched cost + benefit" rule for special powers. Two powers shipped with exactly this bug (Tendulkar's National Icon, Hema Malini's Star Power Rally) before being fixed 2026-07-22. **Explicit exception, decided 2026-07-26: Sardar Patel's Iron Unification cannot activate before phase 5** (`requiresMinPhase: 5`, a new engine mechanism in `mobile/game.js`'s `activatePower`/AI `canPay` check), purely for flavor ("mimicking how long real unification took") — a deliberate, acknowledged violation of this rule, not an oversight to "fix" back. **Extended the same day to four more politicians, each with its own real-world timing story**: Narendra Modi's Demonetization (`requiresMinPhase: 3` — the real 2016 shock landed mid-term, not week one), Indira Gandhi's National Emergency (`requiresMinPhase: 5` — the real Emergency came deep into her tenure), B.R. Ambedkar's Constitutional Reform (`requiresMinPhase: 3` — the real Constitution took ~3 years to draft), P.V. Narasimha Rao's Minority Government Survival (`requiresMinPhase: 6` — governing without a majority only means something once one is starting to look out of reach). Don't add further timing-gate costs to other powers without the same explicit call-out.
- **A duration-based special power that lasts "one phase" must set its expiry field to the current `game.phase` at activation, never `game.phase + 1`.** This bans a delayed "triggers at the start of the next phase" pattern for exactly this reason — the effect must self-clear the moment `game.phase` moves past it, which requires it to already be active during the activation phase itself. Modi's Demonetization shipped with this exact off-by-one (`fundsFrozenUntilPhase = game.phase + 1`), letting the AI opponent invest/tap normally for the rest of the phase it was used in before finally freezing the *following* phase — found 2026-07-28 via a diagnostic script directly exercising `investCash`/`tapAgenda`/`aiStep`, fixed to `game.phase`. Check this specifically whenever a new one-phase-duration power is added. **Update 2026-08-21: Modi's Demonetization was deliberately extended to a genuine two-phase duration** (as part of a balance rework), which legitimately uses `fundsFrozenUntilPhase = game.phase + 1` again — but paired with `fundsFrozen()` changed from an equality check (`=== game.phase`) to a range check (`game.phase <= pl.fundsFrozenUntilPhase`), so it covers the activation phase *and* the next one, not a delayed single-phase trigger. Don't mistake this for the banned off-by-one pattern reappearing — the distinguishing test is whether the freeze is active immediately upon activation (it is, here) versus only starting the following phase (the banned bug).
- **When a region-tagged policy's seat-equivalent comes out sharply negative, check first whether it's an accidental same-state overlap between that policy's own support and oppose tags** (the same failure pattern as pre-fix National Defense and Hindi Language: a state tagged with both a support region and an unrelated oppose region cancels to zero, silently zeroing out what should be a strong supporter) **before concluding the negativity is deliberate polarization.** Several policies (Land Reforms, Public Sector, Uniform Civil Code, Caste Reservation, Indigenous Rights, Agricultural Reforms, Secularism) haven't had this check yet as of 2026-07-22. **Update 2026-07-26: these seven have now had that overlap check — none showed accidental cancellation, but a *separate*, previously-unperformed check (below) found real problems in three of them.** **Update 2026-08-26: 5 same-state cancellations were deliberately reintroduced** when normalizing `tagEffects` magnitudes onto the ±4/±8/±12 convention — accepted as a known, explicit tradeoff on the user's own direction ("some collisions are inevitable"), not an oversight: **Administrative Reform** nets to zero in Madhya Pradesh (`AgriculturalRegion:-4` + `IndustrialCorridor:8` + `TribalLands:-4`); **Environmental Conservation** nets to zero in Jharkhand (`IndustrialCorridor:-8` + `TribalLands:8` — this specific pairing had been the whole reason `IndustrialCorridor` was previously set to -6 instead of -8, so this is a direct, known reversal of that earlier fix); **Nationalization** nets to zero in Andhra Pradesh, Tamil Nadu, and West Bengal (`CoastalIndia:-8` vs. `AgriculturalRegion:8`/`Manufacturing:8`). Don't "re-fix" these without checking with the user first — they're intentional as of this date.
- **A policy's `tagEffects` sign and region choice should also be sanity-checked against real-world Indian political geography, as a distinct pass from magnitude balance or same-state-cancellation** — a policy can have internally-consistent, well-balanced numbers that still point the wrong direction thematically. Found via this check 2026-07-26: Public Sector had industrial-vs-agricultural backwards (PSU jobs/union politics are concentrated *in* industrial states, not agricultural ones); National Defense's `CoastalIndia` negative had no real-world story (coastal India has its own major security stake) and was replaced with a lighter `MinorityAreas` penalty; Caste Reservation conflated caste politics with religious-minority politics and omitted South India entirely despite the Dravidian movement's historically strong pro-reservation tradition. Run this check (does the sign/region choice match a real Indian political fault line?) any time a policy's tags are touched, in addition to the existing overlap/magnitude checks above.
- **Popularity/redistribution state should be tracked in integer basis points (bps, 0–10000), never float percentages**, once the redistribution engine is actually implemented in code. Splitting a change between two recipients must round one side and derive the other by subtraction — never round both independently — to guarantee the three shares (yours/opponent's/Others) always sum to exactly 10,000. See `docs/wiki.html`'s "The map & the 100% rule" section for the full worked example.
- **This same "never round independently" discipline extends beyond 2-way splits to any N-way conversion of bps shares into whole units** (e.g. a state's seats, split across P1/P2/Others) — plain `round()` applied to each share separately can over- or under-allocate the total (concrete break: a 3-seat state at 50%/25%/25% independently rounds to 2+1+1 = 4 seats). Use **largest-remainder apportionment** (Hamilton's method: give each share `floor(quota)`, then hand out the leftover units to whichever shares have the largest fractional remainder) instead — it guarantees the parts always sum exactly to the whole. Decided 2026-07-23 for seat conversion specifically; see `docs/wiki.html`'s "The map & the 100% rule" section for the worked example and pseudocode.
- **A greedy per-action scoring formula (cost-efficiency, boost/cost, etc.) must account for remaining headroom/diminishing returns, not just raw cost-efficiency — otherwise it gets stuck endlessly re-targeting an already-maxed option instead of moving on.** Concrete break: `mobile/game.js`'s AI investment scoring (`boost/cost`) always favored the cheapest state in a group regardless of how close that state already was to 100% ownership, so the AI kept dumping funds into an already-maxed state instead of spreading across the group — fixed by scoring on effective remaining headroom (`min(boost, 10000-currentShare)/cost`) instead of raw boost. Found 2026-07-23 while stress-testing the group-based AI investment targeting.
- **That same greedy-scoring discipline has a second, distinct failure mode beyond headroom: a per-Cr or per-token efficiency score must also be normalized by what's actually being bought (e.g. seats), not left as raw %-per-Cr — otherwise it silently favors whichever option is cheapest per attempt regardless of size.** Concrete break: `scoreInvestState`'s `effectiveGain/cost` scored a 1-seat UT at 20–50 and Uttar Pradesh (80 seats) at 0.25–0.6 even on UP's very first, most-boosted tap — cost scales with seat count but the % boost per tap doesn't, so the AI (and a human chasing the same "biggest % shown" intuition) hammered tiny states that often don't even convert to a seat, instead of spreading nationwide the way the design doc's own cost-per-seat invariance claim assumes. Found 2026-07-25; fixed as part of the broader AI investment-targeting rework.
- **An AI targeting function must not return a strict argmax when it draws from a pool the human also draws from under a *shared* cap — prefer a value-weighted random draw.** Two separate problems, and the second is invisible from reading the function: the bot plays identically every game (boring to play against), and it *consumes the human's access* to the best targets before they can be reached. Concrete break, found 2026-09-01 from a user report: `pickDisciplinedRallyTarget` (`mobile/ai.js`) returned the largest open state, so every rung from level 2 up rallied Uttar Pradesh then Maharashtra in literally every match, and `rally.maxPlaysPerStateShared` (2) is shared across both players, so the AI was eating the human's rally slots in the biggest states. Fixed with `weightedRallyPick`, a seat-weighted draw over the states clearing the same economic bar — 16 distinct states rallied instead of 2, top two down to 13.8% each. **Measured cost: none** — the ladder re-measured over 2,240 games kept its exact ordering and its monotonicity check started passing outright, because value scales linearly with seats and so the weighting preserves expected value. **No strength metric will ever surface this class of problem** — a stronger-but-predictable bot scores better on every seat-margin measurement — so it has to be caught by reading the picker or by playing the game. `pickAIPowerTarget` is still a strict argmax; acceptable only because it competes for no shared cap.
- **Whenever the human interface restricts an action (a batch-only button, a disabled tap target, a UI-level gate), check whether the AI's engine-level code has the same restriction — it usually doesn't by default, since `mobile/game.js`'s AI functions bypass `main.js`'s DOM/button layer entirely and act directly on `game` state.** Concrete break: the human can only invest in 5 of the small UTs (Chandigarh, Dadra & Nagar Haveli/Daman & Diu, Puducherry, Lakshadweep, Andaman & Nicobar) as an all-or-nothing batch via the "Small UTs" button, but `pickAIInvestmentTarget`/`scoreInvestState` treated each as a normal individually-scoreable target — letting the AI cheaply snipe just one (e.g. Puducherry) to deny a regional-dominance group for a fraction of what the human has to spend contesting it back. Found 2026-08-24 from a user report; fixed via `SMALL_UT_BATCH_IDS` forcing the AI into the same batch investment in `aiStep`. Check this pattern any time a new UI-only restriction is added (or a new AI capability gap is reported) — grep the AI's targeting/scoring functions for the same states/scope the restricted UI element covers.
- **Any future change to the AI's own strength or behavior must preserve even, human-comparable real-time pacing — never give it an instant catch-up burst or other advantage a human player can't match at normal play speed.** An early fix for the AI leaving funds unspent by phase end used `G.runAIFull()` right before each phase closed as an instant catch-up burst; the user explicitly rejected this as unfair machine-speed cheating and asked for even pacing throughout the phase instead. AI strength should come from decision quality (what it targets, how it prioritizes), not from acting faster than a human physically can. Decided 2026-07-25 — see `mobile/main.js`'s `planAITickPacing()`.
- **Fixed 2026-08-26: the AI was hard-capped to exactly one rally per phase, on top of (not instead of) the real per-phase spend cap.** `aiStep`'s rally branch (`mobile/game.js`) gated on a since-removed `aiRalliedThisPhase` boolean in addition to the correct check (`tokensSpentThisPhase < maxTokenSpendPerPhase`, currently 2 — the same cap a human plays under), so every AI used at most half the rally budget a human can, every game, regardless of personality profile. This also meant the `rally-spammer` profile (`craftsTokens: false`) could bank surplus tokens it would never spend on anything, since it doesn't rally more than the other profiles and never crafts toward the Nationwide Rally either — a naming/behavior mismatch worth revisiting separately. Found from a user report of a 341-143 blowout where the AI logged only 5 rallies (human: 16) and never used its Nationwide Rally despite apparently "saving up" for it. Removed the flag entirely rather than raising it to 2, since the real limit already lived in `playRallyToken()`'s own spend-cap check — the flag was pure redundant over-restriction, not a second intentional limit.
- **Decided 2026-07-25 (superseding a same-day reversal): the 4 celebrity/non-politician picks (Sachin Tendulkar, Hema Malini, Rajinikanth, Amitabh Bachchan) are deliberately stronger and quirkier than the political roster, not weaker, and not balance-equal to it.** They're explicitly allowed to exceed what a political-roster power does (including Vajpayee's Pokhran Test, previously treated as "the ceiling nothing else reaches" — that no longer holds against this group specifically). Each carries a distinctive mechanic shape (Tendulkar: a 30%-floor `toBps` effect rather than a flat delta; Hema Malini: a stacked nationwide+home bonus; Rajinikanth: an asymmetric dual-region bonus; Bachchan: an "Others"-only source that's high-ceiling but self-limiting). Don't flag one of them out-scaling a political-roster power in the same region (e.g. Rajinikanth vs. Jayalalithaa in South India) as a bug — that's the intended outcome of this decision. **Update 2026-07-28 — clarified scope after a real-money balance pass: "OP" was always meant to describe the special power specifically (these 4, plus the film-persona entry "Sivaji Rao"), not the agenda kit riding alongside it.** All 5 had ended up with both a top-tier agenda kit (national avg 13–16, the highest in the roster) *and* a strong/free power stacked together — an unintentional tuning error, not the design intent (Sivaji Rao's One-Day Ordinance in particular had zero cost of any kind while instantly maxing a 48-seat state). Rebalancing these 5 must go through the **special power's cost/timing/magnitude only — never their agenda kit**, which is a deliberate thematic match and shouldn't be touched. Current post-rebalance numbers (`data/politicians-data.json`): Bachchan 6% nationwide / ₹2,000cr / `requiresMinPhase: 6` (down from 12%/₹1,000cr/no gate); Hema Malini 2% nationwide + 14% home bonus (16% total at home, unchanged) / ₹2,500cr (down from 8%/₹500cr); Rajinikanth +22%/+15% (trimmed from +25%/+18% 2026-08-21) and now costs -14% Hindi Heartland (deepened from -8%) / ₹2,000cr (tried ₹3,000cr the same day, reverted once the deepened penalty + magnitude trim alone got win rate down to 59%); Sivaji Rao unchanged 100%-home-state effect but now costs all future rally-token income for the rest of the match. Don't reintroduce a funds-cost-only-plus-nerfed-magnitude version of any of these without also checking cost/timing. See `docs/wiki.html`'s Special powers section for the current numbers and reasoning.
- **A "steal"-style power must make an explicit, deliberate choice between seize (confiscate — the opponent's resources are destroyed, `seizeFundsPct`/`seizeTokens`) and steal (transfer — the opponent's resources move to the activator, `stealFundsPct`/`stealTokens`), never default to whichever primitive happens to already exist.** Kejriwal's Anti-Corruption Raid, Indira Gandhi's National Emergency, and Sivaji Rao's original One-Day Ordinance were all built on the transfer primitives by default; corrected 2026-07-26 to `seize*` once it was clarified that "raid"/"seizure"/"confiscation" framing means the opponent's resources vanish, not that they move to you. This isn't just wording — it changes the power's real value, so re-check the self-cost magnitude whenever switching between the two.
- **A special power that needs to target the exact states in one of the mobile HUD's quick-invest button groups (e.g. `#neBtn`'s Northeast-8) should use the `svgIds` scope in `resolvePowerScope`** (`mobile/game.js`), passing the same explicit svgId list the button itself uses (e.g. `NORTHEAST_IDS`) — not a broader `tags` scope that happens to overlap. Added 2026-08-22 for Mamata Banerjee's State Autonomy Stand: the existing `EasternBorder` tag also covers Bihar/UP/Uttarakhand/HP/West Bengal, well beyond the 8-state Northeast group the power was actually meant to target. `tags` scope remains correct for effects that are genuinely about a `states_data.json` region property rather than mirroring a specific UI button's coverage.
- **A politician can have more than one `homeState`** via the additive `secondaryHomeStates` array in `data/politicians-data.json` — added 2026-07-25 for Kejriwal (`["Punjab"]`, alongside his primary Delhi) because Delhi's 7 Lok Sabha seats made the standard +25% home-state starting bonus far weaker in absolute terms than politicians whose home is a bigger state. Every consumer of `homeState` (`mobile/engine.js`'s starting-position generator, `mobile/game.js`'s `resolvePowerScope` for the `home`/`opponentHome` special-power scopes, `mobile/main.js`'s display line) must read through the `homeStatesOf()`/equivalent helper rather than the raw `homeState` field directly, or a politician with a second home silently loses the bonus in one of the three places. The existing "shared home state nullifies the bonus for both players" rule now applies per overlapping state, not per player — an unrelated second home state isn't nullified just because one of a player's homes happens to collide with the opponent's.
- **A second home state is a legitimate, deliberate way to give a politician (especially a celebrity, per the "different OP axes" principle above) a real starting-position edge beyond their special power — but `generateStartingPosition()`'s random-draw budget must account for it, or the ~150-seat mean / ~175-at-2-sigma target silently breaks.** Found 2026-08-26 from a user report of Hema Malini (Uttar Pradesh + Tamil Nadu home) starting a real game with 213 seats: home states were fully excluded from the draw pool and got their +25% bonus for free, on top of a draw budget (the 124/154 thresholds) sized as if starting from zero — invisible for a typical single small/medium home, but it pushed UP-alone's mean to ~156.6 and UP+Tamil-Nadu's to ~166.2 (max ~199). Initially "fixed" by just deleting Hema Malini's second home state, until the user pushed back: it was added deliberately (2026-07-26) both for thematic accuracy (born in Tamil Nadu, represents Mathura/UP) and to give her a distinct axis of OP-ness from the other celebrities — the real bug was the generator's math, not her data. Root-cause fix: seed the draw-budget counter with **25% of each player's own home-state seats** (2500/10000 BPS, the home bonus's own magnitude — apportionment scales roughly linearly with a flat vote-share addition, confirmed by direct simulation across several home-state sizes) instead of zero, so a big or multi-home politician simply gets a smaller subsequent draw allotment rather than the home bonus stacking uncompensated. Restores UP-only to ~149.8 mean and Hema Malini's UP+TN to ~156.1 mean / ~177 at 2-sigma. **When adding or resizing a home state (primary or secondary) for any politician, re-run this same kind of direct-simulation check** (sample `generateStartingPosition()` + `nationalSeats()` across many seeds, not a single playtest game) rather than reasoning from the flat +25% bonus number alone.
- **When tuning a randomized game mechanic's parameters (e.g. the starting-position generator's advantage-draw thresholds), verify the resulting distribution via direct mean/σ simulation — not inference from percentiles, or intuition about which knob controls what.** Confirmed 2026-07-28 tuning the starting-position generator to a precise target (mean 150 seats, σ~12.5, so ~125/~175 land at ~2σ): several knobs (the baseline population range, the advantage-draw stop threshold, the advantage-draw popularity range) all shift the mean, but only direct measurement showed that raising the advantage-draw stop threshold (`drawStopAt`/`drawBudget` in `mobile/engine.js`) shifts the mean *without* widening σ — the stopping rule is self-correcting (a player who draws a run of small states just needs more draws before crossing the threshold), which keeps σ stable regardless of where the threshold sits. Don't assume from percentile spacing or first-principles reasoning which knob controls spread vs. center — measure it before and after.
- **For AI-strength questions — how strong a bot is, or whether one profile beats another — run `mobile/ladder-sim.js`** (`node mobile/ladder-sim.js 30`), not `balance-sim.js`. `balance-sim.js` measures *politicians*: it never passes a profile to `setupAI` (so both seats get the same bot) and aggregates on politician id, so nothing in its output is attributable to an AI profile. `ladder-sim.js` is a full round-robin over profiles scored on **seat margin, not win rate** (hung parliaments make win rate mostly draw signal), with **mirrored pairs** — each matchup plays the same two politicians in the same two seats twice with the bots swapped, so seat advantage and politician-kit gaps cancel. It also prints a monotonicity check, which is the only thing that catches a rung that loses to the rung below it. **Never read a single-flag delta from the chain ablation as that flag's value** — it is that flag's value *given every flag below it*, and two interacting flags show up entirely on whichever was added last (confirmed 2026-08-31: `smartGroupTarget` and `spreadInvest` each measure near zero alone and 147 seats together). Re-run after ANY change to shared `aiStep` logic — a shared-code change shifts every rung at once.
- **For politician/agenda balance questions, run `mobile/balance-sim.js`** (`node mobile/balance-sim.js 5`, or higher for more confidence) rather than relying on manual small-sample play — re-run after any politician/agenda tweak. **Read win rate, not avg final seats, as the primary metric.** See `design/testing-and-balance-notes.md` for the full methodology (why AI-vs-AI replaced naive-vs-AI, how hung parliament is scored) and three known methodology traps to avoid when comparing before/after win rates — including a 2026-08-21 finding that this AI-vs-AI harness undervalues conditional/exploit-dependent powers relative to unconditional/guaranteed ones (confirmed on Modi's Demonetization).
- **Hung parliament is always a draw** (`mobile/game.js`'s `finalizeGame()`, ADR-0010 — supersedes ADR-0006, which gave the AI a tie-break advantage on the assumption ties would be rare). Revised 2026-07-28 once the balance-sim measurements above showed hung parliament actually occurs in 48–98% of games per politician — with the roster tuned to be harder to win outright, defaulting every undecided match to an AI win would make "the AI silently wins" the modal outcome, not a rare edge case. **When changing what a `game.winner`/game-state outcome means, grep for every place that independently re-derives the same conclusion, not just where `game.winner` is read** — `mobile/main.js`'s `showEndOverlay()` had its own separate `game.players.p2.isAI` check driving the end-screen headline that would have kept showing "you lose" even after `finalizeGame()` was fixed, since it never actually consulted `game.winner` for this branch. This project has hit this "two places must agree" failure mode before (agenda effect text vs. real benefits/costs, the `[hidden]` CSS attribute rule) — treat it as a standing checklist item whenever a game-outcome rule changes.
- **A special power's deliberate-max seat swing should stay at or under ~50 seats, measured via a realistic full-game AI-vs-AI playthrough (not a blind average and not a frozen starting position).** Established 2026-08-28 after a user playtest of a redesigned Rahul Gandhi power (Bharat Jodo Yatra: arms the Nationwide Rally with a per-phase-elapsed bonus) hit 80 seats on an easily-reachable line (activate phase 1, deploy phase 9) and was flagged as "way too high." Two other existing powers were found to already exceed this — Nitish Kumar's Alliance Switch (±700bps, 55–61 seats) and Atal Bihari Vajpayee's Pokhran Test (+1000bps, zero cost, 52–56 seats) — both flat/nationwide-scope powers where timing doesn't matter, unlike Rahul's deferred-growth design. Retuned to ±500bps and +800bps respectively, landing both around 44–45 seats, roughly matching Lal Bahadur Shastri's existing ~40–44 (left unchanged, already under the ceiling). When adding or reworking any power, measure its realistic seat swing the same way (create a real game, run both sides via AI for several phases, then activate the power and diff `E.nationalSeats()` before/after) before shipping it.
- **When building or editing a politician's agenda kit, check its net effect in that politician's own home state specifically** — not just overall kit balance or the existing same-state-cancellation/geography-sanity checks. Found 2026-08-28: Rahul Gandhi's pre-existing kit (Rural Development/Press Freedom/Secularism/Caste Reservation) netted -4bps in his own home state (Uttar Pradesh) because Secularism's -12 HindiHeartland/-12 Pilgrimage penalty applied in full there while its +12 MinorityAreas benefit never did (UP isn't a MinorityAreas state) — this was the real, dominant cause of his dead-last win rate in `mobile/balance-sim.js`, not his special power. A tested fix (swapping Secularism for Food Security) took his win rate from 38.4% to 57.2% and home popularity from 29.4% to 58.1%, but was reverted at the user's request in favor of a power redesign instead — so this specific fix is not currently shipped; re-check `avgHomePopPct` in balance-sim output whenever a politician's win rate looks anomalously low.
- **The politician-unlock progression (added 2026-08-24, local-only, `localStorage`) is deliberately client-side-enforced and bypassable — a player who edits `localStorage` or the DOM via devtools can unlock everything instantly, and that's accepted, not a bug to fix.** This is single-player-vs-AI with no leaderboard or real-money stake, so a bypass only "cheats" the player themselves; real enforcement would require a server-authoritative account/backend, the same tier of work already deferred for multiplayer. Don't propose obfuscating/hardening the client-side check — that can't actually close the hole (any client-side gate is editable by definition) and isn't worth the effort until real accounts exist. See `mobile/main.js`'s `loadUnlockedPoliticians`/`unlockPolitician`/`isPoliticianUnlocked`.

**Archived section: Local development & testing**
- **Measured 2026-08-29 (v2.1.0 audit) and confirmed NOT performance bottlenecks — don't re-audit these without new evidence.** Engine math is microseconds against real `createGame` state: `investCash` with its full payout sweep **0.27 µs**, `dominanceActive` across all 15 groups × 2 players **11.4 µs**, `nationalSeats` **19.1 µs**, `aiStep` **26.0 µs**. `planAITickPacing`'s `structuredClone` + 500-step dry run is **0.3 ms**, ten times a game (it looks like an obvious per-phase hitch; it isn't). The inline India map SVG is already at 1 decimal place, so rounding its coordinates saves **literally zero bytes** (it gzips to 65 KB with the rest of `index.html`). DOM (~1,925 nodes in game) and CSS (334 rules / 40 KB) are both healthy. This project's performance cost lives in **assets and DOM work**, not game logic — start there.
- **When handing the user a URL to test `npm run serve` (port 8934) on a phone or other device, never give a bare `localhost` URL — it only resolves on this machine, not the device testing it.** Default to a `cloudflared tunnel --url http://localhost:8934` (`npx -y cloudflared tunnel --url http://localhost:8934` — no install needed, works over any network) and hand back the `https://*.trycloudflare.com` URL it prints. Only fall back to the machine's LAN IPv4 (`ipconfig`, the Wi-Fi adapter, not the `vEthernet`/WSL one) if cloudflared isn't available — and if LAN IP still doesn't reach the phone, check `design/testing-and-balance-notes.md`'s Windows network-category (Private vs Public) note before assuming it's a firewall/port problem. Confirmed 2026-08-23 after defaulting to `localhost` first and the user having to ask for this explicitly.

**Archived section: Frontend technical rules**
- **Never set `touch-action:manipulation` in `mobile/index.html` — it still permits pinch-zoom** (the spec value only blocks double-tap-to-zoom, pinch-zoom stays enabled under `manipulation`). Confirmed 2026-08-24 after the user reported the app randomly zooming out during fast two-thumb play — the global `*{touch-action:manipulation}` rule was the cause. Fixed to `touch-action:pan-x pan-y`, which blocks pinch-zoom too while still permitting the app's two genuine native-scroll spots (politician-select carousel swipe, share-sheet horizontal scroll). (Historical note: this bullet used to warn against adding a `<meta name="viewport">` tag — that warning is void as of v2.0.0, which added a real `width=device-width` tag and migrated the whole stylesheet off the 2.5x-authoring convention. Keep the `pan-x pan-y` value regardless — pinch-zoom is still unwanted during fast play.)
- Never rely on CSS Grid shrink-to-fit with `1fr` tracks when a declared pixel/icon size needs to actually hold — the grid container's width goes undefined and the browser silently overrides the item's declared size to fit an accidental viewport computation. Give the grid container an explicit, screen-relative width (e.g. `min(calc(100% - Npx), cap)`) instead. This already caused a real, hard-to-spot sizing bug in a mobile prototype's icon grid.
- Every standalone HTML file (prototype or real game) must declare `<meta charset="UTF-8">` as the very first tag. Claude's Artifact hosting sets the correct charset header automatically, but any other host (a local dev server, GitHub Pages, etc.) may not — without the explicit tag, every emoji, the ₹ symbol, and em dashes render as mojibake.
- When sizing mobile UI chrome (buttons, text, icons) in a prototype meant to represent real device proportions, calibrate against the actual iPhone 14 viewport (390×844, this project's default target) — not against how it looks inside the Claude Artifact viewer. The Artifact viewer wraps pages in its own ~150–200px title-bar chrome, which makes a `flex:1` map region look shorter than it will on a real device, and everything sized to "look right" against that preview reads as too small once viewed full-screen.
- A collapsible tray/panel (`max-height:0; overflow:hidden` to hide, animated open) still occupies its full declared *width* as a flex sibling even while collapsed to zero height — this silently inflates the gap between adjacent elements in the same flex row. Take it out of normal flow with `position:absolute` (anchored to a `position:relative` wrapper around just its own toggle button) instead of relying on collapsed height alone to make it width-neutral.
- For alternating-row hex/brick (honeycomb) layouts, give neighboring rows differing item counts (N, N−1) and center each row independently (`justify-content:center`) — no manual horizontal offset/margin needed. An N-item row and an (N−1)-item row, both centered on the same container width, differ by exactly half a hex+gap pitch, which is the correct honeycomb interlock offset for free.
- Safari-specific mobile bugs (viewport meta behavior, safe-area-insets, dynamic toolbar) must be verified with Playwright's `webkit` engine + an iPhone device profile (`devices['iPhone 14']`), not Chromium — Chromium ignores the page's own `<meta viewport>` tag even with an explicit viewport size set, so it cannot reproduce this whole class of bug.
- Playwright's `devices['iPhone 14']` preset sets `viewport: {width:390, height:664}`, not 844 — 844 is only the preset's `screen` field (the physical panel), modeling real Safari's chrome-reduced usable content area. Judge whether Booth Ink's ~755–765px fixed chrome will fit against 664px, not 844px, when reasoning from a Playwright screenshot — and treat a real device or Capacitor-wrapped build as the final check regardless, since headless webkit still isn't a perfect stand-in for Safari on this specific viewport-fallback behavior.
- **Concrete instance of the above, worth remembering specifically: a dynamic `<meta name=viewport>` tag injected via an early inline script (tested to fix iPad rendering, computing `content="width=<screen.width*2.5>, initial-scale=1"`) tested clean in Playwright/WebKit on both iPhone and iPad device profiles (screenshots + measured `visualViewport.scale` all correct) but caused a severe regression on a real iPhone in real Safari — an extreme zoomed-in crop with the title/buttons entirely off-screen.** Found 2026-08-27 within minutes of the user testing the "verified" build on their actual phone via a cloudflared tunnel. Any future fix touching viewport meta tags or the app's implicit no-tag auto-scale fallback must be verified on a real device (or Capacitor-wrapped build) before being considered done — a clean Playwright/WebKit pass is not sufficient evidence for this specific class of bug.
- Booth Ink's **frozen prototype** (`design/prototypes/pme-mobile-sheet.html`) intentionally has no `<meta name="viewport">` tag and relies on the old ~980px virtual-canvas fallback — leave it, it's a historical snapshot. The **real build** (`mobile/index.html`) migrated off this in v2.0.0: it now has a `width=device-width` tag, a fluid `rem` scale, and reflows/scrolls, so the "fixed chrome doesn't fit a device-width viewport" problem is gone. See `design/viewport-migration-plan.md` (now a done-record) for the full story.
- Booth Ink's map SVG renders 3 states (Uttarakhand `INUT`, Ladakh `INLA`, Himachal Pradesh `INHP`) as a duplicate-ID `<circle>` overlay layered on top of their real `<path>` boundary. Any map interaction code (paint, click, highlight) must query `path[id], circle[id]` together — targeting `path[id]` alone silently breaks these three states.
- Use `fill-opacity` (not `opacity`) to dim SVG map states — plain `opacity` fades the stroke/border along with the fill, which can make a dimmed state's outline disappear entirely.
- **Any element toggled via the `hidden` attribute needs an explicit `<selector>[hidden]{display:none}` rule if that same selector's base class already sets its own `display` value** — author CSS beats the browser's default `[hidden]{display:none}` UA-stylesheet rule at equal specificity, so `el.hidden = true` silently does nothing without it. Confirmed 4 times in this codebase now (`.overlay`, `.group-readout`, `.target-banner`, `.vs-bar`) — treat it as a checklist item whenever a new toggleable block gets its own `display` (flex/grid/etc.) in its base rule.
- **A decorative flex item with no intrinsic content (an empty-span color stripe, a bare spacer div) needs an explicit `width:100%` — it silently collapses to zero width whenever its flex-container parent's `align-items` isn't the default `stretch`.** The same `.tricolor` rule rendered correctly on the select-screen ballot card (parent has no `align-items` override) and was completely invisible (0×0 bounding rect) on the end-of-game card (parent sets `align-items:center` to center the seal/headline). Found 2026-07-24 — don't assume a rule ported from one flex context behaves the same in a sibling one with different alignment.
- **RESOLVED in v2.0.0 (2026-08-28): the "unplayable on iPad" issue is fixed.** `mobile/index.html` had no `<meta viewport>` tag and every size was authored at ~2.5× for the ~980px virtual-canvas auto-zoom fallback, so it was only correct at iPhone-14 portrait's ratio. The v2.0.0 rework added a real `width=device-width` tag, migrated ~330 `px` sizes onto a fluid `rem` scale (`:root` font-size `clamp(15px, 4vw, 16.5px)`, `--fs-*`/`--radius-*` now app-wide), scaled borders/shadows down ~0.6×, and made the app a centred `max-width:520px` (× `max-height:920px`) letterboxed column on anything larger. Verified on real iPhone, a modern iPad, and desktop. **Still degraded on a ~2014 iPad** (old WebKit lacks flex `gap` and `aspect-ratio`, used throughout) — out of scope, a current iPad is fine. Full record: `design/viewport-migration-plan.md`.
- **RETIRED in v2.0.0: the "author every UI at ~2.5×" convention is gone.** The whole stylesheet was migrated to a real `rem` scale. New/edited CSS in `mobile/index.html` is now authored at **real, intended pixel sizes**, expressed in `rem` (÷16) or via the `--fs-1..6` / `--radius-sm/md` tokens. Borders and box-shadows are *also* scaled now (the old "leave borders unscaled" exception was a 2.5×-era premise and no longer holds — keep them modest, ~1–2px). If you find a region still at 2.5× scale (a corner the migration script missed), divide it by ~2.5 and convert to `rem`.
- **iOS Safari blocks `.play()` on an `<audio>` element until that specific element has been played from within a real user gesture at least once — per-element, not per-page.** Confirmed 2026-07-27 debugging a report that the end-game sound wasn't playing: `game_over`, `cash_added`, and `phase_reset` are only ever triggered from a `setInterval` callback (`doEndPhase`), never a tap, so all three were silently blocked on strict mobile browsers while gesture-triggered sounds (agenda taps, rally deploys, special-power activation) worked fine. Fixed via `unlockSounds()` (`mobile/main.js`), which plays-then-pauses every `Audio` element inside the welcome screen's "Begin Campaign" click handler — the first real gesture in the app. Any future sound that only ever fires from a non-gesture context (a timer, an AI turn) must be added to that same unlock pass or it will silently never play on iOS.
- **Any fallback/alternate sound path must preserve whatever ducking state is already active, not reset volume before playing the fallback.** `playPowerSound()` (`mobile/main.js`) ducks `bg_music` to 0 before playing a politician's dedicated power-sound clip; its original `.catch()` fallback (for the 14/20 politicians without a dedicated file) restored `bg_music` to full volume *before* playing the generic `fanfare` sound — undoing the duck right when the fallback needed it most, so fanfare played over full-volume music. Found 2026-07-28 from a user report of power-sound audio "drowned out by background music." Fixed by keeping the duck applied through the fallback too, restoring only once whichever sound (dedicated or fanfare) actually finishes. Also added a shared Web Audio `GainNode` (2.5x) on power-sound playback since `HTMLAudioElement.volume` caps at 1.0 and some clips (e.g. `Rajinikanth.mp3`) are mastered too quietly to hear even with music fully ducked.
- **A new fixed-position UI element in `mobile/index.html` must be checked against `.ut-bar` and `.pol-footer`'s Play button before assuming the screen's corners/bottom edge are free.** `.ut-bar` (the stage HUD) spans the full width at `bottom:14px` — small-UT invest buttons already occupy that entire strip, corners included. `.pol-footer`'s "Play as X" button sits at the bottom of each scrollable ballot card on the select screen — its exact on-screen position depends on card scroll/content height, so it can't be assumed clear either. Confirmed 2026-08-22 building the tutorial's persistent Back/Next controls: an initial viewport-fixed bottom-corner design would have sat directly on top of both. Fixed by docking those buttons in-flow within whichever card/banner is actually showing (JS-relocated single button pair via `appendChild`) instead of `position:fixed`.
- **iOS Safari's Intelligent Tracking Prevention caps all script-writable storage (`localStorage`, Cache API) to ~7 days of inactivity for a site that isn't installed to the home screen** — a plain bookmark/tab is subject to this purge, standalone (Add to Home Screen) mode is exempt. Relevant to both the service-worker offline cache and any `localStorage`-based feature (e.g. the politician-unlock progression) — there's no code-side fix, only prompting users toward Add to Home Screen for reliability. Noted 2026-08-24 while building/discussing both of those features.
- **`<audio>` elements have real, often 100ms+, playback-start latency baked into the browser/OS pipeline — no JS-side reordering fixes this, only Web Audio's `AudioBufferSourceNode` does.** Found 2026-08-25 from a report that the invest-tap sound (`money_spent`) felt delayed: the first fix (playing the sound before `renderAll()`'s synchronous DOM work, instead of after) was real but insufficient — the residual lag was inherent `<audio>`.play() latency, invisible to any JS-level reordering since it's in the browser/OS layer. Fixed by pre-decoding the 3 tap-triggered SFX (`money_spent`/`rally_sound`/`invalid_action`) into `AudioBuffer`s on the first gesture and playing them via `AudioBufferSourceNode` (`primeTapSfx()`/`playTapSfxBuffer()` in `mobile/main.js`), falling back to the original `<audio>` path if Web Audio is unavailable or the buffer isn't ready yet. Reserve this pattern for sounds where tap-to-sound latency is actually noticeable (rapid tap-feedback SFX) — background music and non-gesture sounds (`game_over`, `cash_added`, `phase_reset`) don't need it and stay on the simpler `<audio>` path.
- **A bookmarked (not home-screen-installed) iOS Safari tab auto-suspends an `AudioContext` on any idle/background stretch, far more aggressively than a standalone PWA — and `AudioContext.resume()` is async, so starting a buffer source immediately after calling it (without confirming the context actually reached `'running'`) produces silence that gets reported as success.** Found 2026-08-25 testing the Web Audio tap-SFX fix above on a real iPhone bookmark: `playTapSfxBuffer()` called `.resume()` then immediately `.start()`'d the source and returned `true` regardless, so a tap landing while the context was still suspended went silent *and* never fell back to the working `<audio>` path, since the caller believed it had succeeded. Fixed to check `sfxCtx.state === 'running'` before claiming success; otherwise bail out (kicking off a background `resume()` for the next tap) and let the always-audible `<audio>` fallback play instead. Any future code path that claims a Web Audio action succeeded without checking the context's actual state should be treated as suspect on iOS.
- **Raster assets must be sized to their display resolution — never ship a 2048×2048 / multi-MB PNG for something rendered at ≤400px.** Found 2026-08-29 from a user report of "most politician images missing" in the live build: all 21 portraits returned HTTP 200 at full bytes and were referenced correctly, but they were 2048² PNGs, 1.5–7.9 MB each (~75 MB total), and the select carousel builds all ~21 cards at once — decoding 21 × 2048² × 4 B ≈ 350 MB blows iOS Safari's per-tab decoded-image memory cap, so Safari drops most of them and the `onerror`→colored-initial fallback fires (looking like "missing"). Fixed by `scripts/shrink-images.py` (portraits → 768px, welcome bg → 1280px, ~75 MB → ~20 MB) plus `loading="lazy"` on carousel portraits. **Update 2026-08-29 (v2.1.0) — format matters as much as dimensions: every portrait was an 8-bit RGB PNG with no alpha channel, i.e. a lossless codec for photographic art.** Re-encoding the same 768px pixels as WebP q80 took 19.75 MB → 2.05 MB (9.4×). PNG is already compressed, so GitHub Pages' gzip does nothing for it. `scripts/shrink-images.py` now emits `.webp`. Two party-logo SVGs were the same failure in vector form (`All_India_Trinamool_Congress_logo_(3).svg`: 895 KB, 130,474 coordinate points across 7 auto-traced paths, rendered as a ~30px badge) — rasterised to 96px WebP via `scripts/rasterize-logos.js`, 989 KB → 25 KB. **Coordinate rounding is not a fix for a traced SVG (2dp saved only 13%); rasterise instead.**
- **Bind game actions to `pointerdown` (via `fastTap()` in `mobile/main.js`), never `click` — a `click` cannot fire until the finger LIFTS, so every action is gated behind the player's own press duration (~70-120ms of dwell).** Found 2026-08-29 from a user report that double-tapping a state felt laggy "even if they don't appear laggy" — exactly the right description, because the sound and FX were fast once they started, they just started late. Measured event order in WebKit: `pointerdown` → `touchend` → `click`, with click landing at dwell + ~7ms. **This class of latency is structurally invisible to Playwright**, whose synthetic taps press and release in the same tick (measured 7ms end-to-end there) — don't conclude a tap path is fine because an automated test says so. `fastTap` keeps a `click` fallback deduped on a 700ms window so keyboard activation still works on real `<button>`s. Applied to the map, the five quick-invest buttons, the agenda tray, the rally/special/nationwide buttons, and the group chips — **deliberately NOT the politician carousel**, whose `click` binding is what stops a swipe from firing a card. Any new scrollable/swipeable surface must stay on `click` for the same reason.
- **Tap-feedback CSS must animate `transform`/`opacity` only — never `width`/`height`/`margin`.** `.fx-flash` animated all three, so every frame of its 450ms animation forced a layout + paint on the main thread, exactly while the player was tapping fast. Fixed 2026-08-29 by sizing the element at its END state and scaling *down* to the start (`transform:scale(.1455)` → `scale(1)`), which keeps the final frame pixel-identical. The sibling `.fx-money` was already correct — copy that one, not the old flash.
- **Batch DOM reads before writes in any per-render loop.** `renderRallyTokens()` interleaved `getBoundingClientRect()` with `appendChild` inside one loop, forcing a synchronous layout per rallied state — 2.19ms vs 0.09ms batched (**24×**) at 20 states on desktop WebKit, and it grew with every rally played. Read every rect first, then append one `DocumentFragment`.
- **Anything positioned from a JS measurement of another element's box needs a `ResizeObserver` on that element — not a call at each mutation site.** Rally dots are placed in viewport coords from `getBoundingClientRect()` on the map, so any layout shift strands them. Moving `renderRallyTokens()` last in `renderAll()` was necessary but insufficient: selecting a state, group or cluster calls `updateCard()` *directly* and never enters `renderAll()`. Concrete break found 2026-08-29 from a user report — switching to a cluster card shrank the info panel 86px→65px, moved the state centre 270.5→281.1, and left the dots 10.6px stale. Fixed with a `ResizeObserver` on `.map-wrap` (drift now 0.0px), which also covers rotation, window resize, and mobile browser chrome sliding in/out. The caller list is never complete — observe the layout source instead.
- **Never hang a DOM node off the `game` object.** `planAITickPacing()` does `structuredClone(game)`, a DOM node is not structured-cloneable, and the throw happens *after* `game.rng` has been nulled for the clone — so `game.rng` is never restored and the AI silently dies for the rest of the match. Hit 2026-08-29 while caching `.gchip` elements for `renderGroupCaptureBadges`; the fix is a module-level side map keyed by group key (`groupChipEls`), not a field on `game.groups`.
- **A `loading="lazy"` image inside a `hidden` subtree is never fetched, so its `load` event never fires — don't gate anything on it.** The welcome screen's "wait for 3 portraits" unlock waited on carousel `<img>`s inside a hidden `#selectOverlay` and therefore *always* fell through to its own 3,500ms timeout, stalling every single boot while preloading nothing (probed: 0 of 21 complete, 0 decoded, at unlock). Fixed 2026-08-29 to `new Image()`, which ignores visibility. If you need to preload something behind an overlay, `new Image()` is the tool; a lazy `<img>` is not.
- **The service worker (`mobile/sw.js`) is network-first with `{cache:'reload'}` for the app shell / code / JSON, and cache-first for media (`.png/.jpg/.svg/.mp3/…`).** Set up 2026-08-29 after a deploy kept showing the old version for up to 10 min: GitHub Pages sends `cache-control: max-age=600` on `index.html`/`*.js`, and a plain `fetch(e.request)` in a "network-first" SW still reads that browser HTTP cache. `{cache:'reload'}` bypasses it so a deploy lands on the next reload — but must NOT be applied to media, or repeat visitors re-download ~20 MB of art every load (hence the cache-first split; a media change still ships via the `CACHE` version bump in `activate()`). Don't revert either half without re-introducing one of those two bugs.

**Archived section: Deployment**
- **Live URL is now `https://pradhanmantrielectionsgame.com/`, replacing `kaunbanegapradhanmantri.in` (retired 2026-09-04).** The `.in` domain hit a NIXI `serverHold` days after registration — confirmed via Porkbun's compliance email as "inaccurate WHOIS information" (the registrant record had blank name/address fields; `.in` disallows third-party WHOIS privacy protection and requires the underlying record to be complete). Separately, "Kaun Banega X" — the game's name at the time — is a naming pattern Sony has litigated before over KBC format rights (*Sony v. OnePlus*). Both risks are specific to the old domain/name combo, not to this project's subject matter generally. The game itself was renamed to **PradhanMantri Elections Game** everywhere the old name appeared (`mobile/index.html` title/meta, `mobile/main.js`'s `SITE_URL`/share text, `mobile/privacy.html`, `scripts/deploy-mobile.js`'s `CNAME` output) in v2.7.1. DNS at Porkbun: 4 `A` records → GitHub Pages IPs (`185.199.108–111.153`) at root, `www` `CNAME` → `pradhanmantrielectionsgame.github.io`. The old `.in` domain is left registered but unused — no need to actively kill it.
- **Update 2026-08-29 — the `origin` remote has been REMOVED from this clone, and the branch list collapsed to a single `main`.** This repo is now mobile-source-only with exactly one remote, `mobile` (`github.com/pradhanmantrielectionsgame/mobile`), which carries two branches with different jobs: **`mobile/main` = the flattened deploy build** (what the live game serves, written only by `scripts/deploy-mobile.js`) and **`mobile/source` = this full source tree** (what local `main` tracks and pushes to). Push source with a plain `git push`; ship the game with `node scripts/deploy-mobile.js --push`. Never push source to `mobile/main` or the live game breaks. The old feature branches (`deploy-mobile-fresh-repo`, `mobile-ui-overhaul`, `viewport-responsive`, `perf-audit-fixes`) were all fully contained in `main` and have been deleted; `mobile-deploy` still exists but is deploy infrastructure checked out in `.deploy-worktree`, not a feature branch — leave it. **Why `origin` was removed rather than kept:** local `main` is the mobile source, which deliberately deletes the desktop game's root files, so it was 145 commits ahead of `origin/main` and a single `git push` would have wiped the live desktop site (75 files, including its 30 KB root `index.html`). Removing the remote makes that mistake impossible. The desktop repo is untouched and still live at `https://pradhanmantrielectionsgame.github.io/` — to work on it, clone it separately from `https://github.com/pradhanmantrielectionsgame/pradhanmantrielectionsgame.github.io.git`.
- **The `origin` remote (`pradhanmantrielectionsgame.github.io`) is a live GitHub Pages site with no build workflow — it serves whatever is on `main`'s root directly, no staging step.** Confirmed 2026-07-27: `mobile-ui-overhaul` was 72 commits ahead of `main` with 0 commits behind (a clean fast-forward available), but `git diff main mobile-ui-overhaul --stat` on root-level paths showed `index.html` and all 45 files under `js/` had diverged substantially (many deleted, replaced with new implementations) — not just `mobile/` being added alongside the old desktop game. Merging would have silently overwritten the live desktop game at the root URL, not added the mobile build next to it at `/mobile/`. Before ever merging a long-lived feature branch into `main` on this repo, check root-level file diffs — not just the ahead/behind commit count — and treat any push to `main` as a real public deploy, not a backup. (For remote phone-testing without touching Pages/`main` at all, a temporary tunnel — e.g. `cloudflared tunnel --url http://localhost:8934` — pointed at the local `npm run serve` works and needs no GitHub interaction.)
- **`scripts/deploy-mobile.js` strips the `../` asset prefix by literal string match, so write asset paths in the exact forms it knows — never via an intermediate variable.** It recognises `'../assets/'`, `'../data/'`, `'../sounds/'`, and in JS exactly two composed forms: `'../' + p.image` and `'../' + p.partyLogo`. A local like `'../' + src` survives the rewrite untouched and 404s on the live site, where these files sit at the root instead of one level below. Nearly shipped 2026-08-29 in v2.1.0's portrait preload — the app would still have started (`onerror` resolves the gate) but the preload would have silently done nothing while logging two 404s per visit. **Dry-run `stripRelativePaths()` over `mobile/main.js` and `mobile/index.html` and grep the output for surviving `../` before any push** — this failure is silent on the live site, not a build error.
- **`scripts/deploy-mobile.js` ships a hand-maintained allowlist of files, not everything under `mobile/` — any new runtime file must be added to its `main()` in the same commit that creates it, or it silently vanishes from the live build.** No test covers this: `npm test` and `npm run serve` both read from `mobile/`, where the file exists, so the omission is invisible until the live site breaks. Nearly shipped 2026-08-31 — `mobile/ai.js` had been extracted in v2.4.0 and never added, so a deploy would have served an `index.html` whose `<script src="./ai.js">` 404s: the game boots and the map renders, but `window.PMEAI` is undefined and the AI opponent never takes a single action. Add this to the pre-deploy checks alongside the `stripRelativePaths()` dry-run — diff the `<script src>`/`<link href>` references in `mobile/index.html` against the script's copy list.
- **Commit source changes before running `node scripts/deploy-mobile.js --push`.** The script copies runtime files straight from the working tree (not from HEAD) but labels the deploy-repo commit `Deploy from <git rev-parse --short HEAD>` of the *source* repo — running it dirty ships the changes but stamps the commit with the pre-change sha (the first v2.0.1 deploy landed as "Deploy from 2710623", the old HEAD). After pushing, verify the live site with a short polling loop, not one immediate curl — the GitHub Pages CDN takes ~40s to rebuild and propagate.
- **Real play counts are tracked via GoatCounter (added v1.4.1, 2026-08-27), not GitHub repo Insights.** GitHub's "views"/"clones" numbers on the `mobile` repo only measure git/repo-page activity (cloning the source, browsing github.com) — they never see traffic to the actual deployed game, so they undercount (often to zero) even when people are actively playing. The real signal is the `<script data-goatcounter="...">` snippet in `mobile/index.html` (before `</body>`), reporting to the dashboard at `https://pradhanmantrielections.goatcounter.com` — check that URL for actual visit/unique-visitor counts. It's cookie-less/no-consent-banner-needed. Own testing visits count too; append `?goatcounter-ignore` to the live URL once from a device to stop it from counting that browser.

## 2026-09-03 — Live site is down: the domain is on registry serverHold, not a deploy or DNS-config fault
**Finding:** `kaunbanegapradhanmantri.in` returns **NXDOMAIN from `ns1.registry.in` itself** — the authoritative nameserver for `.in`. The NIXI registry record carries the status **`server hold`**, a registry-applied status whose entire effect is "do not publish this domain in DNS", which matches the NXDOMAIN exactly. Full status list: `server hold`, `server update prohibited`, `server renew prohibited`, `server delete prohibited`, `server transfer prohibited`, `client transfer prohibited`, `client delete prohibited`, `add period`. Applied **2026-09-03** (the record's `last changed`), with the domain only 4 days old and still in `add period`. **Ruled out:** not expired (registered 2026-08-30, expires 2027-08-30); nameservers correctly delegated to all four Porkbun NS (`curitiba`/`fortaleza`/`maceio`/`salvador.ns.porkbun.com`); DNSSEC not involved (`delegationSigned: false`); the `CNAME` file and the GitHub Pages deploy are both intact — v2.7.0 pushed to `mobile/main` cleanly and `pradhanmantrielectionsgame.github.io/mobile/` still 301s to the domain. A control query for `nic.in` through the same resolver path succeeds, so it is not a local resolver, network, or propagation issue.
**Context:** Found while polling the live site to confirm the v2.7.0 deploy had propagated — `curl` returned HTTP 000 while the deploy itself had succeeded. The `server*` status prefix means the registry set these, not the registrar and not anything the user clicked.
**Implication:** **No DNS record change and no redeploy can lift a `serverHold`** — only Porkbun support can, and `serverUpdateProhibited` means the domain probably cannot be edited in the dashboard until it is. Most likely cause is registrant contact verification not completed (the post-registration verification email), but that is **unconfirmed** from the public record — treat it as the first thing to check, not as fact. Next action: check the registrant inbox for a verification link and the Porkbun dashboard for a banner, then open a ticket quoting the domain and the `serverHold` status (`tech@porkbun.com`, abuse `abuse+registries@porkbun.com`, +1.503.850.8351). Re-check without a whois client (there is none on this machine, and `rdap.registry.in` is unreachable): `curl -s -m 25 -L -H "Accept: application/rdap+json" https://rdap.org/domain/kaunbanegapradhanmantri.in` and read the `status` array; DNS side, `nslookup -type=NS kaunbanegapradhanmantri.in ns1.registry.in` — resolving again means the hold is lifted. Meanwhile the build is testable over a `cloudflared` tunnel against `npm run serve`.

## 2026-09-03 — Map colour readability is set by hue separation, not RGB distance
**Finding:** `leaderColor()` paints each state by mixing the neutral grey toward the leader by `intensity = |p1-p2| / 10000`. That spends the light/dark axis on lead *size*, so hue is the only axis left to encode *whose* lead it is — and the on-screen gap between the two sides is `intensity × dist(p1, p2)`, which at a typical measured intensity of **0.26** shrinks a 130-unit RGB gap to **34**. A first implementation used RGB distance and offered a darker/lighter shade of the opponent's own hue; it cleared a 130 bar at full strength and produced an unreadable single-green wash in real play (Nitish `#5CB85C` vs Indira redrawn as `#156E0C` — **6° of hue apart**). Measuring hue separation instead: a matchup that reads fine untouched (Modi vs Rahul) is **85°** apart, and 12 of the 38 colliding pairings sat under 35°.
**Context:** The user asked for same-hue-different-shade specifically; it was built and measured, and they then named the two matchups that expose it (Nitish vs Indira, Ambedkar vs Tendulkar). Screenshots confirmed the wash.
**Implication:** `engine.js`'s `distinctPlayerColor()` now bars on **60° hue separation** (RGB distance kept only as a secondary check) and moves the opponent to the *nearest* hue clearing it, drawn from a curated 12-colour ring — so party fidelity is preserved where the hue wheel has room (12° for Sivaji's teal against Congress green) and sacrificed only where it does not (83° when both sides are green). The engine self-check asserts all 11 hard pairs. **Generally: when a visual encoding already spends lightness on one variable, a lightness-based distinction for a second variable is not available — and any full-strength colour metric overstates on-screen separation by the encoding's own scaling factor.**

## 2026-09-03 — Query strings never reach this user's phone, in any URL format
**Finding:** Three separate forced-matchup links (`?p1=&p2=`, then `?vs=a,b` with no `&` to truncate at) all arrived with the **entire query string missing** — the version badge, which renders from the raw tokens before any roster lookup, read a bare `v2.7.0` every time. The same links worked correctly through the same cloudflared tunnel from a fresh headless browser, so the links and the code were both fine.
**Context:** Reproduced across three attempts while trying to hand over a specific matchup for playtesting. `mobile/manifest.json`'s `start_url` is a bare `./index.html`, so a home-screen launch carries no query at all; links also get mangled in transit. The `?ai=` hook already carries a note about the same class of loss (2026-08-31).
**Implication:** **Do not build a phone-facing playtest affordance on a query parameter.** The working answer is the one `?ai=` already reached: an in-app control plus `localStorage`, which survives reloads, home-screen launches and shared links alike — hence the long-press playtest toggle. Keep the badge tag as the diagnostic: a bare version string means the query never arrived; a tag naming an unresolvable token means it arrived but did not resolve.

## 2026-09-03 — A guard built from new RegExp with escaped strings failed open and would have waved every deploy through
**Finding:** The first version of `scripts/deploy-mobile.js`'s `assertPlaytestFlagsOff()` built its patterns dynamically with `new RegExp` from an escaped string. The escaping collapsed on the way into the file, leaving a pattern equivalent to `vars+UNLOCK_ALLs*=s*true` — which matches nothing. The guard reported "flags off" for a build with `PLAYTEST_BUILD = true` and would have shipped it. Caught only by deliberately flipping the flag on and running the real script.
**Context:** Found immediately after writing the guard, while testing that it actually fires.
**Implication:** Use **regex literals** for anything security- or safety-shaped; the string form needs double-escaping and a slip there fails *open*, which is worse than having no guard at all. **Never accept a guard as working because it reported success on a clean input — always verify it fails on a dirty one.** The guard now uses literals and is verified in both directions (exit 1 with a flag on, passes with both off).

## 2026-09-03 — Version-label AI-profile tap stored a value that is not a profile key
**Finding:** The undiscoverable playtest tap on the welcome screen's version label cycled through `['', 'max'].concat(others)`, but the real profile keys are `level-1`…`level-8` — `PMEAI.profileByKey('max')` is `false`. So `forcedAIKey()` rejected the stored value: the badge never tagged, and because the cycle index derives from `forcedAIKey() || ''`, every subsequent tap re-selected the same dead value instead of advancing. The build-identity safeguard that the code's own comment says "cost two inconclusive playtests to learn" was silently doing nothing.
**Context:** Found while testing that a new long-press gesture on the same element did not collide with the existing tap.
**Implication:** Fixed by deriving the hardest key from `PMEAI.MAX_LEVEL` rather than hardcoding a name, so it cannot drift out of sync with the profile list again; a stale `'max'` in `localStorage` self-heals. **A hardcoded string that must match a value in another module is a drift point — derive it.** Also: a playtest safeguard that fails silently is indistinguishable from one that works, so it needs its own check.

## 2026-09-03 — Carousel cards bake their locked state in at build time
**Finding:** `buildPolCard(p, locked)` captures `locked` in the play button's click closure and its label. The grid is built once at boot, so toggling playtest mode afterwards left every card still reading "🔒 Defeat to unlock" and still refusing the pick, even though the mode was active — 20 of 21 cards in the first end-to-end test.
**Context:** Found in the first end-to-end test of playtest mode.
**Implication:** Any state that changes which cards are playable must call `renderPolGrid()` before the select screen is shown, not merely set a flag. Fixed by rebuilding on entry to the select screen.

## 2026-09-03 — The longest state name overflows the info panel (pre-existing)
**Finding:** `.info-row h3` has `flex-shrink:0` with nothing capping its width, so "Dadra And Nagar Haveli And Daman And Diu" laid out at its 437px max-content width inside a 390px panel and ran off the screen edge. Reachable in normal play by tapping its LED chip in the Small UTs cluster card.
**Context:** Found while checking that the new state-card invest button had clearance from the longest name — the name was already overflowing before the button existed.
**Implication:** Fixed with `max-width:100%`, which lets the text wrap to a second line without reopening the squeeze `flex-shrink:0` exists to prevent. **`flex-shrink:0` needs a `max-width` companion whenever the content can exceed the container.**

## 2026-09-02 — investCash charges full price for zero gain once a state is at 100%
**Finding:** `investCash()` (`mobile/game.js:416`) deducts the cost and increments the per-state tap counter *before* it knows whether anything was actually gained. `gainAt()` caps the gain at remaining headroom (`min(boost, 10000 - self)`), so at 100% ownership the gain is 0 — but the call still returns `{ok: true}`, so `main.js` plays the success sound and spawns the money FX for a purchase of nothing. Measured on Uttar Pradesh: reached 100% at tap 23; the next tap cost ₹800Cr, `gained=0bps`, funds actually deducted 800, counter incremented 23→24. The wasted tap also permanently weakens every future tap on that state, since the counter never resets. **Still open as of 2026-09-02 (v2.6.2)** — diagnosed but deliberately not fixed, as the user's reported symptom was the decay curve (a separate stall) rather than this.
**Context:** Found while investigating a user report that repeated taps on one state "just stall". There are two distinct stalls: the geometric decay curve pinning at its 1-bps floor, and this one. Only the first was addressed in v2.6.2.
**Implication:** Two candidate fixes, both small: refuse the tap at zero gain (refund and return `{ok:false, reason:'maxed'}`, so it shakes/toasts like any other invalid action), or prorate cost by `gained / boost`. The first is one line and changes no economy; the second shifts the economy and would want a `balance-sim.js` pass. More generally: **any action function that charges before computing its effect can silently sell nothing** — check the charge/effect ordering whenever a cap or clamp sits between them.

## 2026-09-02 — An `ok: true` that means "the action was spent", not "the effect landed"
**Finding:** `activatePower()` (`mobile/game.js`) returns `{ok: true, nullified: true}` when the activator's power has been secretly nullified by Nehru's Non-Alignment — `ok` means the activation was consumed, not that anything happened. The human path (`finishActivatePower` in `main.js`) read the secondary `nullified` flag correctly and showed a fizzle toast; `mobile/ai.js` checked only `.ok`, so a nullified AI power still played the full power sound and the "⚡ BREAKING: X invoked Y" burst. The nullification itself was always mechanically correct (effects are skipped at the `pl.powerNullified` early return); only the presentation lied, which is indistinguishable from "the power didn't work" to a player.
**Context:** Reported as "even after using Nehru's special power the opponent's special power went through." Verified by direct simulation: with Nehru firing first, the AI's power action comes back `nullified: true` and no effects run; the control (Nehru never fires) comes back `nullified: false`.
**Implication:** When a return value carries both a success flag and a secondary outcome flag, **grep every caller of that function, not just the path the bug report names** — the reported path is as likely to be the correct one as the broken one. This codebase has now hit the sibling "two places must agree" failure four times (agenda effect text vs. real effects, `[hidden]` CSS, `finalizeGame` vs. `showEndOverlay`, and now `startGame` vs. `startReplay`).

## 2026-09-02 — The wiki's investment-curve numbers were never derived from the real function
**Finding:** `docs/wiki.html`'s direct-cash-investment section carried two figures that were false under *any* version of the curve: it listed tap 10 as +3.42% (the real `investmentBoostBps` returns +3.58%), and described taps 50+ as "→0%, asymptotically worthless — never a hard floor" while simultaneously claiming a 1-seat state's cost per +1% "crosses a full phase's income (2,500 Cr) around tap 43." Both are wrong: `max(1, …)` **is** a hard floor (1 bps, permanently), and it caps a 1-seat state's cost per +1% at exactly 1,000 Cr — so 2,500 Cr is unreachable at any tap count, before or after the v2.6.2 rate change.
**Context:** Found while updating that same table for the `boostDecayRate` 0.85→0.933 change. The numbers looked plausible and had survived at least one prior edit of the surrounding prose.
**Implication:** **Derive documented numbers by running the real function, not by hand or by extrapolation** — `node -e` against `global.PMEEngine` takes seconds and this section had two independent errors that no amount of re-reading would have caught. Applies to every worked example in `docs/wiki.html`, which is this project's single authoritative design reference and is explicitly held to a no-drift rule.

## 2026-09-02 — A Python `open(path,'w')` that throws mid-write leaves the file at zero bytes
**Finding:** `io.open(p,'w',encoding='utf-8').write(s)` truncates the target the moment it opens it, *before* encoding the content. A `UnicodeEncodeError` raised during that write therefore leaves a 0-byte file, not the original. Worse, the obvious retry — re-run the same script — then reads the now-empty file, finds no anchor to replace, and writes the empty string straight back, destroying the evidence that anything went wrong. This wiped `CHANGELOG.md` (1,816 lines); recovered intact with `git checkout HEAD -- CHANGELOG.md`.
**Context:** A lone surrogate half from an emoji literal in a bash heredoc raised `UnicodeEncodeError: surrogates not allowed` at position 1585 while writing a new changelog entry. The first symptom was `head -9 CHANGELOG.md` printing nothing.
**Implication:** For any edit to a file that matters: **encode to bytes first, write to a temp path, then `os.replace()`** — the encode failure then happens before anything is truncated, and the replace is atomic. Also treat "a file-reading command suddenly returns nothing" as evidence of truncation, not of a display glitch; check `ls -la` for a 0-byte size immediately. Committing before a bulk doc edit is what made this a non-event.

## 2026-09-02 - Neither a URL flag nor a code change reliably reaches the phone: two independent mechanisms, both silent
**Finding:** Two separate problems compounded into "your changes don't work on my device," and neither produces an error. (1) **URL query params are unreachable on a phone.** `mobile/main.js`'s install gate hides the game in any phone browser tab (coarse pointer + max-width 700px + not standalone), so the only playable path is the home-screen icon - and that launch uses `manifest.json`'s `start_url`, a bare `./index.html`. Everything after the `?` is gone before `main.js` runs. A `?unlockall` flag verified working in desktop Playwright therefore failed twice on a real phone. (2) **The service worker serves stale code indefinitely on a slow connection.** `mobile/sw.js` fetches the app shell/code network-first with `fetch(e.request, {cache:'reload', signal: AbortSignal.timeout(3500)})` and falls back to `caches.match(e.request)` on rejection. Over a cloudflared tunnel from a phone, 3.5s is easy to exceed, so the device can sit on an old `main.js` for an entire session while fix after fix is "shipped" to it.
**Context:** Found while adding a playtest unlock flag. The user reported `?unlockall` not working and added the general observation "in general I've noticed these url modifications don't work" - which was the clue that this was systemic, not a typo in the flag. `main.js`'s own comment above `forcedAIKey` records the same discovery being made for the `?ai=` flag on 2026-08-31, and the fix then was to persist to localStorage; that precedent was in the file and still got missed.
**Implication:** **Never ship a phone-facing behaviour behind a URL query param.** Persist it to localStorage (the `forcedAIKey` pattern) or hardcode a constant. `?p2=` (main.js ~line 1448) still reads `location.search` directly and is unreachable on a phone for the same reason. Separately: during a playtest session, **the version badge is the only reliable signal of which build the device is actually running** - bump `GAME_VERSION` whenever handing over a build, and have the tester read the badge back before trusting any "it didn't work" report. A `CACHE` bump in `sw.js` is the fix, but it only lands once `sw.js` itself is fetched, which is subject to the same timeout.

## 2026-09-02 - In a CSS `background` shorthand, a colour is only legal in the LAST layer
**Finding:** `background: var(--chip-fill) padding-box, conic-gradient(...) border-box` silently invalidates the entire declaration - `getComputedStyle().backgroundImage` returns `none`, with no console warning and no parse error visible anywhere. The rule appears intact in `document.styleSheets` (`r.style.background` reads back the full text) while computing to nothing, which makes it look like a specificity or override problem rather than a validity one. The fix is the standard gradient-border idiom: wrap the fill in a gradient, `linear-gradient(var(--chip-fill) 0 0) padding-box, conic-gradient(...) border-box`.
**Context:** Hit building the group-chip completion ring as a two-layer background over a transparent border. Diagnosed by setting each layer inline in the page and comparing: the single-layer conic worked, the two-layer version returned `none`.
**Implication:** Any two-layer `background` doing the padding-box/border-box gradient-border trick must express the fill layer as a gradient, never as a colour or a custom property holding one. When a background rule computes to `none` but reads back correctly from the stylesheet, suspect layer validity before specificity.

## 2026-09-02 - `Manufacturing` is the anti-`AgriculturalRegion` pole, so only 4 states can join it without creating same-state cancellations
**Finding:** Brute-forcing every state against the `Manufacturing` and `Education` groups showed the tag system has a strong built-in polarity: `Manufacturing` appears with opposite sign to `AgriculturalRegion` in **9** policies and `TribalLands` in **7**, at matched +/-4/8/12 magnitudes. Adding any agricultural state therefore zeroes those policies there outright. Punjab - thematically excellent (Ludhiana hosiery, bicycles, machine tools) - creates **8** new cancellations by itself; Andhra Pradesh creates 6. Only **Chandigarh, Jammu & Kashmir, Delhi and Rajasthan** can join `Manufacturing` cleanly, and two of those are already in `Education`. This polarity is also *why* the two groups overlapped in the first place: both sit on the same "modern urban" pole, making them near-synonyms by construction rather than merely by state list. The 14 collisions from the shipped regrouping all reduced to one pattern, fixed by stepping `Manufacturing` down one rung in the 4 policies where it opposes `AgriculturalRegion` at matched magnitude (net cancellation cost +9 -> +1). Stepping `AgriculturalRegion` *up* instead backfires - it creates 11 new collisions in Haryana, Maharashtra, Telangana and Madhya Pradesh.
**Context:** Found while reducing Education/Manufacturing overlap (Jaccard 0.50, sharing 5 of ~7 states). The user supplied MoSPI's top-10-by-registered-factories list as the thematic ground truth, which independently confirmed dropping West Bengal.
**Implication:** Before adding a state to any group, brute-force the resulting cancellations rather than reasoning from theme alone - a thematically perfect fit can be structurally impossible. When a group's membership must broaden across its own opposite pole, the fix is asymmetric magnitudes (the secondary identity pulls less than the primary), not membership gymnastics. Check the direction of the magnitude step: lowering the newly-added tag is safe, raising the opposing one propagates collisions elsewhere.

## 2026-09-02 - Politician win rate is driven by agenda-kit national pull, not home state; the home-popularity correlation is an artefact
**Finding:** A full 4,200-game all-vs-all tournament at max AI difficulty (`node mobile/balance-sim.js 10 level-8`, both seats forced to `level-8`) produced an **84-point win-rate spread**: Rahul Gandhi 14.3% to Rajinikanth 98.3%. The driver is each politician's **seat-weighted national agenda pull**, computable from `tagEffects` before a single game is played - Spearman **rho=0.73, p=0.0002** against win rate. Home-state net effect is noise (rho=0.22, p=0.35). An earlier claim that win rate tracked `avgHomePopPct` "almost perfectly" was wrong: that column is measured at end of game, so it is an *outcome* of winning, not a cause. The user disproved it in one line - the bottom five are all Uttar Pradesh politicians, but so are Amitabh Bachchan (75.3%) and Hema Malini (83.8%), with 8 of 21 UP in total. Rahul is last on national kit pull (3.39, next-worst 4.93) **and** the only politician whose kit is net negative in his own home state (-4). Vajpayee is the counter-example that confirms it: third-worst kit (5.30) but 52.5% win rate, carried by Pokhran. Also measured: hung-parliament rate collapses at max difficulty (0.5-19% vs the 48-98% recorded at the default rung), so win rate is real signal there rather than mostly draws.
**Context:** Run at the user's request. The correlation error was caught by the user immediately, from domain knowledge the numbers alone didn't surface.
**Implication:** Diagnose an anomalous win rate from `tagEffects` first - it is cheap, causal, and available without simulating. **Never read an end-of-game measurement as an explanatory variable**; `avgHomePopPct` in balance-sim output is downstream of winning. Balance numbers measured at the default rung do not transfer to `level-8`, where the draw rate is an order of magnitude lower.

## 2026-09-02 - Rahul Gandhi's deferred-growth power is deployed near-optimally; the power was never the problem
**Finding:** Bharat Jodo Yatra arms the Nationwide Rally with +50bps per elapsed phase. Instrumenting 200 games at `level-8`: armed at phase **2.98** on average, Nationwide Rally deployed at phase **7.76** (histogram 7x69, 8x110, 9x13, 10x4), giving **4.75 phases of accrual** - +237bps on a 500bps base, so the rally lands ~1.5x normal strength. The AI cannot deploy much earlier even in principle: `nationwideRallyMinPhase` is 6. So the hypothesis that the AI was wasting the power by deploying it early is wrong, and the redesign works as intended.
**Context:** The user proposed early deployment as the explanation for Rahul's dead-last 14.3% win rate, since the power had specifically been redesigned to grow over time.
**Implication:** Rahul's problem is his agenda kit (see the natAvg finding above), consistent with the 2026-08-28 note that Secularism's -12 HindiHeartland/-12 Pilgrimage both land in UP while its +12 MinorityAreas never does. A one-shot +237bps nationwide cannot offset four agendas pulling nationwide every phase for ten phases - when a power and a kit are both suspects, price the kit first.

## 2026-09-02 - Adding states to a regional-dominance group makes it HARDER to capture, not easier
**Finding:** Measured over 1,260 games per configuration (level-8 both seats, counting how often each group is ever held): expanding `Manufacturing` from 7 to 9 states dropped its capture rate from **78.7% to 69.4%**; `Education` gaining Assam went 80.5% -> 76.7%. Every added state is another veto point under the per-state AND-gate, and cost scales with seats (210 -> 248). This also showed up in the politician balance run as P.V. Narasimha Rao (Telangana, in both groups) losing 12.8 points while Sardar Patel and Narendra Modi (Gujarat, Manufacturing-only) gained 11.7 and 6.0 - the two significant movers out of 21, everything else inside noise.
**Context:** The user's intuition was the opposite - that adding states made the group easier to acquire - so it was measured rather than argued.
**Implication:** Group difficulty is dominated by member *count* under the AND-gate, not by total seats. When resizing a group, measure capture rate directly; and expect the effect to land asymmetrically on politicians whose home state sits inside vs. outside the changed group.

## 2026-09-02 - The group-capture fanfare fired at the next phase boundary, found by string-matching the log
**Finding:** `doEndPhase` contained `if (game.log.slice(0, 10).some(e => e.msg.indexOf('You hold') === 0)) playSound('fanfare')`. So capturing a group mid-phase was silent, and a fanfare arrived later at the phase boundary, detached from the action that caused it - and colliding with `fanfare`'s other meaning (rally held). Replaced with edge detection in `renderGroupCaptureBadges` against a module-level `prevCapturedP1` map, seeded in `buildGroupsBox` from the starting position so a new game doesn't fire for groups already held at kickoff. Measured SFX loudness for reference via browser `decodeAudioData`, since no local mp3 decoder exists: `fanfare` -23.7 dBFS RMS, `money_spent` -17.5, `phase_reset` -19.1, `rally_sound` -21.3, `game_over` -13.7, `bg_music` -13.8.
**Context:** The user asked for a distinct capture sound, noting fanfare already means "rally held."
**Implication:** Detecting a game event by matching prose in the log is fragile and fires at the wrong moment - edge-detect the state the renderer already computes instead. Keep the loudness table above when adding any new clip; target ~-13 dBFS RMS to sit with `game_over`/`bg_music` and clearly above `fanfare`.

## 2026-09-02 - RESOLVED: the new group-capture sound is silent on a real phone while verifying correctly in Chromium
**Finding:** `sounds/group_won.wav` (synthesized locally - no ffmpeg available; 32kHz mono 16-bit PCM, 0.80s, -13.0 dBFS RMS) plays correctly in headless Chromium: forcing a capture yields `sounds played: ['group_won']`, exactly once per false->true transition and silent while merely still held, and the clip loads (`oncanplaythrough`). It is registered in `sounds` before `unlockSounds()` runs, so iOS's per-element gesture unlock covers it. The user captured 7-8 groups on a real phone and heard nothing, across two builds. Ruled out: no group captured (the user confirmed 8), non-finite `seats` breaking the seat-weighted ratio (0 states affected), `buildGroupsBox` re-seeding on every render (it is called only at game start, 2 sites), volume (the first version was already 8 dB louder than `fanfare`).
**Context:** Three rounds of fixes - louder clip, cache bump v12->v14, version badge - none resolved it. Deliberately left open rather than guessed at again, at the point the user asked to checkpoint and keep playtesting.
**Resolution (2026-09-02, v2.6.1):** Guess (3) below was the right one. The `.wav` was replaced wholesale by a user-supplied mp3 (`sounds/bell_chime.mp3`) and the sound was immediately audible on the same real phone, first try. The synthesized 32kHz mono PCM WAV was the problem; every other clip in `sounds/` is an mp3, and that is now true of this one too. **Prefer mp3 for any new clip — do not ship a WAV to iOS Safari on the strength of a Chromium check.** A second, separate defect surfaced once it was audible: the supplied clip carried 0.70s of *leading* silence, so the cue fired 0.7s after the capture. Measured per-frame RMS by decoding through headless Chromium's `OfflineAudioContext` (no ffmpeg on this machine) and trimmed both ends at MP3 frame boundaries, 4.20s → 2.35s. **When a new clip feels mistimed rather than missing, measure its leading silence before touching any code.**

**Implication:** Next things to try, in order: (1) confirm the device is actually on v2.6.0 via the version badge - the stale-service-worker finding above makes an old `main.js` the leading suspect; (2) `playSound` does `a.currentTime = 0` before `a.play()`, which throws `InvalidStateError` in Safari when `readyState` is 0, aborting playback silently - wrap it in try/catch; (3) re-encode as mp3 to match every other clip, in case iOS Safari is rejecting the WAV. Do not add more loudness - that has been measured and is not the cause.

## 2026-09-01 — An AI targeting function returning argmax over a *shared* resource is both predictable and quietly unfair to the human
**Finding:** `pickDisciplinedRallyTarget` (`mobile/ai.js`) sorted open states by seats and returned `pool[0]`, the single largest. Since every ladder rung from level 2 up carries `tokenDiscipline`, **every AI above the easiest one rallied Uttar Pradesh in every game**, then Maharashtra once UP hit the play cap — fully deterministic, never varying by politician, seed or phase. Two distinct problems, and the second is the one that isn't obvious from reading the function: `game.cfg.rally.maxPlaysPerStateShared` (2) is a **shared** cap across both players, so a bot that always takes the biggest state doesn't merely play predictably, it *consumes the human's access* to the best rally targets before the human can reach them. Replaced with a seat-weighted random draw over the states that clear the same economic bar (`weightedRallyPick`): a rally's value scales linearly with seats, so weighting by seats tracks the real payoff instead of discarding it. Measured over 40 games at level 8: **16 distinct states rallied, top two down to 13.8% each** from effectively 100%.
**Context:** Reported by the user from live play — "the stronger AIs place rally tokens predictably in the largest states UP, Maharashtra etc… it's also boring to play against them… It also robs you from holding rallies there." Both halves of that report were confirmed exactly in the code on first read.
**Implication:** Whenever an AI selection function picks a target from a pool the human also draws from under a shared cap, argmax is the wrong shape regardless of how correct its scoring is — prefer a value-weighted random draw, which preserves most of the expected value while leaving the contested resource genuinely contested. Audit the other pickers on this basis: `pickAIPowerTarget` still returns a strict argmax, though it competes for no shared cap. This is a *fairness/fun* axis that no strength metric will ever surface, since a stronger-but-boring bot scores better on every seat-margin measurement.

## 2026-09-01 — Randomising the AI's rally target cost nothing measurable and made the ladder strictly monotone
**Finding:** Re-running the full round-robin (`node mobile/ladder-sim.js 20`, 2,240 games) after replacing the rally argmax with a seat-weighted draw shifted every rung's margin down slightly — level 1 −101→−115, level 8 +180→+175 — while leaving the ordering completely intact. The monotonicity check went from two violations in the earlier 10-profile field to **passing outright: every rung beats the rung below it.** Current ladder: −115, −78, −60, −40, −10, +43, +86, +175 (± 6–11). Level 7→8 remains the one step inside noise (67 ± 38).
**Context:** Run in the background immediately after the rally change specifically to price it, on the assumption that deliberately weakening the bot's target selection would cost real strength.
**Implication:** A seat-weighted draw over an economically-screened pool is close to free relative to argmax, because expected value under linear-in-seats scoring is preserved by the weighting itself — so variety and unpredictability can be bought cheaply in this engine. More generally: price a "make it less optimal" change with the harness before assuming it costs strength; the intuition that any deviation from argmax must weaken the bot was wrong here.

## 2026-08-31 — `scripts/deploy-mobile.js` silently omitted `ai.js`, so the shipped game would have had a dead AI
**Finding:** The deploy script copies an explicit, hand-maintained file list (`copyTransformed`/`copyVerbatim` calls in `main()`), and `mobile/ai.js` was never added to it after the v2.4.0 AI-module extraction. A deploy would have shipped an `index.html` whose `<script src="./ai.js">` 404s, leaving `window.PMEAI` undefined — the game boots and the map renders, but `game.js`'s `ai()` lookup fails and the opponent never takes a single action. Caught only by checking the script's file list before pushing; nothing in `npm test` or the local dev server can see it, because both read from `mobile/` where the file exists. Also confirmed in passing that the live site was still on **v2.3.4** — v2.4.0 was committed but never deployed, which is the only reason this never shipped broken.
**Context:** Found while preparing the v2.5.0 deploy, following CLAUDE.md's existing standing instruction to dry-run `stripRelativePaths()` before pushing. That check passed; the missing-file problem was a separate one noticed while reading the same function.
**Implication:** The deploy file list is a manual allowlist that no test covers — **any new file added under `mobile/` must be added to `scripts/deploy-mobile.js`'s `main()` in the same commit**, or it silently vanishes from the live build. Add this to the pre-deploy check alongside the `../` dry-run: diff the set of `<script src>`/`<link href>` references in `mobile/index.html` against the script's copy list.

## 2026-08-31 — The app's global `touch-action:pan-x pan-y` silently breaks any `<input type="range">` on touch
**Finding:** `mobile/index.html`'s global rule `*{... touch-action:pan-x pan-y}` — added 2026-08-24 to block pinch-zoom during fast play — also makes the browser claim a horizontal drag on a range input as a pan/scroll gesture, so the slider thumb never moves on a touchscreen. The fix is `touch-action:none` on the range input specifically, which is scoped and does not reintroduce pinch-zoom anywhere. Note this class of bug is invisible to a Playwright check that sets `.value` and dispatches an `input` event programmatically (as this session's verification initially did) — that path never exercises the touch gesture at all.
**Context:** Hit while replacing the settings difficulty picker with a slider. Anticipated from the existing CLAUDE.md note about the global rule rather than discovered from a failure report, and applied pre-emptively.
**Implication:** Any future draggable control (range input, custom swipe handle, carousel built on pointer events) needs its own `touch-action` override against that global rule — and verifying it needs a real touch drag (`page.touchscreen`/real device), not a synthetic `.value` assignment.

## 2026-08-31 — Full AI round-robin tournament: 2,700 games, eight cleanly separated difficulty rungs
**Finding:** `node mobile/ladder-sim.js 30` ran every AI profile against every other, 60 games per pairing (45 pairings), mirrored so each matchup is played twice with the same two politicians in the same two seats and the bots swapped. Mean seat margin vs the whole field, weakest first: **easy −100.8 ± 4.4, medium −95.9 ± 4.1, hard −61.2 ± 4.8, expert −42.6 ± 4.4, cap0 −31.4 ± 5.2, regional-2 −22.3 ± 5.9, cap1 +10.2 ± 5.7, cap2 +55.1 ± 5.7, cap4 +108.7 ± 6.4, max +180.2 ± 7.8.** Every step exceeds its error bar except two: `medium` is genuinely *weaker* than `easy` (−14.4 ± 7.2, the unfixed agenda-amortisation bug), and `cap4` vs `max` is unresolved (49.3 ± 34.0 — they may be the same strength). `cap0` also loses to `regional-2` (−20.0 ± 7.7), so those two are out of order.
**Context:** Measured after `profile.groupCap` was added as the dial across the old 147-seat cliff between the weak rungs and `max`. Runtime ~4m15s for the full 10-profile matrix. A 12-seed run (1,080 games) 20 minutes earlier produced the same ordering and margins within noise, so 30 seeds is comfortably past the point where more games change the answer.
**Implication:** The ladder is real and usable: dropping `medium` and `cap0` (the two that break monotonicity) leaves eight strictly-ordered rungs spanning ~281 seats — easy, hard, expert, regional-2, cap1, cap2, cap4, max. Re-run this after ANY change to shared `aiStep` logic, since ADR-0016 notes a shared-code change shifts every rung's margin at once. Use seed counts of 30+; 12 is enough for ordering but leaves the top two rungs indistinguishable.

## 2026-08-31 (later still) — Chain ablation credited a two-flag partnership to one flag, and group capture turned out to be an all-or-nothing snowball rather than a skill
**Finding:** Measuring the AI ladder by adding one capability flag at a time (ADR-0016's design) attributed a 147-seat jump entirely to `spreadInvest`, the last flag added, and made `smartGroupTarget` look worthless (+15 ± 10, inside noise). Both readings were wrong: the two flags do nothing apart and everything together. Mirrored 28-game measurements of groups captured — `expert` (group targeting, no fixed scorer) **2.4**, `regional-2` (fixed scorer, no live group targeting) **0.2**, `max` (both) **7.3** — show the interaction directly. The mechanism is an economy, not a skill: a captured group pays cash, which buys investment taps, which capture more groups. `max` makes **429** investment taps a game against `regional-2`'s **170**, on near-identical unspent cash (~5 Cr), purely because seven captured groups fund it.
**Context:** Built `mobile/ladder-sim.js` (mirrored-pair, seat-margin, full round-robin over AI profiles — `balance-sim.js` cannot measure profiles at all, since it never passes a profile to `setupAI` and aggregates on politician id). A user proposal for an intermediate bot that obsesses over 1–2 random groups was built and measured; it lands mid-field (beats `expert` by 36) but *widens* the gap to `max` to 225, because confining a bot to two groups is exactly what stops it capturing the seven that fund the snowball.
**Implication:** Never read a single-flag delta from a chain ablation as that flag's value — it is the value of that flag *given every flag below it*, and a partnership shows up entirely on whichever half was added last. Any ADR-0016 rung measurement needs the full round-robin matrix, and interacting flags need a solo-flag row to be attributable at all. Practically: the gap between the weak rungs and `max` is not a dial-able competence, because regional dominance requires *every* state in a group over 50% and so pays nothing until it pays everything. The working dial is `profile.groupCap` — capping how many groups a bot may hold throttles the compounding directly and produces evenly spaced rungs (measured overall margins: cap0 −31, cap1 +14, cap2 +55, cap4 +98, uncapped +178).

## 2026-08-31 (later) — `max` beat a human 374–142 on the first confirmed-build playtest, overturning the same day's "a human vetoes the group engine" finding
**Finding:** With the four shipped profiles commented out of `AI_PROFILES` so `max` was the only drawable profile — and the version label reading `v2.4.0+max` on the device that played it — `max` (as Rajinikanth) beat the user 374–142, taking **3 regional groups to the human's 0** and using its Nationwide Rally. The earlier same-day entry below, which recorded `max` taking **0 groups** and losing 196–288 against a human, was almost certainly measured against a shipped profile, not `max`: it predates the single-profile pool and relied on the `?ai=`/localStorage override plumbing that the first finding in this file documents as silently failing on the phone. Treat that entry's human-vs-`max` numbers as void; its bot-vs-bot measurements (7–9 groups, ~400 seats) and its mechanical account of *why* group compounding is vetoable both stand. **Superseded in part, same day — see the entry above this one: the group-compounding claim it made was right, and a later chain-ablation reading that appeared to refute it was itself the error.**
**Context:** Three of the four AI-ladder playtests on 2026-08-31 could not confirm which bot they played. The fix that finally worked was not better override plumbing but deleting the need for it — commenting out the other profiles so `pickAIProfile` has one possible return value, which no query string, service worker or storage quirk can defeat. The opponent drew Rajinikanth, the roster's acknowledged strongest kit, so the margin overstates the typical case; the group count does not, and that is the number the earlier entry got wrong.
**Implication:** A capability that can only be observed by playing a match should be isolated by *removing the alternatives*, not by adding a selector — a one-line comment-out is more trustworthy than any amount of override plumbing, and it fails loudly (wrong bot is impossible) rather than silently (wrong bot looks like a weak bot). Re-measure any human-vs-bot claim made before the single-profile pool existed. The ladder's group-veto concern is still open but is no longer supported by evidence.

## 2026-08-31 — Every URL- and localStorage-based playtest override failed on the real device; only a build marker rendered in the UI proved anything
**Finding:** Three successive mechanisms for forcing a specific AI profile during a playtest all failed on the user's phone while working end-to-end in desktop Chrome: `?ai=<key>`, `?p2=<politician id>`, and finally a tap-the-version-label toggle persisted in `localStorage`. Root cause never found. What *did* work was rendering the state into the UI: the version label now shows `v2.3.4+max` when an override is live, `+ai` when `mobile/ai.js` loaded but no override took, and plain `v2.3.4` on an old build — the user reported `+ai`, which localised the failure to the override plumbing in one message instead of another full match. Two playtests were spent before this existed, both misread as "the new bot is weak" when the new bot may not have been running at all. Also disproved along the way: the service worker is **network-first for JS** (`fetch(..., {cache:'reload'})` with a 3.5s timeout, `skipWaiting` + `clients.claim`), so a stale precache was never the explanation, contrary to an initial diagnosis.
**Context:** Two live playtests of the new `max` profile came back looking like ordinary shipped-AI games (0 regions dominated, no Nationwide Rally), with no way to tell whether the profile was actually in play. Resolved by driving the tunnel URL in desktop Chrome and reading `window.__game.players.p2.aiProfile.key` directly, which confirmed the override worked there.
**Implication:** Any behavioural change that can only be observed by playing a match needs a visible build/config marker in the UI *before* the first playtest, not after two inconclusive ones. Never diagnose "the AI played badly" from match stats alone until the build identity is confirmed on the device that played it. The eventual fix was to stop overriding at all and put `max` in the normal random pool, so no plumbing stands between the code and the playtest.

## 2026-08-31 — mobile/main.js has mixed CRLF and LF line endings
**Finding:** `mobile/main.js` contains both `

` and bare `
` line terminators in different regions of the same file (`mobile/game.js`, by contrast, is uniformly CRLF — 983/983 lines). A scripted exact-string patch that normalises its search text to CRLF matches in some parts of main.js and silently fails to match in others; the version-label block near line 12 is LF while the game-creation block near line 1339 is CRLF.
**Context:** Hit twice while applying scripted edits to main.js — an anchor verified by eye against `sed` output still reported zero matches, until the raw bytes were dumped with `JSON.stringify`.
**Implication:** Any scripted edit to `mobile/main.js` must try both the literal and CRLF-normalised form of its anchor (and assert exactly one match) rather than assuming the file's dominant ending. Dumping the raw bytes around the anchor is the fastest way to diagnose a "this anchor is obviously there" failure.

## 2026-08-31 — Fire-on-craft power timing lets a politician's own special power permanently disable its Nationwide Rally
**Finding:** `aiStep` activates a crafted special power on the very next tick, unconditionally — a rule inherited by the new `max` ladder profile. As **Sivaji Rao (Nayak)**, whose *One-Day Ordinance* costs "every future rally token you'd otherwise earn" (`tokenIncomeStopped`), the AI crafts the special around phase 2–3, fires it immediately, and thereby cuts off the token income it needs to ever reach the Nationwide Rally's 12-token craft cost. It trades ~16 future tokens — the whole Nationwide Rally plus change — for 100% popularity in one 48-seat state. Measured across the roster (8 seeds per politician, `max` in the p2 seat): Nayak `usedNationwide` **0%** / `tokenIncomeStopped` **100%**; Mamata Banerjee also **0%** but *not* token-stopped (a second, still-undiagnosed cause); every other politician **100%**.
**Context:** User playtested `max` in the browser via `?ai=max`, drew Nayak as the opponent, and reported the AI "hoarded tokens but didn't use nationwide at all" while feeling no harder than the shipped AI. Also confirmed in the same investigation that the browser's AI tick pacing is *not* a factor: replicating `planAITickPacing`'s exact budget (dry-run count, `phaseMs/count` clamped to 300–4000 ms) in a headless run produced results identical to unpaced play, 30 seeds, same seats/groups/nationwide rate.
**Implication:** Power timing is load-bearing for any bot that also wants the Nationwide Rally, not the optional polish item it was first filed as — a bot must not fire a power whose cost is *future* tokens until the nationwide rally is crafted or deliberately abandoned. Separately, a methodology note: one live playtest draws one random opponent, and this one happened to draw the single politician whose power self-sabotages. A single match can't distinguish "the AI is weak" from "this politician's kit self-destructs" — check the per-politician sim table before drawing either conclusion.

## 2026-08-31 — Regional-dominance compounding is the strong bot's whole edge, and a human vetoes it by holding one state per group
**Finding:** The `max` profile's strength comes overwhelmingly from group-payout compounding: in a traced game it spent **45,540 Cr against a 27,500 Cr baseline budget** (starting funds plus refreshes), the surplus being regional-dominance payouts at 5 Cr/seat instant plus 0.5 Cr/seat/phase held, recycled into more investment taps. Against the four shipped profiles it averages 7–9 groups and ~400 of 543 seats. Against a human playing an ordinary game it took **0 groups** and lost 196–288. Regional dominance requires *every* member state above the 50% threshold, so an opponent who holds a single state in each group switches the entire compounding engine off; the bot then runs on its investment lever alone, which converts at a flat ~200 Cr/seat.
**Context:** Built `max` as the ladder's top rung and measured it bot-vs-bot, then the user played it live and won comfortably while "playing pretty average" and forgetting their own Nationwide Rally.
**Implication:** Bot-vs-bot margins systematically overstate any group-dependent profile, because the thing that separates the rungs is exactly the thing a competent human neutralizes — the same trap already recorded on 2026-08-21 for conditional special powers, now with a second instance. The difficulty ladder cannot be calibrated from AI-vs-AI alone; a rung's rating needs at least a sanity check against real play. A group chase should also be robust to contest — sinking cash into a group an opponent can veto with one state pays nothing at all for the effort.

## 2026-08-30 — Custom domain on the `mobile` project repo serves at the domain root, not `/mobile`
**Finding:** Attaching the custom domain `kaunbanegapradhanmantri.in` to the `pradhanmantrielectionsgame/mobile` *project* repo (via a `CNAME` file `scripts/deploy-mobile.js` now writes into every build) makes that repo serve at the **root** of the domain — `https://kaunbanegapradhanmantri.in/` is the mobile game, with no `/mobile` path segment. The old `https://pradhanmantrielectionsgame.github.io/mobile/` now 301-redirects to the new domain automatically. The legacy desktop game (its own separate repo, `pradhanmantrielectionsgame.github.io`, not a remote of this clone) is untouched at `https://pradhanmantrielectionsgame.github.io/` and is unreachable from the new domain. `mobile/index.html`'s relative asset paths (`assets/…` after the deploy script strips `../`) resolve correctly at the domain root with no change.
**Context:** User raised a concern that the nested desktop-vs-`/mobile` structure might cause the domain to point at the wrong game. Verified with `curl` right after the v2.3.1 deploy: new domain returns the mobile `<title>` + `india-map` SVG, `/mobile/` 301s to it, desktop root still returns "India Elections Game V2".
**Implication:** No routing/redirect work is needed to "point the domain only at the mobile build" — putting the `CNAME` in the `mobile` repo's deploy output is the entire job. DNS: 4 `A` records at Porkbun → GitHub Pages IPs `185.199.108–111.153`, plus `www` `CNAME` → `pradhanmantrielectionsgame.github.io`.

## 2026-08-30 — `git push` with no args fails on this clone; use `git push mobile HEAD:source`
**Finding:** Local `main` tracks `mobile/source`, but `push.default` is unset and the local/remote branch names differ, so a bare `git push` refuses with "The upstream branch of your current branch does not match the name of your current branch." The working command for source pushes is `git push mobile HEAD:source`. Every push also prints `git: 'credential-manager' is not a git command` — this is harmless noise (a stale credential-helper config), the push still completes.
**Context:** Hit on the first source push of the v2.3.1 work. CLAUDE.md's Deployment section currently says "Push source with a plain `git push`", which does not work as-is on this clone.
**Implication:** Use `git push mobile HEAD:source` for source; `node scripts/deploy-mobile.js --push` still handles the deploy branch. Ignore the credential-manager line.

## 2026-08-30 — GitHub Pages HTTPS cert issued in ~1–2 min when DNS is pre-propagated
**Finding:** After adding the Porkbun DNS records, `kaunbanegapradhanmantri.in` resolved to all four GitHub IPs within minutes — before the `CNAME` was even deployed. Once the deploy pushed the `CNAME` file, GitHub provisioned the Let's Encrypt cert and `https://kaunbanegapradhanmantri.in/` served a 200 within ~1–2 minutes. GitHub's own docs quote 15 min–1 hr.
**Context:** Polled the live URLs with `curl` immediately after `deploy-mobile.js --push`.
**Implication:** When DNS is already propagated, there's no meaningful wait between the `CNAME` deploy and testing the live HTTPS domain — don't pad the schedule for it. "Enforce HTTPS" still has to be ticked by hand in the repo's Pages settings once the cert exists.

## 2026-08-30 — The engine's p1/p2 coupling is asymmetric: seat output already 3-way, action input rigidly 2-way
**Finding:** Grepping `mobile/engine.js` + `mobile/game.js` to cost out an "Others as a spending agent" feature: the seat-*output* path is already fully 3-way — `pop[svgId]` is `{p1,p2,others}` in bps everywhere, `apportionSeats()` and `nationalSeats()` split each state's seats across `['p1','p2','others']` with Hamilton's method (Others already wins ~70–99 of 543 seats per sim game), and `generateStartingPosition` starts Others as the largest bloc (~66% mean; `randInt(rng, 500, 2900)` per player, others = remainder). But the *action/input* path is rigidly binary: `otherPlayer(actor)` is `actor === 'p1' ? 'p2' : 'p1'`, wired into `gainAt`/`loseAt`/`resolveSimultaneousGain`/`computeScore`/the AI scorer; `startPhase` plus all three payout functions hard-code `['p1','p2'].forEach`; the whole action layer and `aiStep` assume the actor is p1 or p2. `gainAt` is commented in-file as "the one mechanic every lever routes through."
**Context:** Design discussion on 2026-08-30 about a "regional allies" / "allied mode" gameplay axis (captured in `design/allied-mode-proposal.md`).
**Implication:** Any feature that gives "Others" agency should manipulate the `pop` object directly at phase boundaries (scripted environmental resistance), never add `game.players.others`. A literal third player means rewriting `gainAt` to be 3-way, auditing every `['p1','p2']` loop, driving a third agent through `aiStep` under the human-fair-pacing rule, and fixing `simulate.js`/`balance-sim.js` — wide blast radius through the most rounding-sensitive code in the project. The scripted approach doesn't foreclose a real third player later if a 3-way FFA mode is ever wanted.

## 2026-08-30 — `showEndOverlay()` also runs at the end of a tutorial game, so the new high-score check fires there
**Finding:** `showEndOverlay()` in `mobile/main.js` is called from two places: the normal game-over branch of `doEndPhase()` (~line 1456) and the tutorial sign-off handler (`tutorialSignoffContinueBtn` → ~line 2721). By the time the tutorial path reaches it, `wasTutorialGame` has already been reset to `false` (in `doEndPhase`, before the sign-off overlay shows). The new `maybeRecordHighScore()` call sits at the top of `showEndOverlay()`, so a completed tutorial game records/updates the personal-best high score exactly like a real game — the tutorial game finalizes normally through `finalizeGame()` and gets a `game.score`.
**Context:** Traced `showEndOverlay` callers while adding the v2.3.0 high-score hook.
**Implication:** Not excluded deliberately (kept lazy — a tutorial score is still a score the player achieved, and it only ever seeds the baseline silently or gets beaten). If tutorial high scores turn out unwanted (e.g. the tutorial's bonus funds/tokens inflate the score past what a normal game reaches), capture `wasTutorialGame` before `doEndPhase` clears it and thread it into `showEndOverlay`, or move the `maybeRecordHighScore()` call into `doEndPhase`'s non-tutorial branch.

## 2026-08-29 — Only `generateStartingPosition` consumes replay-relevant RNG; every action function is deterministic
**Finding:** Before building the action-log replay, grepped the whole authoritative path (`mobile/engine.js` + `mobile/game.js`) for `rng` / `Math.random` / `Date` / `performance`. The only RNG draws that matter for reproduction are at game creation: `E.generateStartingPosition()` (the opening map) plus AI setup (`pickAIProfile`, `aiTargetGroup`). During play, only `pickAIRallyTarget` calls `game.rng()`. All six player-action functions (`investCash`, `playRallyToken`, `craftToken`, `tapAgenda`, `activatePower`, `activateNationwideRally`) and `endPhase`/`startPhase`/`applyPayouts`/`finalizeGame` are fully deterministic — no randomness, no wall-clock. Because replay re-applies the recorded *primitive calls* (not `aiStep`), the AI's own RNG never runs during replay, so a single seeded PRNG passed to `createGame` reproduces the entire game. Confirmed across 5 full politician-pair games (hundreds of interleaved human+AI actions each) replaying to byte-identical final seats and composite score.
**Context:** Systematic grep of the engine/game layer during the v2.2.0 action-log implementation, then verified by the new `assertReplayMatches` guard in `mobile/simulate.js`.
**Implication:** Input-based replay is low-risk in this codebase specifically because the action layer is already pure. Any future edit that adds `Math.random()`, `Date.now()`, or other non-determinism into an action function (or `endPhase`) silently breaks replay — and, later, multiplayer sync and server-side leaderboard validation. `npm test`'s `assertReplayMatches` catches it; keep that guard.

## 2026-08-29 — Playwright headless WebKit cannot reliably drive service workers
**Finding:** Attempting to smoke-test the new `sw.js` offline-fallback path (network-first fetch capped at `AbortSignal.timeout(3500)`), `navigator.serviceWorker.controller` never became truthy within 30 s in headless WebKit, and `getRegistration()` returned no active/installing/waiting worker even though `navigator.serviceWorker.register('sw.js')` itself resolved. Chromium behaved the same. What *could* be verified in-harness: `AbortSignal.timeout` exists in WebKit, the SW registration call succeeds, and an aborted `fetch` rejects (routing to the existing `.catch` → cache path, which is unchanged).
**Context:** Tried both a hand-rolled Node HTTP server and `serviceWorkers: 'allow'` contexts; SW never took control of the page.
**Implication:** Service-worker and offline-launch behavior must be verified on a real device — consistent with the existing CLAUDE.md rule for SW/viewport changes. A green Playwright run is not evidence for this class of change. The v2.2.0 SW timeout shipped verified-by-inspection (the fallback path is untouched; the change only adds a faster trigger into it) plus a real-device check still owed.

## 2026-08-29 — The phone install gate blocks Playwright functional tests on phone-sized viewports
**Finding:** `#welcomeStartBtn` stayed `hidden` (0×0 bounding box) in any Playwright context using the `iPhone 14` device profile or `hasTouch: true`. The install gate (`mobile/main.js`, top of IIFE) fires on `matchMedia('(pointer: coarse)')` + `matchMedia('(max-width: 700px)')` + not-standalone, and Playwright's touch emulation satisfies `pointer: coarse`, so the gate hides `#welcomeOverlay` and shows `#installGateOverlay` instead.
**Context:** Found while writing the v2.2.0 replay smoke test; the first two context configs (`devices['iPhone 14']`, then `{hasTouch:true}`) both left the start button unclickable.
**Implication:** Functional Playwright tests of the mobile game UI must bypass the gate — either use `viewport: { width: 480, height: 900 }` with no `isMobile`/`hasTouch`, or force `document.getElementById('installGateOverlay').hidden = true; document.getElementById('welcomeOverlay').hidden = false` via `page.evaluate()` right after `goto`. This does not apply to real-device testing (a real phone browser is genuinely non-standalone and the gate is intended there).

## 2026-08-29 — Consolidating onto `main` created a one-command path to deleting the live desktop game
**Finding:** Merging the mobile work onto `main` and deleting the other branches left local `main` **145 commits ahead of `origin/main`** — and `origin` is `pradhanmantrielectionsgame.github.io`, which serves the **live desktop game** from `main`'s root with no build step. By commit count the merge looked completely safe: `git merge-base --is-ancestor main perf-audit-fixes` reported a clean fast-forward, main 0 ahead / 145 behind, nothing divergent. The root-level file diff told the real story — **75 files deleted**, including `index.html`, `welcome-screen.html`, the whole `styles/` tree, `favicon.ico` and `ads.txt`, every one of which the live desktop site was serving at that moment (`index.html` → HTTP 200, 30,034 bytes). A single `git push` on `main` would have wiped the desktop game off the internet. Resolved by pushing the source to a new `mobile/source` branch and **removing the `origin` remote from this clone entirely**, so the destructive push is now impossible rather than merely avoided.
**Context:** Found while carrying out a request to merge all branches into `main` and delete the rest. The ahead/behind check looked clean; the CLAUDE.md rule mandating a *root-level file diff* before any merge into `main` on this repo is what surfaced it. Confirmed by curling the live desktop URLs for each file the merge would delete.
**Implication:** On this repo, ahead/behind counts are **actively misleading** about merge safety, because the mobile branch lineage deliberately deletes the desktop tree — a "clean fast-forward" here means "cleanly destroys the other game." That trap is now structurally removed rather than dependent on remembering the check. To work on the desktop game, clone `https://github.com/pradhanmantrielectionsgame/pradhanmantrielectionsgame.github.io.git` separately; it is no longer reachable from this working copy.

## 2026-08-29 — The deploy repo's `main` is a build artifact, not a backup — 7 commits of source existed only on one disk
**Finding:** While deciding where to point local `main` after the merge, it turned out the mobile **source** had no remote home at all. The `mobile` remote's `main` holds only the flattened deploy build that `scripts/deploy-mobile.js` writes (`index.html`, `engine.js`, `game.js`, `main.js` at the root — no `mobile/`, `design/` or `scripts/` folders), so it is an output artifact, not a copy of the project. The newest source snapshot anywhere off-machine was `origin/deploy-mobile-fresh-repo`, **7 commits behind** local `main` — meaning a week of work including the entire v2.1.0–v2.1.2 performance rework existed on this laptop and nowhere else. Fixed by creating a `mobile/source` branch carrying the full source tree, which local `main` now tracks.
**Context:** Surfaced when choosing a replacement upstream after removing `origin`; checked what each remote branch actually contained (`git ls-tree`) rather than assuming the deploy repo held the project.
**Implication:** The `mobile` remote now carries two branches with strictly separate jobs — `mobile/main` is the live deploy build (written **only** by `deploy-mobile.js --push`) and `mobile/source` is the source tree (plain `git push`). Never push source to `mobile/main` or the live game breaks. Push to `source` routinely as ordinary backup, not only when deploying — deploying does not back up the source, and never did.


## 2026-08-29 — `click` can't fire until the finger lifts, and Playwright structurally cannot measure it
**Finding:** A user report that double-tapping a state to invest "feels laggy even if they don't appear laggy" — an exactly correct description, because the sound and FX were fast once they started, they just started late. All 34 interaction handlers in `mobile/main.js` were bound to `click`, which cannot fire until `touchend`. A real finger dwells on the glass for ~70–120 ms, and every one of those milliseconds lands before the handler runs. Captured event order in WebKit: `pointerdown+0 → touchstart+0 → pointerup+0 → touchend+0 → click+7ms`. That 7 ms is a *synthetic* tap — Playwright presses and releases in the same tick, so the harness can never reproduce the dwell that is the entire quantity being removed. Fixed with a `fastTap()` helper that binds `pointerdown` and keeps a `click` fallback (deduped on a 700 ms window) so keyboard activation still works on real `<button>`s. Two smaller contributors on the same path: `.fx-flash` animated `width`/`height`/`margin` (layout properties, so 450 ms of main-thread layout+paint per tap, precisely while tapping fast), and `investPaid()` spawned the visual FX *after* `renderAll()` even though the sound had already been moved ahead of it for exactly this reason.
**Context:** Found while investigating the user's report after a v2.1.0 performance pass. The engine benchmark had already ruled out game logic (all hot paths in the µs range) and the in-game tap measured 3 ms median / 7 ms max, so the lag had to be upstream of the handler.
**Implication:** Bind game actions to `pointerdown`, not `click` — but never the politician carousel or any scrollable surface, where the `click` binding is what stops a swipe from firing a card. More generally: a clean automated-test pass is not evidence about perceived input latency, because synthetic taps have zero dwell. This class of change needs a real thumb on real glass.

## 2026-08-29 — A `loading="lazy"` image in a hidden subtree never loads, so the welcome gate was a pure 3.5 s stall
**Finding:** The welcome screen held "Begin Campaign" disabled until three carousel portraits settled, with a `setTimeout(resolve, 3500)` as a safety net. The timeout was not a safety net — it was the only path that ever fired. All 21 portraits carry `loading="lazy"` and live inside `#selectOverlay`, which is `hidden` behind the welcome screen, and a browser never fetches a lazy image in a hidden subtree. So `load` never fired, `img.complete` stayed false, and every single boot sat out the full timer while preloading nothing. Probed at the exact moment the button unlocked: 21 portraits found, 21 lazy, **0 complete, 0 with `naturalWidth > 0`**. Replaced with a `new Image()` preload of the first two portraits (which ignores visibility) plus a 1,500 ms straggler guard. Measured on the same harness with changes stashed/unstashed: navigation start → button enabled went **3,602 ms → ~150 ms**.
**Context:** Found during a full performance audit, by probing image state at button-unlock rather than trusting the gate's stated intent.
**Implication:** Never gate anything on a lazy `<img>`'s `load` event. To warm an image behind an overlay, use `new Image()`. Also a measurement lesson: `waitUntil:'load'` is the wrong yardstick for time-to-interactive here — it waits on the external GoatCounter script long after the button is tappable, and produced a misleading 2.4 s reading for the fixed build.

## 2026-08-29 — Portraits were alpha-free RGB PNG; gzip cannot compress PNG, and coordinate rounding cannot fix a traced SVG
**Finding:** All 21 politician portraits were 8-bit **RGB** PNG — colour type 2 in the IHDR, i.e. no alpha channel — a lossless codec carrying photographic art. Because PNG is already deflated, GitHub Pages' `content-encoding: gzip` does nothing for it, so 19.75 MB was real wire traffic on every cold visit. Re-encoding the identical 768 px pixels as WebP q80 gave **19.75 MB → 2.05 MB (9.4×)**; JPEG q82 gave 2.87 MB. Separately, two party-logo SVGs were the same failure in vector form: `All_India_Trinamool_Congress_logo_(3).svg` was 895 KB — 130,474 coordinate points across 7 auto-traced paths — rendering as a ~30 px badge. Rounding its coordinates to 2 decimal places saved only 13%; rasterising to 96 px WebP took it to 17 KB (989 KB → 25 KB for the pair). Deployed assets went 23 MB → 2.5 MB, boot payload 3.67 MB → 0.89 MB.
**Context:** Found during a full performance audit by reading the PNG IHDR colour-type byte directly and by counting coordinate floats in the SVG path data, rather than trusting file extensions.
**Implication:** Check an asset's colour type as well as its dimensions — no alpha means it should never be a PNG. And an auto-traced SVG is a raster problem wearing a vector extension: rasterise it, don't minify it. Note the earlier 2026-08-29 finding below fixed only the *dimensions* half of this; format was still wrong.

## 2026-08-29 — A DOM node on the `game` object silently kills the AI for the rest of the match
**Finding:** Caching the 15 `.gchip` elements as `g.chipEl` on each group object (to avoid re-running `document.querySelector('.gchip[data-key="…"]')` 15× per render) broke the AI entirely. `planAITickPacing()` does `structuredClone(game)`, and a DOM node is not structured-cloneable. Worse than a visible crash: the clone is taken *after* `game.rng` has been nulled for it (functions aren't cloneable either), so the throw skips the line that restores it. The AI then dies for the rest of the match with `game.rng is not a function`. Fixed by keeping the references in a module-level `groupChipEls` side map keyed by group key.
**Context:** Surfaced by a scripted WebKit playthrough that watches `pageerror` — two errors appeared, "The object can not be cloned." followed by "game.rng is not a function", neither of which was visible from reading the diff.
**Implication:** Nothing but plain data belongs on the `game` object. Keep every UI reference in a module-level side map in `main.js`. Any change that adds a field to `game`/`game.groups`/`game.states` should be checked against `structuredClone` — and note the failure mode is silent AI death, not a crash the player would report as such.

## 2026-08-29 — `#cardGroups` is a shared DOM slot, so a content-key rebuild guard goes stale
**Finding:** Adding a `dataset.key` guard to skip rebuilding the info panel's LED chip grid (the same trick `syncNewsFeed()` already uses) introduced a display bug, because `#cardGroups` is not owned by one renderer. Three others — `renderActionInfo`, `renderAgendaCard` and `renderStateCard` — repurpose the same element for entirely different content without touching the key. So the sequence view a group → tap a state → re-select the group left the key still matching, the rebuild skipped, and the state card's content stranded on screen. Fixed by clearing `dataset.key` in all three sibling renderers.
**Context:** Caught by a scripted card-cycling regression check (group → state → group → cluster, asserting the grid repopulates each time), not by reading the diff.
**Implication:** Any cache or guard keyed on a shared DOM slot must be invalidated by every writer of that slot, not just its primary owner. This project has now hit the "two places must agree" failure mode several times — treat it as a standing checklist item.

## 2026-08-29 — `deploy-mobile.js` rewrites paths by literal string match, so a local variable defeats it
**Finding:** `scripts/deploy-mobile.js` strips the `../` prefix from asset paths by literal string replacement, and knows exactly two JS forms: `'../' + p.image` and `'../' + p.partyLogo`. The v2.1.0 portrait preload hoisted the path into a local (`'../' + src`), which survived the rewrite untouched — the deployed build would have requested `../assets/images/…` from the site root and 404'd both preloads on every boot. The app would still have started (the `onerror` handler resolves the gate, in fact faster) but the preload would have done nothing, and two 404s would appear on every live visit. Rewritten to use `'../' + p.image` inline, matching the four existing call sites.
**Context:** Caught before pushing by dry-running the deploy script's `stripRelativePaths()` transform over `mobile/main.js` and `mobile/index.html` and grepping the output for surviving `../` occurrences — the only legitimate leftover was inside a comment.
**Implication:** Write asset paths in the exact literal forms `stripRelativePaths()` recognises, never via an intermediate variable. Dry-run that transform over the sources and grep for leftover `../` before any deploy — the failure is silent on the live site, not a build error.

## 2026-08-29 — Rally dot spacing was a fixed-px constant the v2.0.0 rem migration missed
**Finding:** A user report that two rally tokens on the same state sit too far apart, "fine for large states but weird for small ones." The dot pitch in `renderRallyTokens()` was a hardcoded `16px` while the dot itself is `0.375rem` (~5.84 px measured) — so two dots sat nearly two full dot-widths apart. The v2.0.0 viewport rework migrated ~330 CSS `px` values onto a `rem` scale but did not touch JS-side geometry constants. Fixed by deriving the pitch from the live root font size (`0.55rem`) and clamping it to the state's own width (`w * 0.38`, floor `0.28rem`), so a pair reads as one tight cluster instead of straddling a small state's borders.
**Context:** Reported by the user after playing the v2.1.0 build on a real device.
**Implication:** The v2.0.0 rem migration covered CSS only. If other spacing looks subtly wrong at non-default scales, grep `mobile/main.js` for remaining fixed-px geometry rather than assuming the migration was complete.

## 2026-08-29 — Rally dots go stale on *any* layout change, because several paths bypass `renderAll()`
**Finding:** Rally dots are placed in viewport coordinates from a live `getBoundingClientRect()` measurement of the map, so any layout shift moves the states out from under them. Moving `renderRallyTokens()` to last in `renderAll()` (so it measures a settled layout) was necessary but **not sufficient**: selecting a state, a group, or a quick-invest cluster calls `updateCard()` *directly* and never enters `renderAll()` at all. Reproduced concretely — switching to a cluster card shrank the info panel 86 px → 65 px, the map grew, the state's centre moved 270.5 → 281.1, and the dots stayed at 270.5, a **10.6 px** stale offset. Fixed with a `ResizeObserver` on `.map-wrap`, which catches every such path at once plus rotation, window resize, and mobile browser chrome sliding in and out. Measured drift after the fix: **0.0 px**. No feedback loop, because `renderRallyTokens` only writes into `#rallyTokenLayer`, a `position:fixed` sibling that cannot affect `.map-wrap`'s size.
**Context:** The user described it as the dots looking wrong "when the screen resizes slightly when the info bar height changes slightly" — pointing directly at the info panel is what located it. Confirmed with a scripted probe comparing each dot's y to its state's live centre across three different card types.
**Implication:** Anything positioned from a JS measurement of another element's box needs a `ResizeObserver` on that element, not a call at each mutation site — the caller list is never complete, and misses resize/rotation entirely. Do not assume `renderAll()` is the only path that changes layout.

## 2026-08-29 — The game engine is not a bottleneck anywhere; three suspected hot spots measured clean
**Finding:** A full performance audit benchmarked the real engine against live `createGame` state and found nothing worth optimising. `investCash` with its full regional-dominance + clean-sweep payout sweep: **0.27 µs**. `dominanceActive` across all 15 groups × 2 players: **11.4 µs**. `nationalSeats`: **19.1 µs**. `aiStep`: **26.0 µs**. `planAITickPacing`'s `structuredClone(game)` + 500-step AI dry run — which looked like an obvious per-phase hitch — is **0.3 ms**, ten times a game. The inline India map SVG (198 KB of path data across 36 paths) is already at 1 decimal place, so rounding coordinates saves *literally zero bytes*, and it gzips to 65 KB with the rest of `index.html`. DOM size (1,925 nodes in game) and CSS volume (334 rules / 40 KB) are both healthy.
**Context:** Measured with a Node harness using `process.hrtime.bigint()` over the real game object, and in-browser with `performance.now()`, as the first step of the audit — before making any changes.
**Implication:** Do not spend future optimisation effort on engine math, `planAITickPacing`, or map-SVG minification. This project's performance cost lives in assets and DOM work, not in game logic. Measure before optimising: two of these three were on the suspect list and both were wrong.


## 2026-08-29 — "Missing" politician images were oversized PNGs blowing iOS Safari's image-memory cap
**Finding:** A user report that "most politician images are missing" in the latest deploy. Every one of the 21 portraits returns HTTP 200 at full bytes from the live site, and the deployed `main.js` references them correctly (`p.image` → `assets/images/X.png`, sibling of the page). The real cause is client-side: the portraits are 2048×2048 PNGs, 1.5–7.9 MB each (~75 MB total), and the select carousel builds all ~21 cards at once, kicking off all 21 `<img>` fetches + decodes simultaneously. That exceeds iOS Safari's per-tab decoded-image memory cap (~200–300 MB; 21 × 2048² × 4 B ≈ 350 MB), so Safari drops most of them, `onerror` fires, and you get the colored-initial fallback. Fixed by downscaling to 768 px (`scripts/shrink-images.py`, ~75 MB → ~20 MB, decoded RAM ~50 MB) plus `loading="lazy"` on carousel portraits.
**Context:** Diagnosed by curl-checking every live image URL (all 200, full size), verifying deployed `main.js` image refs, then running `file` on the source PNGs and seeing 2048×2048 / multi-MB.
**Implication:** Keep raster assets sized to their display resolution — never ship 2048² for a ≤400 px render. When "images missing" is reported, check actual file dimensions and byte sizes, not just paths and HTTP status. The `onerror`→initials fallback masks this as "missing" rather than "too big to decode."

## 2026-08-29 — GitHub Pages max-age plus a plain SW fetch() = deploys lag up to 10 min on-device
**Finding:** After deploying v2.0.1 the user still saw v2.0.0. GitHub Pages sends `cache-control: max-age=600` on `index.html`/`*.js`. The service worker was "network-first" but its plain `fetch(e.request)` still reads the browser's HTTP cache, so it served the 10-minute-stale copy. Passing `{cache:'reload'}` to `fetch()` bypasses the HTTP cache. Separately: the GitHub Pages CDN itself takes ~40 s to rebuild and propagate after a push (observed `GAME_VERSION` flip from 2.0.1→2.0.2 on the 3rd poll, 40 s apart).
**Context:** User reported stale version; checked live response headers with `curl -I`, then polled the live `main.js`/`sw.js` in a loop after the v2.0.2 push.
**Implication:** A "network-first" SW must pass `{cache:'reload'}` for anything that needs to be fresh on deploy (code, JSON) — but NOT for large media, or repeat visitors re-download it every load (split media to cache-first, flush via CACHE version bump). After a deploy, verify the live site with a polling loop, not one immediate curl.

## 2026-08-29 — deploy-mobile.js copies the working tree, not HEAD, and labels the commit with the source HEAD sha
**Finding:** `scripts/deploy-mobile.js` reads runtime files straight from the `mobile/` and `assets/` working tree (`fs.readFileSync` / `fs.cpSync`), then labels the deploy-repo commit `Deploy from <git rev-parse --short HEAD>`. Running it with uncommitted source changes ships those changes but stamps the commit with the *pre-change* sha — the first v2.0.1 deploy landed as "Deploy from 2710623" (the old HEAD) instead of the actual source commit.
**Context:** Noticed the deploy worktree had the 768 px images and `GAME_VERSION 2.0.1` but its commit message referenced the HEAD from before those changes were committed.
**Implication:** Commit source changes before running `node scripts/deploy-mobile.js --push` so the deploy commit message points at the right source sha.

## 2026-08-28 — Viewport migration (v2.0.0): what old WebKit (~2014 iPad) does and doesn't support
**Finding:** The user's ~2014 iPad runs an old WebKit that silently drops several CSS features the v2.0.0 rework used: `margin-inline` / `margin-block` (Safari 14.5+), `clamp()` (13.4+), `visualViewport` (13+), flex `gap` (14.1+), `aspect-ratio` (15+). With `margin-inline:auto` dropped, the capped 520px column never centred — it pinned to the top-left corner. Separately confirmed: old WebKit does **not** re-resolve `margin:auto` after `max-width` clamps the width of a `position:fixed` element (the "apply the rules again with computed width" step of CSS 2.1 §10.3.7) — so even `margin-left/right:auto` left the box in the corner. The working fix was `left:50%` + `transform:translate(-50%,-50%)` (plain transform math, iOS 9+), in a rule placed at the very end of the stylesheet.
**Context:** User tested v2.0.0 on a ~2014 iPad (standalone/Home-Screen), reported "everything bunched up in the corner." An on-screen debug readout (temporarily added) showed `window.innerWidth` 768 but `document.documentElement.clientWidth` 168, `visualViewport` returning 0/0, and `rootFS 16px` (the `clamp()` fallback). A modern iPad and iPhone rendered correctly in Playwright/WebKit sim throughout — the sim uses current WebKit and can't reproduce the old-engine gaps.
**Implication:** For this project's CSS, centring a capped-width fixed layer must use `left/top:50% + transform`, never `inset:0 + margin:auto`. A ~2014 iPad is out of scope (would need every flex `gap` replaced with margins and `.action-btn` given an explicit height) — a current iPad is fine. Any future "looks broken only on an old device" report: add a temp on-screen readout of `innerWidth` / `documentElement.clientWidth` / `visualViewport.scale` / `getComputedStyle(:root).fontSize` rather than guessing.

## 2026-08-28 — `.stage` had no `[hidden]` rule, so the empty game board rendered behind every other screen
**Finding:** `.stage` sets `display:flex` in its base rule and there was no `.stage[hidden]{display:none}` — so the bare `[hidden]` UA rule lost to it (author beats UA at equal specificity) and `<div class="stage" hidden>` rendered anyway. `main.js` only ever sets `$('stage').hidden = false` (never back to `true`), and the other display-setting overlays (`.overlay`, `.welcome-overlay`, …) all *do* have their own `[hidden]` rules — `.stage` was the lone gap. Latent since the original build; invisible on a phone (the full-screen welcome/select overlays cover it) but very visible once every layer became a centred 520px column on iPad.
**Context:** Found while diagnosing the iPad screenshot showing the welcome screen and a half-rendered game HUD stacked on top of each other. A debug check reported `stageHidden: true` but `stageDisplay: "flex"`.
**Implication:** This project has hit "a `[hidden]` toggle silently does nothing because the base rule sets `display`" repeatedly (`.overlay`, `.group-readout`, `.target-banner`, `.vs-bar`, now `.stage`) — it's already a CLAUDE.md checklist item. Add the explicit `<selector>[hidden]{display:none}` for any new toggleable block whose base rule sets its own `display`.

## 2026-08-28 — `viewport-fit=cover` is what activates `env(safe-area-inset-*)`; the old no-tag build never had it
**Finding:** The old build shipped with no `<meta viewport>` tag at all, so every `env(safe-area-inset-*)` in the CSS resolved to its `0px` fallback — content sat flush to the screen edges. Adding `viewport-fit=cover` to the new viewport tag switched those insets on, which introduced a ~34px strip at the bottom of the info panel (the home-indicator safe area) that the user correctly flagged as new wasted space. Dropped `viewport-fit=cover` (kept `width=device-width, initial-scale=1`) to restore flush edges; left the `env(…, 0px)` calcs in place (harmless, resolve to the constant) in case cover is ever wanted for a notched-phone pass.
**Context:** User compared v2.0.0 against the old build: "the old version was snug to the screen bottom without the 34px strip."
**Implication:** Don't add `viewport-fit=cover` to `mobile/index.html` without also auditing every `env(safe-area-inset-*)` usage — turning it on changes real layout. The app is designed to sit flush.

## 2026-08-28 — Headless Playwright/WebKit routes page audio to the system output
**Finding:** A screenshot script that clicked "Begin Campaign" started the game's background music audibly on the machine, even though the browser was headless. The user heard it and asked what was playing.
**Context:** Building throwaway screenshot harnesses for the viewport migration; each `page.goto` + click-through-to-game triggered `unlockSounds()` / `bg_music`.
**Implication:** Any Playwright script for this project that navigates into the running game must stub audio in an `addInitScript` — override `HTMLMediaElement.prototype.play` to a no-op resolved promise and replace `AudioContext` / `webkitAudioContext` with a no-op Proxy. `--mute-audio` is Chromium-only and doesn't help WebKit.

## 2026-08-28 — Rahul Gandhi's original agenda kit netted negative popularity in his own home state
**Finding:** The pre-existing kit (Rural Development/Press Freedom/Secularism/Caste Reservation) netted -4bps in Uttar Pradesh — Secularism's -12 HindiHeartland/-12 Pilgrimage penalty applied in full there, but its +12 MinorityAreas benefit never did (UP isn't a MinorityAreas state). This was the dominant cause of his dead-last 38.4% win rate in `mobile/balance-sim.js`, not his special power. A tested fix (swapping Secularism → Food Security) raised win rate to 57.2% and home popularity from 29.4% to 58.1%, but was reverted at the user's request in favor of redesigning the special power instead, leaving this root cause unaddressed in the shipped state.
**Context:** Diagnosed via `mobile/balance-sim.js` plus a custom per-state net-effect script cross-referencing `data/policy-tags.json` against `data/states_data.json` group membership.
**Implication:** If Rahul's win rate is still weak after the power redesign, this agenda-kit home-state drag is the real remaining lever to pull.

## 2026-08-28 — toBps ("floor") special powers show zero measured seat impact when used late-game
**Finding:** Sardar Patel's Iron Unification and Sachin Tendulkar's National Icon (both `toBps` "raise to at least X%" effects) measured 0 seats added when applied against realistic late-game average popularity, because natural growth over the game usually already exceeds their floor by then. Not a measurement bug — the floor genuinely has nothing left to raise once popularity has organically passed it.
**Context:** Found while building a full-roster power comparison (applying each politician's real power benefits to their own average final-game popularity to measure seat delta).
**Implication:** Don't compare a floor-type power's "seats added" to a flat-bonus power's without noting when in the game it was measured — floor powers are front-loaded (most valuable early), unlike a flat or deferred-growth bonus.

## 2026-08-28 — craftToken() (Special Powerup / Nationwide Rally crafting) is not subject to the per-phase rally-spend cap
**Finding:** `playRallyToken()` enforces `maxTokenSpendPerPhase` (2/phase), but `craftToken()` (used to craft both the Special Powerup and the Nationwide Rally) has no such check — a player can bank tokens across several phases and spend a large chunk (6 or 12 at once) on crafting without it counting against that phase's rally-spend limit.
**Context:** Traced while checking whether an earlier (since-abandoned) power design — requiring 2 rallies each in 6 states plus 1 each in 4 more, alongside crafting a Special Powerup — was reachable within a 10-phase game's token budget.
**Implication:** Relevant to any future token-economy balance work — crafting costs and rally-spend costs draw from the same token pool but aren't rate-limited the same way per phase.

## 2026-08-27 — GitHub repo Insights (views/clones) never sees traffic to a deployed GitHub Pages site
**Finding:** The `mobile` repo's Insights showed 58 clones but 0 views / 0 unique visitors over 14 days, despite the user knowing friends had downloaded and played the live game. GitHub's repo-level "views"/"clones" metrics only measure git/GitHub-UI activity — cloning the source (`git clone`/`pull`, including CI/bots) or browsing the repo's code page on github.com — not requests to the deployed GitHub Pages site, which is a separate serving surface Insights doesn't instrument at all.
**Context:** User asked "who's cloning?" then "so how many people are playing the game," expecting Insights to answer both; investigation showed the two questions need entirely different data sources.
**Implication:** Any future "how many people are using X" question for a GitHub-Pages-hosted project must be answered via in-page analytics, not repo Insights. Fixed by adding a GoatCounter snippet (free, cookie-less, no consent banner) to `mobile/index.html`, reporting to `https://pradhanmantrielections.goatcounter.com` — see CLAUDE.md's Deployment section for the standing pointer.

## 2026-08-27 — mobile/index.html's inline CSS has ~410 raw px declarations across 8-9 untokenized visual subsystems; only the in-game HUD uses the existing --fs-*/--radius-* tokens
**Finding:** All CSS lives inline in `mobile/index.html` (653 lines across two `<style>` blocks, no separate stylesheet). ~575 px-value occurrences, ~410 actual property declarations (font-size, padding, margin, gap, width, height, border-radius), zero `rem` usage, one media query total (an accessibility `prefers-reduced-motion` rule, not a responsive breakpoint). A partial design-token system exists (`--fs-1` through `--fs-6`, `--radius-sm`/`--radius-md`) but per its own code comment was only ever applied to "the HUD screen only (topstrip, map corner buttons, info panel)" — 13 of 65 non-tokenized font-size declarations use it. Everything else (politician-select ballot cards, welcome screen, tutorial overlay, settings, share sheet, end-of-game card, install gate) is raw one-off literal px per rule.
**Context:** Deep-reviewed the stylesheet at the user's request, to assess how big a "do it right" responsive/viewport redesign would be, after a same-session iPad rendering bug and its real-device-breaking first fix (see next two entries).
**Implication:** A proper responsive redesign should extend the existing `--fs-*`/`--radius-*` token pattern project-wide (plus add the spacing tokens that don't yet exist) rather than starting a token system from scratch — but still requires auditing and re-verifying every screen individually, since the project also has several previously-fixed pixel-specific bugs (flex `align-items:stretch` zero-width bug, honeycomb N/(N-1) centering math, `[hidden]` CSS-specificity collisions, safe-area-inset handling) that a global scale change risks reopening. Scoped as its own dedicated design cycle, not a reactive patch — deferred at user's direction.

## 2026-08-27 — mobile/main.js's map-tap/DOM-position math is already scale-agnostic; a responsive redesign is CSS-only
**Finding:** `mobile/main.js`'s map-tap and DOM-position code uses `getBoundingClientRect()` and `clientX`/`clientY` throughout — both automatically account for the browser's current zoom/scale with no manual calculation needed. No JS code depends on the app's implicit ~0.4x auto-zoom fallback.
**Context:** Checked while assessing the scope of a proper viewport/responsive redesign, to confirm whether the fix would need to touch game/interaction logic or just styling.
**Implication:** Future responsive redesign work can be scoped purely to `mobile/index.html`'s CSS without touching `mobile/main.js`/`game.js`/`engine.js` — confirms the user's own assumption going in.

## 2026-08-27 — Playwright's bundled WebKit is not a faithful stand-in for real Mobile Safari on the no-viewport-tag auto-scale fallback specifically
**Finding:** A dynamic-viewport-meta-tag fix (injecting `<meta name=viewport content="width=<screen.width*2.5>, initial-scale=1">` via an early inline script) tested correctly in Playwright/WebKit across both iPhone 14 and iPad device profiles (screenshots confirmed full-card, full-HUD rendering; measured `visualViewport.scale` matched the intended ~0.4x on both). On an actual iPhone in real Safari (tested live via a cloudflared tunnel), the same fix caused a severe regression: an extreme zoomed-in crop showing only oversized cropped background art, with the title and buttons entirely off-screen.
**Context:** Discovered immediately after the user tested the "verified" fix on their real phone, right after Playwright verification had passed with no red flags.
**Implication:** This specific class of viewport/auto-scale behavior must be verified on a real device (or Capacitor-wrapped build) before shipping — Playwright/WebKit screenshots passing is not sufficient evidence here. Root-caused to the explicit `initial-scale=1` in the injected tag interacting differently with real Mobile Safari's auto-fit algorithm than with Playwright's WebKit build; the eventual safe fix dropped `initial-scale` entirely and scoped the injected tag to tablet-and-up screen widths only (phones take an early-return path with zero DOM changes). This fix was later reverted in full per user direction — see the deferred-redesign decision above — but the underlying Playwright-vs-real-Safari gap remains true and worth remembering for any future viewport work on this project.

## 2026-08-27 — The app's iPad rendering broke because its implicit no-viewport-tag auto-scale fallback scales an iPad far less than an iPhone
**Finding:** Without a `<meta name=viewport>` tag, mobile Safari/Chrome default to a fixed 980px layout viewport, auto-scaled to fit the device — which computes to ~0.4x on an iPhone (390-430px wide) but only ~0.83-1.0x on an iPad (810-1024px wide), confirmed via Playwright/WebKit measurement (`visualViewport.scale`: iPhone 14 ≈0.40, iPad gen7 ≈0.83, iPad Pro 11 ≈0.85). Since `mobile/index.html`'s CSS is authored at ~2.5x assuming the iPhone-only ~0.4x scale, the same CSS renders roughly 2x too large on iPad, overflowing fixed-height containers — concretely, the politician-select ballot card's "Play" button was pushed below the fold with no scroll available to reach it, confirmed via screenshot.
**Context:** User reported "anything outside a mobile screen size is non functional or looks horrible" after testing different viewports; root-caused via Playwright webkit + iPad device profiles (`iPad (gen 7)`, `iPad Pro 11`) against the local dev server.
**Implication:** Any future fix for this must account for the fallback's device-width-dependent scale ratio (not just declare one fixed corrective value), and must be verified on a real device per the entry above. This specific bug is unresolved as of this checkpoint — the interim JS fix was built, then fully reverted at user request in favor of treating viewport/responsive support as its own separate design cycle.

## 2026-08-26 — Normalizing a magnitude to the nearest tier can be unsafe in both directions when two other tags combine against it
**Finding:** Normalizing Administrative Reform's `IndustrialCorridor` from 6 to the nearest convention tier (4 or 8) is unsafe either way: Jharkhand and Uttar Pradesh each pair it with a single `-4` tag (needs `IndustrialCorridor != 4`), while Madhya Pradesh pairs it with *two* `-4` tags summing to `-8` (needs `IndustrialCorridor != 8`) — no value in {4, 8} satisfies both constraints at once. Only the third tier, 12, clears all three states.
**Context:** Verifying same-state-cancellation safety before normalizing 12 off-convention `tagEffects` magnitudes (±6→±8, ±10→±12) across 7 policies onto the ±4/±8/±12 convention the new agenda emoji-reaction feature reads.
**Implication:** When normalizing a magnitude onto a fixed tier set and a same-state conflict exists, check the full tier set, not just the two numerically-nearest options — the safe answer can be the tier requiring the biggest jump from the original value. (In this specific case, the user later directed a uniform round-to-nearest rule regardless of new collisions, so this particular safety analysis wasn't ultimately applied — but the technique is worth reusing for any future tier-normalization work.)

## 2026-08-26 — A single group's repeat snipe-and-recapture cycle can consume a large fraction of one player's lifetime budget via the instant (not recurring) dominance payout
**Finding:** "Losing and re-earning dominance pays again in full each time" (an existing, untouched mechanic) means one large group (Agricultural Region, 273 seats) recaptured 3 extra times after its first capture yields ~6,552 Cr total (4 instant payouts of 1,365 Cr + ~1,092 Cr recurring) — about 24% of the ~27,500 Cr lifetime budget, from one group's back-and-forth alone.
**Context:** Computed while investigating a reported blowout result that persisted after two rounds of cutting the recurring group-holding bonus (2→1→0.5 Cr/seat); the user ultimately attributed the blowouts to the recurring bonus instead, based on their own play-history timeline (narrow results before the recurring bonus existed, blowouts after), and directed leaving the instant-payout mechanic alone.
**Implication:** If blowout reports continue even at 0.5 Cr/seat recurring bonus, the instant re-capture payout is the next most likely structural lever — but this is explicitly deferred per direct user instruction, not to be re-raised without new evidence.

## 2026-08-26 — Only one AI personality profile builds groups; sniping them is a free side effect of every profile's baseline scoring
**Finding:** Only `group-bonus-rusher` (1 of 4 AI profiles) actively pursues completing a regional-dominance group for itself; the other three only capture a group incidentally. Separately, every profile's baseline investment score includes a small bonus for states where the opponent currently leads — which can incidentally knock a human's already-completed group below the 50% threshold with no group-awareness at all, since dominance requires *every* member state above threshold but breaks the instant any *one* drops below it.
**Context:** User reported the AI "kept sniping" groups they'd won while itself finishing every game with 0 regions dominated (confirmed across two separate reported blowout games, 7-0 and 5-0 splits).
**Implication:** Denial of an opponent's group is structurally free/universal in this AI design, while conquest is rare and profile-gated — this asymmetry, not the holding-bonus magnitude, is the more likely structural driver of one-sided "regions dominated" splits. User has directed no AI change for now; revisit if requested later.

## 2026-08-26 — The AI was hard-capped to one rally per phase by a redundant flag stacked on top of the real spend cap
**Finding:** `aiStep()`'s rally branch (`mobile/game.js`) gated on a since-removed `aiRalliedThisPhase` boolean *in addition to* the correct, already-present check (`tokensSpentThisPhase < maxTokenSpendPerPhase`, currently 2 — the same cap a human plays under). Every AI used at most half the rally budget a human can, in every game, regardless of personality profile.
**Context:** User asked why the AI used so few rallies (5 vs. the human's 16) and never used its Nationwide Rally despite apparently "holding out" for it.
**Implication:** Whenever AI behavior looks throttled relative to a human's, check for a redundant AI-only gate stacked on top of the real shared limit, not just whether the shared limit itself is correctly applied.

## 2026-08-26 — policy-tags.json's tagEffects region keys are exactly the 15 regional-dominance groups' own keys, one-to-one
**Finding:** Confirmed by direct comparison against `states_data.json`'s region columns: every region string used as a `tagEffects` key (e.g. `EasternBorder`, `CoastalIndia`) is identical to one of the 15 regional-dominance groups' own `key` field, and the sets match exactly (15 of each, no leftovers either side).
**Context:** Discovered while simplifying the new agenda emoji-reaction feature — an initial implementation dynamically simulated each tap's actual redistribution outcome (producing messy, hard-to-tier numbers from 50-6900 bps) before realizing the static authored magnitude was directly available.
**Implication:** Any future UI or engine code needing "how strongly does policy P pull on group G" should read `policy.tagEffects[G.key]` directly (see `mobile/game.js`'s `agendaGroupEffects()`) — never re-derive it by simulating a tap's actual outcome, which reflects redistribution math the player doesn't see and doesn't match the clean authored magnitude.

## 2026-08-26 — Home-state bonus's real marginal seat contribution is empirically ~25% of the state's raw seats, matching its own bonus/BPS ratio exactly
**Finding:** The home-state bonus (+2500 bps, a flat addition to the player's own share with "others" absorbing the entire hit rather than splitting with the opponent) contributes almost exactly 25% of a home state's raw seat count to the player's expected final seats — confirmed consistently across Gujarat (26 seats), Uttar Pradesh (80 seats), Delhi+Punjab (20 seats combined), and Uttar Pradesh+Tamil Nadu (119 combined) home-state configurations.
**Context:** Calibrating how much of a home state's raw seats to fold into `generateStartingPosition()`'s random-draw budget counter — a naive 1:1 folding over-corrected, dropping every politician's mean below the no-home baseline (since a drawn state's 35-65% boost converts far more efficiently to seats than the home bonus's flat +25%).
**Implication:** This 25% conversion factor is a principled, derivable constant (`HOME_STATE_BONUS_BPS / BPS`), not an arbitrary empirical number — if the home-bonus magnitude ever changes, the draw-budget weighting formula must be recalculated from the new ratio, not left at a hardcoded 0.25.

## 2026-08-26 — generateStartingPosition() excluded home states from its own seat-budget accounting, letting a large or multi-home politician's bonus stack for free
**Finding:** The random-draw budget thresholds (calibrated for a ~150-seat mean) fully excluded home states from the seat-budget counter that limits the draw — home states got their +25% bonus for free, on top of a budget sized as if starting from zero. Invisible for a typical small/medium single home state, but broke down for a large one (Uttar Pradesh alone: mean crept to ~156.6) and especially a multi-home politician (Hema Malini's Uttar Pradesh + Tamil Nadu: mean ~166.2, max ~199) — producing the reported 213-seat outlier start.
**Context:** User reported Hema Malini started a real game with 213 seats, "way too high." First attempted fix (deleting her secondary home state entirely) was reverted after the user clarified the dual-home was a deliberate design choice; the real bug was in the generator's accounting, not her data.
**Implication:** Any future change to a politician's home-state count or size must be validated via direct simulation (`generateStartingPosition()` + `nationalSeats()` across many seeds), not by reasoning about the flat +25% bonus number in isolation — the real seat impact depends on how it interacts with the draw-budget accounting.

## 2026-08-26 — Tutorial AI could launch its own Nationwide Rally because only one of two required flags was pre-set
**Finding:** `mobile/main.js` pre-set `usedSpecial = true` for the tutorial's AI opponent to disable its special power, but never set the equivalent `usedNationwide` flag — so the AI could still craft and activate its own Nationwide Rally before the tutorial's scripted player-only moment for it.
**Context:** User reported "Rahul Gandhi used the nationwide rally" during the tutorial, which isn't supposed to happen.
**Implication:** When disabling an AI capability for a scripted/tutorial context, grep for every flag the AI's engine code actually gates on (here, both `usedSpecial` and `usedNationwide`), not just the one most obviously related to the reported symptom.

## 2026-08-25 — Tutorial coach text was rendered via `.textContent`, and a matching stage-title element existed but was never populated
**Finding:** Two separate gaps found while adding bold headers to the in-match tutorial. First, `renderTutorialStep()`/`renderTutorialStageStep()` in `mobile/main.js` set tutorial title/body text via `.textContent`, which silently strips any HTML tags — so no `<b>`/heading markup could ever have displayed, even if written into the step data. Second, `mobile/index.html` already had a `#tutorialCoachStageTitle` element (styled identically to the select-screen's working coach title), but `renderTutorialStageStep()` hardcoded it to `hidden = true` and never wrote to it — a dead, unused element sitting parallel to a fully wired one.
**Context:** Found while implementing the user's request to add emphasis/formatting and section headers to `TUTORIAL_STEPS`/`TUTORIAL_STAGE_STEPS` copy.
**Implication:** Switched both render points to `.innerHTML` (safe here — all tutorial copy is static, developer-authored strings, never user input) and wired the stage title element up to a new `title` field on each step, matching the existing select-screen coach pattern. When adding a new UI element that parallels an existing one, check whether the render function actually uses it, not just whether it exists in the markup.

## 2026-08-25 — Invest-tap sound lag had two separate causes: JS render ordering, then inherent `<audio>` playback latency
**Finding:** The `money_spent` sound felt delayed after tapping to invest. First cause: `investPaid()` and the two batch-invest handlers called `renderAll()` (a full synchronous re-render, including a layout-forcing `getBoundingClientRect()` inside `renderRallyTokens()`) *before* `playSound()` — reordering to play the sound immediately after the invest succeeds helped, but the user still reported it "still feels delayed-ish" afterward. Second cause, found after the reorder: `<audio>` elements carry real, often 100ms+, playback-start latency in the browser/OS pipeline itself, which no amount of JS-level reordering touches.
**Context:** User reported the lag twice — once before any fix, once after the render-ordering fix alone. The second report is what revealed the reorder wasn't sufficient and pointed at a lower-level cause.
**Implication:** For any future report of a UI action "feeling delayed" that involves a sound, check render-ordering first (cheap, and often the actual bug), but don't assume it's the *only* cause if the report persists after fixing it — inherent `<audio>`.play() latency is a distinct, real constraint that only Web Audio's `AudioBufferSourceNode` (pre-decoded buffer, sample-accurate start) actually solves.

## 2026-08-25 — iOS Safari auto-suspends AudioContext more aggressively for a bookmarked tab than a standalone PWA, and un-awaited resume() silently breaks the success/fallback contract
**Finding:** After switching tap-feedback SFX to Web Audio (`primeTapSfx()`/`playTapSfxBuffer()`), the user reported "some sounds stopped loading" testing on an iPhone with a bookmarked (not home-screen-installed) page. Root cause: `playTapSfxBuffer()` called `sfxCtx.resume()` (async, not awaited) then immediately started the buffer source and returned `true` unconditionally — so a tap landing while the context was still suspended (iOS auto-suspends contexts on any idle/background stretch, worse for bookmarked tabs than installed PWAs) produced silence but was reported as a success, so `playSound()` never fell through to the working `<audio>` fallback.
**Context:** Reproduced by code review (headless Chromium via Playwright showed zero errors — this is iOS-Safari-specific behavior, not reproducible in a desktop browser), reasoning from the exact mechanism: an async resume() promise not being awaited before the immediately-following synchronous `.start()` call.
**Implication:** Any function using Web Audio that claims success/failure to a caller expecting a reliable fallback must confirm `audioContext.state === 'running'` before declaring success — never assume a fire-and-forget `resume()` completed. This is a general pattern risk, not specific to this one function; check for it in any future Web Audio code in this project.

## 2026-08-25 — A symmetric bonus (both players get it identically) won't move AI-vs-AI win rate — check hung-parliament rate and seat-lead spread instead
**Finding:** Sweeping the new group-holding-bonus magnitude from 0→2 Cr/seat showed almost no change in win rate, seats, or hung-parliament rate — initially read as "the mechanic barely matters," until a frequency check showed it actually fires in 55% of games (avg 2.64 times/game). The real explanation: at that magnitude it was genuinely small relative to a player's ~₹27,500cr lifetime budget (~1-2%). Re-swept 2→6 Cr/seat and found win rate correctly stayed flat throughout (~52-53%) — as the user pointed out, this is *expected* for a symmetric bonus, not a sign it's inert — while hung-parliament rate (80.4%→78.7%) and seat-lead spread (avg lead +5%, blowout games nearly doubling 11→21 of 840) moved as the real signal.
**Context:** User corrected my initial read that flat win-rate meant "no effect" — a genuinely useful methodology correction for a mechanic that's symmetric between players rather than a possible source of relative advantage.
**Implication:** When balance-testing any future symmetric mechanic (applies equally to both players, doesn't favor one over the other), don't use win rate as the primary signal — it's structurally incapable of showing an effect. Use hung-parliament rate and seat-lead/blowout metrics instead, the way this session did once redirected.

## 2026-08-25 — Small-UT states tagged into big dominance groups, combined with the old investment curve's permanent floor, let a 1-seat state veto a much larger group's payout for near-zero ongoing cost
**Finding:** Regional dominance requires every state in a group at 50%+ (`dominanceActive`'s strict `.every()` in `mobile/engine.js`). Small UTs like Puducherry (1 seat) are deliberately tagged into big multi-state groups (e.g. `SouthIndia`, alongside Tamil Nadu/Kerala/Karnataka) so they can't be ignored. But the investment-boost curve (`investmentBoostBps`) decayed linearly for 20 taps, then locked at a permanent 200bps floor forever — so re-suppressing that one cheap state cost a flat 10Cr/tap indefinitely, no matter how many times it had already been fought over, letting it act as a near-free permanent veto on the whole group's much larger payout.
**Context:** User reported dumping "several thousand crores" into UTs without winning any state groups. Traced by reading `dominanceActive`/`investmentBoostBps`/`investmentCostCr` in `mobile/engine.js` and `mobile/game.js`'s AI investment-scoring (`scoreInvestState` = effective gain ÷ cost), then confirming via `states_data.json` that Puducherry/Goa/Lakshadweep are tagged into big groups (`SouthIndia`, `CoastalIndia`, `TravelAndTourism`).
**Implication:** Any future group-membership or investment-cost-formula change should re-check this interaction — a cheap group member combined with a flat, never-exhausting cost is the exploit shape to watch for. Also explains why the AI appears to "camp" on cheap UTs: `scoreInvestState` scores boost-per-crore with no exhaustion, so a state that's cheap to re-contest is the AI's best score every single tick, indefinitely — not deliberate denial logic, an emergent side effect of the greedy formula.

## 2026-08-25 — AI-vs-AI simulation confirmed the small-UT spam fix closes the gap without shifting overall balance
**Finding:** Full 1,260-game politician-matrix AI-vs-AI run (same seeds before/after, `boostDecayRate:1` reproduces the old permanent floor exactly, `0.85` is the shipped fix): UT-linked groups' win rate rose from 4.1% to 5.6% (closing the gap with UT-free groups' 5.7-6.4%), combined spend on the 7 smallest UTs fell ~71% per game, avg final seats rose (+8.3 p1 / +7.3 p2) as seats stuck with "Others" fell -15.6, hung-parliament rate fell 89.4%→82.1%, and overall p1 win rate was unchanged (51.9% vs 53.1%, within noise).
**Context:** Built two one-off Node scripts (scratchpad only, not committed) reusing `mobile/game.js`'s real AI harness (same pattern as `mobile/balance-sim.js`), comparing the old vs new `boostDecayRate` config value across the full politician roster.
**Implication:** These are the reference numbers/methodology for this fix — if further UT/dominance-balance work happens, compare against this baseline. Setting `boostDecayRate:1` in a cloned config is a clean, reusable way to reproduce "pre-fix" behavior for any future before/after simulation on this specific curve, without needing two code paths.

## 2026-08-24 — Offline play failed on bookmarked/typed URLs because the service worker's cache key only matched the filename form
**Finding:** `mobile/sw.js` precached the app shell under the key `./index.html`, which resolves (relative to the SW script's own location) to `.../mobile/index.html`. But the URL people actually bookmark — per the deploy README, `https://pradhanmantrielectionsgame.github.io/mobile/` — has no filename. Online, the server transparently resolves both URLs to the same content, so the mismatch was invisible. Offline, only the service worker answers, and the Cache API does exact-URL-match lookups with no server-style default-document resolution — so the bare-directory URL missed the cache entirely and the page failed to load, even though the shell was genuinely cached under the other URL.
**Context:** User reported the PWA "always hits the server" despite being built as offline-capable; traced by comparing what URL the deploy README tells people to bookmark against the literal cache key `CORE` builds.
**Implication:** Fixed by falling back to the cached `./index.html` for any failed navigation request (`e.request.mode === 'navigate'`), regardless of which URL form triggered it. Any future service-worker precache/fallback logic in this project must account for multiple possible navigation-request URL shapes (bare origin, trailing slash, explicit filename) rather than assuming the one used to build the cache key is the only one real users will hit.

## 2026-08-24 — iOS Safari's ITP caps script-writable storage (localStorage, Cache API) to ~7 days of inactivity for sites not installed to the home screen
**Finding:** A site opened as a plain Safari bookmark/tab (not "Add to Home Screen") on iOS is subject to Intelligent Tracking Prevention's storage cap — localStorage and Cache Storage can be purged after roughly a week of no user interaction. Sites running in standalone/home-screen-installed mode are exempt.
**Context:** Surfaced while discussing the offline-play fix (Cache API) and resurfaced when explaining a separate "lost my unlocked politicians" report (below) — relevant to both service-worker caching and the new localStorage-based unlock-progression feature.
**Implication:** Any future feature relying on localStorage/Cache API persistence should note this caveat when discussing reliability on iOS specifically, and the fix (if it ever matters enough to act on) is prompting users to Add to Home Screen rather than anything code-side — there's no way to opt out of ITP's cap from web code.

## 2026-08-24 — "Lost my unlocked politicians after updating" was localStorage's per-origin scoping, not a bug — confirmed by checking no tunnel process was still running
**Finding:** User tested the new unlock-progression feature across multiple URLs in the same dev session: a cloudflared quick-tunnel (`opinion-movers-petition-buy.trycloudflare.com`, gets a new random subdomain every restart), the LAN IP (`192.168.1.253:8934`), and potentially `localhost:8934`. `localStorage` is scoped to the exact origin (protocol+host+port) — these are three separate storage buckets to the browser even though they serve identical code. Confirmed the tunnel had actually died (`ps aux | grep cloudflared` returned nothing) rather than assuming; this ruled out "same tunnel URL, storage still cleared" as the explanation and pointed at origin-switching instead.
**Context:** User reported losing unlock progress after "updating the app"; user then confirmed they were testing on the local dev server (not the real deployed site) when asked directly.
**Implication:** For any future local dev testing of persistence-dependent features (this unlock system, or anything else using localStorage), stick to one stable URL for the whole test session — the LAN IP is more stable than a tunnel, since the tunnel's address changes on every restart. This entire failure mode doesn't exist on the real deployed site, where the origin never changes between updates.

## 2026-08-24 — Circular party-logo emblems (Congress, Trinamool) need more fill than non-circular ones to look correct inside a circular badge
**Finding:** `.pol-seal img` was sized at 62px inside a 100px circular ring for all party logos. BJP's lotus and AAP's broom (both drawn close-cropped to their own artwork bounding box) looked fine at that size, but Congress's hand-in-a-circle and Trinamool's flower-in-a-circle logos are themselves circular emblems with their own internal padding baked into the source SVG — shrinking an already-circular image inside another circle at 62% size produced an obvious "ring within a ring" gap that the non-circular logos didn't show.
**Context:** User reported "Congress and Mamata Banerjee logos don't fill their circle." Verified before/after visually via Playwright screenshots of all 4 distinct logo shapes in the roster (BJP, AAP, Congress, Trinamool), not just the two flagged ones, to confirm the fix (62px→88px) didn't crop the already-correct non-circular logos.
**Implication:** When sizing a party-logo image inside a circular frame in this codebase, circular-emblem-style logos need noticeably more fill than tightly-cropped non-circular ones to read the same — 88px was chosen to satisfy the worst case (circular logos) without clipping the tighter ones.

## 2026-08-24 — Party-symbol emoji fallback (✋ for Congress) was hardcoded in 4 in-game HUD/end-card spots, bypassing the real-logo-first fallback the select carousel already used
**Finding:** `partyBadge()` (used on the politician-select carousel) already preferred a politician's real `partyLogo` SVG and fell back to the `PARTY_SYMBOLS` emoji only on load failure. But `p1PartySymbol`/`p2PartySymbol`/`cardP1Symbol`/`cardP2Symbol` (in-game HUD top strip and the "vs" comparison bar) called `partySymbol()` directly via raw `.textContent =` assignments, always showing the emoji — so any of the 8 INC politicians showed ✋ in-game and on the end-of-game card even though `Indian_National_Congress_hand_logo.svg` exists and is correctly wired for all of them.
**Context:** User reported "in multiple places we use the ✋ emoji for Congress candidates even though we have the proper icon." Traced by grepping every call site of `partySymbol()`/`PARTY_SYMBOLS` rather than assuming the carousel's correct behavior extended everywhere.
**Implication:** Added `setPartySymbol(el, p)` in `mobile/main.js`, mirroring `partyBadge()`'s img-with-onerror-fallback logic but writing into an existing element instead of building a new wrapper. Any future spot that displays a party symbol must route through this helper, not a raw `PARTY_SYMBOLS` lookup — two code paths that look like the same mechanism but aren't is a recurring failure mode in this codebase (see the `.tutorial-locked` / `setTutorialMode()` finding from 2026-08-23).

## 2026-08-24 — Two README "Known Limitations" claims (missing portraits, undersized SVG viewBox) were stale and factually wrong on direct verification
**Finding:** "11 of 20 politician portraits missing" was false — a direct file-existence check against every `image` path in `data/politicians-data.json` showed 20/20 exist. "SVG map undersized, ~27% wasted viewBox" was also misleading — that claim is about `assets/icons/INDIA_V3_smaller_viewbox.svg`, referenced only by the deprecated legacy desktop `js/app.js`, not `mobile/index.html`'s real inline SVG (`viewBox="84 27 833 946"`), which a prior session (2026-07-27, recorded in CLAUDE.md) already confirmed via Playwright `getBBox()` is already tight — only ~2-6% margins on any edge, and a prior attempt to scale it up clipped the Northeast panhandle.
**Context:** User directly questioned the portrait claim ("All politicians have portraits. Maybe this is an old note?"); the SVG claim was checked proactively when the user asked to act on it, before touching any code.
**Implication:** Don't trust "Known Limitations"/status claims in README.md or CLAUDE.md as current without verifying against the actual files first — both had silently gone stale since they were written. Removed both bullets; the real remaining gap in this area is sound clips, not portraits: only 4/20 politicians (Modi, Bachchan, Rajinikanth, Tendulkar) have a dedicated power-activation `.mp3` in `sounds/`, the other 16 fall back to a generic fanfare.

## 2026-08-24 — A paired A/B simulation showed clean-sweep bonuses contribute a negligible ~3 Cr / ~0.03 seats on average — not the source of the AI feeling harder
**Finding:** Ran 40 full AI-vs-AI games (identical seeds and matchups, `cleanSweep.payoutCrPerSeat` at the real value of 5 Cr/seat vs. forced to 0) via a standalone script reusing `mobile/game.js`'s real `createGame`/`setupAI`/`runAIFull`. Average final-funds delta: ~3 Cr. Average final-seats delta: ~0.03. Against a ~30,000 Cr total-budget game, this is noise.
**Context:** User reported the game felt harder after clean-sweep bonuses were introduced (2026-08-17) and pushed back with sound cost-vs-reward skepticism on an initial hand-wavy hypothesis blaming clean sweep directly. Ran the real paired measurement instead of continuing to argue from the investment-cost formula alone.
**Implication:** `scoreInvestState` (the AI's investment-targeting function) never factors in the clean-sweep payout when choosing where to invest — the bonus is a passive side effect of states the AI was already going to max out for their seats, so removing it barely changes behavior or funds trajectory. Don't attribute a future "AI feels stronger" report to clean sweep without measuring first — the more likely real sources are the July AI-scoring efficiency fixes (headroom-awareness, per-seat normalization) and/or the 2026-08-21 agenda-pack rebalance, both of which predate clean sweep's introduction and actually changed how efficiently the AI plays.

## 2026-08-24 — The AI could snipe a single small UT (e.g. Puducherry) individually to cheaply deny regional dominance, while the human can only invest in that cluster as an all-or-nothing batch
**Finding:** `mobile/main.js`'s "Small UTs" button always invests in all 5 non-Delhi/Goa small UTs (Chandigarh, Dadra & Nagar Haveli/Daman & Diu, Puducherry, Lakshadweep, Andaman & Nicobar) at once — the human has no way to target just one. But `mobile/game.js`'s `pickAIInvestmentTarget`/`scoreInvestState`/`bestInPool` treat every state, including each small UT, as an individually scoreable target with no such restriction, since they simply iterate `game.states` (or a group-filtered pool of it). Direct simulation confirmed the AI could concentrate many taps into one small UT while leaving the others untouched.
**Context:** User reported the AI reliably maxing out Puducherry alone to block South India's group-dominance bonus, for far less than what the human has to spend batch-investing across all 5 to contest it back.
**Implication:** Added `SMALL_UT_BATCH_IDS` (the 5 minus Delhi/Goa, which do have their own dedicated buttons and stay individually investable for both sides) and a branch in `aiStep` that batch-invests across all 5 whenever the AI's best-scoring target lands on one of them — mirroring the human button exactly. Verified post-fix: AI taps land evenly (5-8 each) across all 5 batch states in a full simulated game, not concentrated in one. Any future asymmetry report between AI and human capability in this codebase is worth checking the same way — grep the AI's engine-level function for a restriction the UI-level human interface enforces, since the AI bypasses the DOM/button layer entirely.

## 2026-08-24 — Share result button reported broken by the user ("nothing at all" on tap) could not be reproduced despite testing the user's exact real-world scenario
**Finding:** Reproduced the user's setup as closely as headless testing allows — plain `http://<LAN-IP>:8934` (not localhost, not HTTPS), confirmed genuinely insecure context (`window.isSecureContext === false`, `navigator.share === undefined`) — in both headless Chromium and WebKit with the real iPhone 14 device profile (0.398 auto-zoom scale, matching real Safari's no-viewport-tag fallback). In both, `html2canvas` rendered `.declare-card` successfully and the fallback share overlay (`openShareOverlay`) opened with no errors.
**Context:** User reported the regression immediately after the v1.0.1 session's changes (touch-action, party-symbol refactor, nationwide-rally fanfare). None of that session's diff touches `shareResult()`/`buildShareCardBlob()`/`openShareOverlay()` — grepped and confirmed no shared code path.
**Implication:** Root cause not found. Most likely candidates, in order: a stale browser HTTP cache on the user's specific device (the dev server sets no `Cache-Control` headers at all, so a phone browser can hold onto an old `main.js` even though the HTML looks current — this project has hit exactly this class of "looks updated but isn't" bug before with the service worker), or a real-device/browser-specific quirk that neither Playwright engine reproduces. Advised the user to hard-refresh or try a private/incognito tab, and to report back the specific device/browser if it recurs — do not re-guess blindly if this comes up again; get real console output off the actual device (`chrome://inspect` or Safari Remote Web Inspector) instead.

## 2026-08-23 — The gap between the topstrip and Ladakh on the map is mostly occupied by `.player-strip`, leaving almost no free space for an overlay
**Finding:** Measured via Playwright `getBoundingClientRect()` on the iPhone 14 profile: the news-ticker bottom sits at ~161px, Ladakh's top edge at ~311px — a 150px band — but `.player-strip` (the funds/seats readout absolutely positioned at the top of `.map-wrap`) already occupies 175-291px of that same band, leaving only ~20px genuinely free above Ladakh. A toast/banner placed anywhere in that band will overlap either the ticker, `.player-strip`, or Ladakh; there is no position where all three stay clear at once for content taller than ~20px.
**Context:** Repositioning the toast notification to sit "above Ladakh and below the seat counter" per user request — first attempt (fixed pixel `top`) looked fine for short messages but the real clean-sweep-bonus message (which wraps to 2 lines) collided with both `.player-strip` and Ladakh.
**Implication:** Any future UI element meant to live in that same visual region (between the topstrip and the map's landmass) must either accept overlapping `.player-strip` or be anchored dynamically off a live measurement (as the toast now is, via `document.getElementById('INLA').getBoundingClientRect()`) rather than a fixed pixel guess — a static value cannot satisfy "clear of everything" there.

## 2026-08-23 — Delhi/Goa/Kerala's map elements are small but non-zero size — a rally-token dot rendered there was technically correct but invisible in practice
**Finding:** `renderRallyTokens()`'s existing zero-size skip (`if (!r.width || !r.height) return`) never triggered for `INDL`/`INGA`/`INKL` — their SVG shapes have a real (if tiny) bounding box, so the deployed-rally dot rendered exactly on the map at their true (hard-to-see/hard-to-tap) location, the same underlying problem their corner buttons exist to solve for taps.
**Context:** User reported no visible indicator after deploying a rally token via the new Delhi/Goa/Kerala buttons; a screenshot showed the dot actually was rendering, just camouflaged on the map itself rather than on the button the player was looking at.
**Implication:** For these three states specifically, any future on-map visual indicator (not just rally tokens) should default to anchoring off the corresponding button element (`SMALL_STATE_BTN_ID` in `mobile/main.js`) rather than checking for a zero-size map element as the trigger — the map element's size is not a reliable signal that it's a good place to put UI.

## 2026-08-23 — `setTutorialMode(false)` was an incomplete tutorial-state cleanup funnel — found broken twice in one session for two different leftover elements
**Finding:** After fixing the carousel freeze/pulse-play classes not being cleared on Play Again (`.tutorial-frozen`, `.tutorial-pulse-play` etc.), the same underlying gap resurfaced for a different element: `#tutorialCoach` (the coach banner, last showing "Click 'Play as Modi' to begin your campaign!") was also never hidden again after the tutorial's coaching phase ended, since `renderTutorialStep()` — the only code that ever sets its `hidden` state — only runs during the tutorial itself.
**Context:** User reported the banner still visible after Play Again, immediately after the carousel-freeze fix had shipped in the same session; both bugs share one root cause (state set once during the tutorial, never revisited by anything that turns tutorial mode off).
**Implication:** `setTutorialMode(false)` is the intended single funnel for all select-screen tutorial cleanup — confirmed twice now that anything `renderTutorialStep()`/`renderTutorialStageStep()` toggles and doesn't get explicitly un-toggled elsewhere will leak into the post-tutorial session. Before shipping any *new* tutorial-step visual (a class, a hidden flag, a highlight), check whether `setTutorialMode(false)`'s cleanup block needs a matching line for it.

## 2026-08-23 — A tutorial phase-gate that skips startPhaseTimer() leaves the new phase's clock at ~0, instantly re-triggering doEndPhase on resume
**Finding:** `checkTutorialPhaseGate()` (called from `doEndPhase` right after `G.endPhase(game)` bumps `game.phase`) short-circuited `startPhaseTimer()` entirely whenever it fired to re-pause and show a coach step — but `startPhaseTimer()` was also the only place `timeLeft` got reset to a full `phaseDurationSeconds`. With the gate skipping it, `timeLeft` stayed at the ~0 it had just hit ending the previous phase, so the next time the tutorial resumed the countdown (e.g. via `enterTutorialPhaseWait()`'s `resumePhaseTimer()`), the interval's very next tick drove `timeLeft` negative and called `doEndPhase()` again immediately — skipping the entire live phase the player was meant to get.
**Context:** User reported "the tutorial just jumps a phase from step 29 to step 30 without letting the player play out phase 2." Traced by re-reading `doEndPhase`/`startPhaseTimer`/`checkTutorialPhaseGate` together rather than each in isolation.
**Implication:** Split `startPhaseTimer()` into `resetPhaseTimer()` (clock + AI-pacing reset only) and the interval-starting part; `doEndPhase` now calls `resetPhaseTimer()` unconditionally on every phase transition, and only conditionally skips `resumePhaseTimer()` when a tutorial gate is about to pause. Any future "pause the game for a coaching moment" mechanism in this codebase must keep those two concerns (reset vs. start) separately callable — never let an early-return before the reset short-circuit it.

## 2026-08-23 — A tutorial step's token grant only fired if the step was entered through the phase-gate path, not a plain Next click
**Finding:** The nationwide-rally grant step (`grantNationwideTokens:true`) has no `waitForPhase` of its own — its phase-6 gate lives on the *preceding* step — so it's always reached via a plain `goTutorialStageStep()` Next click, which calls `enterTutorialStageStep()` directly and never touches `checkTutorialPhaseGate()`. The token-grant logic (`grantTutorialTokens(...)`) lived exclusively inside `checkTutorialPhaseGate()`, so it silently never ran for this step, even though the identically-shaped special-power grant step (phase 3) worked fine because *that* step carries its own `waitForPhase` and so is always entered through the gate.
**Context:** User reported "step 33 the player doesn't get awarded extra tokens to actually let them deploy the nationwide rally." Found by tracing which function actually executes for each of the two possible entry paths into a stage step, not by assuming both paths behaved the same.
**Implication:** Moved both token-grant calls (`grantSpecialTokens`, `grantNationwideTokens`) into `enterTutorialStageStep()` — the one function both entry paths (direct Next click, phase-gate resolution) always call — instead of the phase-gate resolver alone. Any future per-step side effect in this tutorial engine (grants, flag resets, highlight targeting) belongs in `enterTutorialStageStep()`, not in `checkTutorialPhaseGate()`, unless it's specifically meant to fire only on the gate-wait→resume transition.

## 2026-08-23 — #selectOverlay's `.tutorial-locked` class is only synced by the `setTutorialMode()` setter, not by the `tutorialMode` variable itself
**Finding:** `.tutorial-locked` (dims every non-Modi "Play as X" button to 40% opacity) and `.tutorial-modi-locked` (a *different* class, kept in sync automatically inside `renderTutorialStep()`) look like the same mechanism but are not — `.tutorial-locked` is only ever toggled by the `setTutorialMode(on)` function. `finishStageTutorial()` (and anything else that flips the `tutorialMode` variable directly rather than calling the setter) leaves `.tutorial-locked` stuck at whatever it was, so after a tutorial game ended and the player clicked "Play again," the select screen came back with every other politician still dimmed/unplayable even though `tutorialMode` itself was already `false`.
**Context:** User reported "the play again button should go back to the regular modi home screen without the tutorial mode." Confirmed via a headless Playwright check: `.tutorial-locked` was still `true` on the select overlay after Play Again, before the fix.
**Implication:** Any code path that resets tutorial state for a fresh game (Play Again, a future "skip tutorial" action, etc.) must call `setTutorialMode(false)`, not just check or set the `tutorialMode` variable — the variable and the DOM lockout class are two separate pieces of state that only the setter keeps in sync.

## 2026-08-22 — A flat 10,000cr tutorial funds grant stranded the player in ~1/3 of random seeds
**Finding:** Direct engine simulation (real `investCash`/`playRallyToken`/`tapAgenda` calls, not guessed math) of the full tutorial flow through "control the Eastern Border group" showed a flat ₹10,000cr bonus grant ran out mid-way in roughly 1 in 3 random RNG seeds tested — Uttar Pradesh (80 seats) and West Bengal (42 seats) are large enough that their per-tap cost (seats×10cr) exhausts funds before reaching the 50% dominance threshold, especially when starting popularity happens to be low.
**Context:** Building tutorial steps 19-24 (state-group dominance) exactly as specified ("give the player extra 10000 crores"); ran a real Node simulation against `mobile/game.js`/`mobile/engine.js` across multiple seeds before shipping, rather than trusting the number would obviously be enough.
**Implication:** Any future one-time "tutorial bonus" grant tied to a gameplay goal should be computed dynamically from the real cost (clone the target pop object, replay `investmentBoostBps`/`gainAt` off-model, sum the cost) rather than a flat guessed number — implemented as `estimateInvestCostToReach()` in `mobile/main.js`, generic over whichever state/group it's pointed at. Re-verified after switching the target group from Eastern Border to Western Border (user request, much cheaper: 5 states/70 seats vs 14/197) — succeeds across 7 seeds with grants of only 1,020-4,230cr.

## 2026-08-22 — "Hindi Heartland" is a state-group region tag, not an agenda — and Modi has no Hindi-Heartland-themed agenda
**Finding:** A tutorial step instruction named "Hindi Heartland" as an agenda to tap-to-completion, but no such agenda exists in `data/policy-tags.json`. `HindiHeartland` is a state tag (`data/states_data.json`) and a real group-chip key (`GROUP_META` in `mobile/game.js`, label "Hindi Heartland" 🕉️) used for regional-dominance grouping — a different mechanic entirely. Modi's actual 4 agendas are Infrastructure, Hindutva, Economic Liberalization, National Defense; none targets that region by name.
**Context:** Implementing a tutorial step gated on `agendaProgress[name] >= tapsToComplete` — checked the exact name against `data/policy-tags.json` before wiring the gate, since a name that never matches any real agenda would make the step permanently unsatisfiable. Flagged to the user via `AskUserQuestion`; they picked National Defense.
**Implication:** When a request names a game-content string (agenda, politician, state, group) that will drive a gameplay gate/condition, verify it against the real data files before implementing — a plausible-sounding but wrong name silently produces an impossible-to-satisfy condition instead of an obvious error.

## 2026-08-22 — mobile/engine.js exposes its API via `root.PMEEngine` (global assignment), not `module.exports` — Node scripts must read `global.PMEEngine` after requiring it
**Finding:** `require('./mobile/engine.js')` in a plain Node script returns `{}` — the file has no `module.exports` at all. It assigns `root.PMEEngine = {...}` onto `window`/`global` inside a `(function(root){...})(typeof window !== 'undefined' ? window : global)` wrapper, matching the plain-`<script>`-tag convention the rest of this project's browser code uses. `require()` still runs the file (and its self-check block, if `require.main === module`), so the side effect of populating `global.PMEEngine` does happen — the return value is just never useful.
**Context:** Writing a standalone Node verification script (outside `mobile/simulate.js`'s existing harness) to test a proposed funds-grant formula against the real redistribution math; `E.investmentBoostBps is not a function` on the naive `require()` return value led to checking the file's export pattern directly.
**Implication:** Any future one-off Node script that needs `mobile/engine.js`'s functions directly (rather than going through `mobile/game.js`, which already handles this correctly internally) must do `require(path)` for the side effect, then read `global.PMEEngine` — not destructure the `require()` return value. `mobile/simulate.js`'s existing pattern (`Game.loadGameDataSync`) already sidesteps this by not needing direct `E.*` access.

## 2026-08-22 — The fresh "mobile" deploy repo had not been pushed to since 2026-07-29, three weeks and 8 local commits stale
**Finding:** `git ls-remote --heads mobile main` showed the live deploy repo's `main` sitting at commit `1bb7314` ("verify GitHub Pages deployment live 2026-07-29"), while the local `deploy-mobile-fresh-repo` branch had moved 8 commits ahead — including the entire 2026-08-21 agenda-pack rebalance, the share-result feature, and this session's Kejriwal/Mamata changes. None of it had ever reached `pradhanmantrielectionsgame.github.io/mobile/`.
**Context:** User questioned why the deploy setup (a manual byte-copy of `mobile/index-redesign-a.html` to a root `index.html` plus a `<base href="mobile/">` tag) felt "weird"; comparing remote vs. local branch heads directly (rather than trusting the doc's description of the mechanism) surfaced the actual staleness.
**Implication:** A manual "must remember to re-copy and push" deploy step will eventually get forgotten — confirmed here, not hypothetical. The replacement, `scripts/deploy-mobile.js --push`, must be run explicitly after any `mobile/`-affecting change meant to go live; there is still no automatic trigger.

## 2026-08-22 — mobile/manifest-redesign-a.json's start_url pointed at a filename that would 404 on Add to Home Screen
**Finding:** The manifest's `start_url` was `./index-redesign-a.html`, and its `description` field literally said "separate manifest so Add to Home Screen doesn't point back at the real app's index.html" — both artifacts of when this file was a side-by-side comparison prototype, left unnoticed once redesign-a became the sole real build.
**Context:** Grepped every `index-redesign-a`/`manifest-redesign-a` reference across the repo before renaming the file to `mobile/index.html`, specifically to catch self-referential paths a simple `git mv` wouldn't touch.
**Implication:** When retiring/renaming a canonical file in this project, check JSON configs (manifests, service-worker precache lists) for filename strings embedded as data, not just `<script src>`/`<link href>` markup — those are easy to miss since they don't show up as a broken relative path until a user actually tries the affected flow (here, installing the PWA).

## 2026-08-21 — CSS grid containers with 1fr tracks inside a shrink-to-fit parent silently fail to hold a declared button size
**Finding:** Building the agenda tray's 2x2 grid layout, an initial `grid-template-columns:1fr 1fr` on an auto-width (shrink-to-fit) `.ne-actions`/`.action-block` container reproduced the exact CSS-grid pitfall already documented elsewhere in this project's CLAUDE.md — switching to explicit `100px 100px` column tracks fixed it immediately.
**Context:** Reshaping the AGENDA and RALLY trays from single-column stacks into 2x2 grids per user request.
**Implication:** Any future grid-based tray/panel in this codebase should use explicit px column tracks, not `1fr`, unless the grid container itself has a definite (non-auto) width.

## 2026-08-21 — The end-of-game declare-card overflowed the viewport specifically on a win, not on a loss or draw
**Finding:** Measured via Playwright at the iPhone-14 viewport: the win-only code path (which adds a winner-portrait block absent on loss/draw) pushed card content to 2048px against a 1668px viewport, cutting off the last stat rows and both footer buttons. Trimming spacing across the seal/portrait/parliament-chart/ledger/stats/footer brought it to 1533px, fully visible with no scrolling needed.
**Context:** User reported the win screen was "too big for the mobile view" right after a "Clean sweeps" stat row was added to the match-stats block.
**Implication:** Any future addition to the declare-card should be re-measured against real viewport height via Playwright before shipping, checking the win-only path specifically since it starts taller than draw/loss even before the new addition.

## 2026-08-21 — previewAgendaTapSeatDelta's seat math verified exact against real tapAgenda outcomes, including net-negative taps
**Finding:** A Node verification script comparing predicted vs actual `E.nationalSeats` delta after each real `tapAgenda` call matched exactly across 15 real taps, including two taps that netted -1 and -2 seats despite the agenda's own tags being nominally "supportive" for the tapping player.
**Context:** User reported that depending on which states you already dominate, tapping an agenda can lose net seats rather than gain them; built a real-time seat-swing preview and verified its accuracy before shipping it in the UI.
**Implication:** Confirms the reported bug is real (ownership caps + seat-apportionment rounding can make a nominally-positive agenda tap net-negative) and that the new preview correctly surfaces it to the player before they commit funds — safe to treat `previewAgendaTapSeatDelta` as ground truth for any future UI that wants a pre-commit projection of an agenda/investment action.

## 2026-08-21 — Three politicians shared an identical, mechanically-flat 2-tag pair before this session's agenda-pack rework
**Finding:** Mamata Banerjee, Jayalalithaa, and Hema Malini all carried the exact same pair {Women's Empowerment, Healthcare} — both flat-`nationwideBonus` tags with zero regional signature, so tapping either did the identical thing regardless of which of the three was being played. Found via a pairwise tag-overlap script across all 21 politicians' 4-agenda kits; 6 pairs total shared >=3 of 4 tags, all concentrated in the celebrity/glamour cluster.
**Context:** Requested analysis of "where is the agenda pack thematically weak" across the full roster, ahead of a broader thematic-distinctiveness pass.
**Implication:** When auditing or adding agenda kits going forward, flag any politician pair sharing 3+ of 4 tags as a priority fix, and treat flat-nationwide-bonus tags (no regional shape) as the least mechanically distinctive slot to check first — they're indistinguishable from each other regardless of theme.

## 2026-08-21 — 4 real region tags in states_data.json were almost entirely unused across the policy-tags.json pool
**Finding:** `CoastalIndia` (1 policy used it), `TravelAndTourism` (1, negative-only), `Pilgrimage` (3), and `NationalParksWildlife` (0 — completely unused) sat idle despite being real fields on every state record.
**Context:** Checked tag-usage frequency across the pool while designing new thematically-grounded agenda tags, to find open design space grounded in real (not invented) region data.
**Implication:** These are the region tags with the most remaining headroom for any future agenda-tag expansion — new tags leaning on them get real regional shape "for free" instead of competing for already-saturated tags like IndustrialCorridor/Manufacturing/AgriculturalRegion.

## 2026-08-21 — balance-sim.js's AI-vs-AI win rates systematically undervalue conditional/exploit-dependent special powers
**Finding:** Narendra Modi's Demonetization (pure denial — freezes the opponent's funds, no direct benefit to Modi) moved his win rate by ~0% across two independent single-lever changes (cost -5%→-2%; duration 1→2 phases). Adding a flat, unconditional +6% (then simplified to +4%) nationwide popularity benefit alongside the same freeze moved his win rate 8+ points immediately (43.5%→54%).
**Context:** User asked to boost Modi's bottom-tier win rate after a fresh `balance-sim.js` run; two lever attempts on the existing denial-only power both failed to move the number before the power's benefit *shape* was identified as the actual problem.
**Implication:** Any power whose payoff depends on the AI noticing and capitalizing on an opponent's disadvantage (timing, targeting) will likely show an artificially low win rate versus what a skilled human could extract from it — treat such win rates as a floor, not a ceiling, before nerfing. Recorded as a third methodology trap in `design/testing-and-balance-notes.md`; Yogi's Bulldozer Action and Nehru's Non-Alignment flagged as candidates worth re-examining under this lens later.

## 2026-08-21 — Chocolatey needs admin elevation on this machine; winget isn't installed; a portable binary download sidesteps both
**Finding:** `winget install` failed outright (command not found — winget isn't installed on this machine at all). `choco install cloudflared -y` failed with "Access to the path ... is denied" — Chocolatey itself is present but package installs require an elevated (admin) shell, which this session doesn't have and shouldn't grab without being asked. Downloading the standalone `cloudflared-windows-amd64.exe` directly from Cloudflare's GitHub releases (no installer, no admin) worked as a drop-in substitute for `cloudflared tunnel --url ...`.
**Context:** Needed an HTTPS tunnel to the local dev server so a phone could test a Web-Share-API feature that requires a secure context; the CLAUDE.md-documented cloudflared pattern assumed the binary was already available.
**Implication:** On this machine, prefer downloading a portable/standalone binary directly over winget or Chocolatey for any future small CLI tool need — both package managers hit friction here.

## 2026-08-21 — navigator.share's file-attachment capability requires a secure context; a LAN IP is not one
**Finding:** `navigator.share({files: [...]})` / `navigator.canShare({files: [...]})` silently report as unsupported (no error) over a plain `http://<LAN-IP>:8934` origin accessed from a phone — the Web Share API's file-sharing capability is gated on a secure context (HTTPS, or literally `localhost`), and a LAN IP doesn't qualify even though it's the same machine.
**Context:** Diagnosing why a newly-built image-attachment share feature couldn't be verified against the existing local dev-server + LAN testing setup used throughout this project.
**Implication:** Any future testing of Web-Share-API file-attachment behavior on a real device needs an actual HTTPS origin — the cloudflared tunnel pattern (portable-binary workaround above) or the deployed GitHub Pages site, not the plain-http LAN address alone.

## 2026-08-21 — iOS's Web Share API silently drops accompanying text when sharing to WhatsApp, regardless of payload shape
**Finding:** Calling `navigator.share({text: "...challenge line... https://..."})` on a real iOS device shared *only* the bare URL to WhatsApp, dropping the preceding challenge text — confirmed via real-device testing across two separate fix attempts (not reproducible in headless Chromium, where `navigator.share` is simply `undefined`). WebKit's share sheet appears to auto-detect the URL substring inside the shared text and hand WhatsApp's share extension a URL-only attachment; WhatsApp then discards everything else. Passing `text` and `url` as two separate fields (a commonly-suggested online workaround) did not fix it either.
**Context:** User reported "sharing functionality does not work well" on their phone; first fix attempt (dropping the separate `url` field) didn't resolve it, confirming the bug is in the OS/WhatsApp share-extension layer, not the navigator.share payload shape.
**Implication:** Never route this game's share text through `navigator.share`'s own text field for WhatsApp specifically. Use WhatsApp's own `https://wa.me/?text=` deep link instead (its own URL-scheme handler, not the generic OS share extension) — it reliably carries the full string. `mobile/main.js`'s `openShareOverlay()` now always uses per-app deep links rather than ever calling `navigator.share` for text-only sharing.

## 2026-07-29 — GitHub Pages deployment confirmed live at pradhanmantrielectionsgame.github.io/mobile/
**Finding:** The fresh-repo deploy (`github.com/pradhanmantrielectionsgame/mobile`, documented in the entry below) is confirmed live and playable — GitHub Pages is serving the site at `https://pradhanmantrielectionsgame.github.io/mobile/`.
**Context:** User reported this after clearing session context, needing a checkpoint since the prior session ended without one.
**Implication:** The "still needs confirming: whether GitHub Pages was actually enabled" open item from the prior deploy session is closed. Next open item on this deploy is still the deferred `js/`/`styles/` desktop-code cleanup noted in that same entry.

## 2026-07-29 — Resolved the second-GitHub-account push blocker from 2026-07-28; mobile now deploys from a fresh dedicated repo with a `<base href>` root copy instead of a redirect
**Finding:** The 2026-07-28 entry below diagnosed three failure modes pushing to a second GitHub account (`pradhanmantrielectionsgame`) from this machine: default cached creds authenticate as the wrong account, forcing the right username triggers an interactive OAuth prompt that hangs in this sandboxed environment, and a fine-grained PAT defaults to read-only. This session's fix sidestepped all three at once: generate a classic PAT scoped `repo` (read-write by construction, no fine-grained toggle to miss) on the `pradhanmantrielectionsgame` account, then store it directly and non-interactively via `printf '...' | git credential-manager-core store` (run by the user themselves via the session's `!` prefix, so the token never appeared in tool output — the previous attempt's PAT was exposed in plaintext via a `git remote -v` call and had to be treated as compromised). This never invokes the interactive/OAuth code path at all, so it can't hang. Separately, the user clarified the deploy target should be a brand-new repo (`github.com/pradhanmantrielectionsgame/mobile`, GitHub Pages from root) rather than grafting `mobile/` onto the existing `pradhanmantrielectionsgame.github.io` repo's `main` — avoiding the root-level-divergence risk documented in the 2026-07-27 entry below. Since Pages only serves a repo's root (or `/docs`), not an arbitrary subfolder, the new repo's root `index.html` needed to actually load `mobile/index-redesign-a.html`'s content; a plain meta-refresh redirect was the first draft but the user asked to avoid the extra hop. Fix: copy `index-redesign-a.html` byte-for-byte to root `index.html` and add exactly one `<base href="mobile/">` tag in `<head>` — this makes every existing relative reference (`script src="engine.js"`, `../assets/...`, `game.js`'s `basePath='../data/'`, `main.js`'s `../sounds/...`) resolve correctly with zero changes to `engine.js`/`game.js`/`main.js`/`sw.js`, since `<base>` retargets relative-URL resolution for the whole document (including `fetch()` calls made by scripts it loads) without needing every internal path rewritten.
**Context:** Direct continuation of the 2026-07-28 stuck session; user opened with "I think last we tried it we got stuck at using the personal access token."
**Implication:** Root `index.html` in the new `mobile` repo is now a manually-synced copy of `mobile/index-redesign-a.html` (plus the one `<base>` tag) — it must be manually re-copied after every future edit to `index-redesign-a.html`, or the live deploy silently drifts. Documented in `docs/wiki.html`'s Architecture section and `CLAUDE.md`. The `<base href>` trick is generally reusable any time a GitHub Pages (or similar root-only static host) deploy needs to serve a page that was authored assuming it lives one directory level deeper.

## 2026-07-29 — `mobile/index.html` is confirmed deprecated; `mobile/index-redesign-a.html` is the canonical build, and carries a Kerala button `index.html` never had
**Finding:** During a tutorial-planning session, a first plan draft referenced `mobile/index.html` and its quick-invest corner-button cluster (`#delhiBtn`/`#goaBtn`/`#neBtn`/`#utsBtn`), which the user corrected: `index.html` is deprecated, `mobile/index-redesign-a.html` ("Refined Booth Ink") is the real build, and it has a 5th button, `#keralaBtn` (Kerala's narrow coastline is hard to tap reliably on the map), that `index.html` never had. This confirms and upgrades the 2026-07-27 memory note that redesign-a "may become" the final UI — it now is.
**Context:** User caught the stale file reference while reviewing a draft "How to Play" tutorial plan built against the wrong file.
**Implication:** All future file/element references for the mobile UI should target `index-redesign-a.html`, not `index.html`. Two follow-ups are still outstanding: CLAUDE.md's "real, playable build" bullet still names `index.html` and needs updating; `mobile/sw.js`'s service-worker precache list should be checked against whichever file is canonical.

## 2026-07-29 — Tutorial mode's "must take a fixed, predictable path" requirement is already satisfiable with zero engine changes
**Finding:** `mobile/game.js`'s `createGame(data, p1PoliticianId, p2PoliticianId, rng)` (line 151) and `mobile/engine.js`'s `generateStartingPosition(..., rng)` (line 177) already accept an injectable RNG (`rng = rng || Math.random`), so passing a fixed seed plus fixed politician IDs reproduces an identical starting map/funds/tokens on every run.
**Context:** Researched while planning the "How to Play" tutorial's requirement (from the user) that the walkthrough always take the same fixed path, regardless of the game's normal randomized starting-position draw.
**Implication:** A future tutorial-mode implementation should reuse this existing RNG-injection point directly rather than building new determinism machinery.

## 2026-07-29 — `design/economy-status-map.md` was stale on multiple current facts despite being edited the same week for other sections
**Finding:** Cross-checking the design doc against actual code/data during a wiki refresh found three concrete drift points: (1) its "Hung parliament resolution" callout still describes the superseded ADR-0006 rule (draw vs. a human, loss vs. the AI) instead of ADR-0010's "always a draw," even though `mobile/game.js`'s `finalizeGame()` implemented ADR-0010 the same day the doc file was last touched for unrelated edits; (2) Public Sector's tag direction is still described as "a separate finding, not fixed... worth a human call," even though `data/policy-tags.json` shows the fix (industrial/manufacturing/tribal-lands now support, agricultural regions oppose) shipped 2026-07-26, matching CLAUDE.md's own decision record; (3) its Regional Dominance worked example still cites "a player's entire 12,500 Cr lifetime budget," the pre-2026-07-24 figure that a different section of the same document already says was corrected to 30,000 Cr.
**Context:** Cross-referencing `design/economy-status-map.md` against `data/policy-tags.json`, `mobile/game.js`, and CLAUDE.md while refreshing `docs/wiki.html` to the current codebase state.
**Implication:** The design doc was formally deprecated 2026-07-29 (banner added, `docs/wiki.html` declared authoritative in CLAUDE.md) rather than patched, since it had demonstrated it can drift out of sync even while under active edit. Any future "how does X work" question should check `docs/wiki.html` first; the design doc is kept only as a dated historical decision log.

## 2026-07-29 — Policy pool grew from 24 to 25 entries with no corresponding decision record
**Finding:** `data/policy-tags.json` now has 25 top-level keys, not 24 — a `Film and TV` entry exists (presumably added for the film-persona politicians, Rajinikanth and Sivaji Rao) with no CLAUDE.md decision entry or doc update anywhere recording when or why. Re-running `node recompute_policy_ranking.js` against current data also shows only 4 of 25 policies net-negative nationally now (Indigenous Rights, Hindi Language, Agricultural Reforms, Secularism) — down from 7 of 24 previously, since Public Sector, Caste Reservation, and Land Reforms all flipped positive after their 2026-07-26 region-tag fixes.
**Context:** Counting `Object.keys(policyTags)` directly and running the recompute script while auditing the wiki's Agenda section against current data.
**Implication:** Any future roster/agenda-count reference should say 25, not 24. If policy additions like this happen again without a recorded decision, they'll keep silently invalidating any doc's policy-count and ranking-table claims — worth a one-line CLAUDE.md note next time a policy is added.

## 2026-07-29 — All 21 politicians now have a resolving portrait image; the "11 of 20 missing" figure is stale
**Finding:** Directly checking every politician's `image` field against the filesystem (`fs.existsSync`, all 21 entries in `data/politicians-data.json`) shows every one resolves to a real PNG in `assets/images/`. CLAUDE.md and a code comment in `mobile/main.js`'s `setPortrait()` (line 35) both still say 11 of 20 portraits don't exist — stale since at least the 2026-07-26 commit that added 12 portraits in one batch (Amitabh Bachchan, Bhimrao Ambedkar, Hema Malini, Indira Gandhi, Jawaharlal Nehru, Jayalalithaa, Lal Bahadur Shastri, P.V. Narasimha Rao, Rajinikanth, Rajiv Gandhi, Sachin Tendulkar, Sardar Patel).
**Context:** Auditing the roster section against `assets/images/*.png` while refreshing `docs/wiki.html`.
**Implication:** The portrait-fallback code path (colored-initial circle in `setPortrait()`) currently has nothing left to fall back for. The `main.js` comment and CLAUDE.md's portrait-count bullet still need a direct correction — out of scope for the wiki-only task that found this, flagged for a future session.

## 2026-07-29 — Roster is 21 politicians, not 20, as several docs still assumed
**Finding:** `data/politicians-data.json` has 21 entries (`d.politicians.length === 21`), including `sivaji-rao` as a 21st, film-persona-only pick alongside the 20 usually cited. 9 of the 21 have Uttar Pradesh as a home state (not 10 of 20, the figure the pre-refresh wiki stated).
**Context:** Counting directly while updating the wiki's roster section.
**Implication:** Any current-state prose (not a historical description of an earlier roster size) should say 21 going forward — several CLAUDE.md bullets and the pre-refresh wiki still said 20.

## 2026-07-28 — Changing `game.winner` alone wasn't enough to make hung parliament read as a draw — the end screen had its own independent, redundant check
**Finding:** `mobile/main.js`'s `showEndOverlay()` computed its hung-parliament headline from `var vsAI = game.players.p2.isAI; headline = vsAI ? 'Hung parliament — you lose' : 'Hung parliament';` — it never actually read `game.winner` for this branch, it independently re-derived the same "AI wins ties" conclusion ADR-0006 encoded in `finalizeGame()`. Fixing `finalizeGame()` alone (setting `game.winner = 'draw'` unconditionally) would have left the end screen still showing "you lose" on every hung parliament, since this UI code path never consulted `game.winner` in the first place.
**Context:** Found while implementing ADR-0010 (hung parliament is always a draw, superseding ADR-0006) — a grep for `vsAI`/`isAI` in `main.js` turned up the duplicate logic before it shipped as a silent regression.
**Implication:** When changing what a `game.winner`/game-state outcome means, grep for every place that independently re-derives the same conclusion, not just where `game.winner` itself is read — this project has hit this "two places must agree" failure mode repeatedly (CLAUDE.md documents several: agenda effect text vs. real benefits/costs, the `[hidden]` CSS attribute rule, etc.) and this is another instance of the same pattern.

## 2026-07-28 — `balance-sim.js`'s reported win rates were almost entirely a measurement artifact: 76% of games hit a hung parliament, and the game's own tie-break rule always awarded it to p2
**Finding:** Checking the existing `mobile/balance-log.jsonl`, 1,595/2,100 games (76.0%) ended in a hung parliament (neither side reached the 272-seat majority). The real game's own rule (`finalizeGame()`, ADR-0006) resolves a hung parliament as `game.winner = game.players.p2.isAI ? 'p2' : 'draw'` — correct for a human-vs-AI match, but in every simulated game p2 was always the AI slot, so p1 (whichever politician was being measured) structurally lost 76% of games by rule alone, regardless of kit quality. This was a far bigger driver of the near-0% win rates across most of the roster than the naive-p1-vs-full-AI-p2 strategy gap that was initially suspected.
**Context:** User asked "how can the win rate for so many be 0 percent" after a first re-ranking by win rate (not avg seats, per user's correction) showed 13 of 21 politicians at 0-3%.
**Implication:** Any future win-rate measurement from this simulator must either (a) treat hung parliament as a neutral outcome (this session scored it 0.5/0.5 in `balance-sim.js`'s own tally, without touching the real game's ADR-0006 rule) or (b) explicitly account for its ~76-95%-per-politician frequency before drawing conclusions from a raw win/loss count.

## 2026-07-28 — `mobile/game.js`'s AI logic (`aiStep`/`runAIFull` and their scoring helpers) was hardcoded to player key 'p2' throughout, not just at the top level
**Finding:** Beyond the previously-known `aiStep`/`runAIFull` hardcoding, the helper functions they call — `pickAIPowerTarget`, `pickAICompletedAgenda`, `groupFocusBonus`, `scoreInvestState`, `bestInPool`, `pickAIInvestmentTarget` — all had 'p1'/'p2' literals baked into their logic (e.g. `scoreInvestState`'s tie-break term was hardcoded `game.pop[s.svgId].p1 - game.pop[s.svgId].p2`, assuming the actor is always p2 and the opponent always p1). All were generalized to take explicit `playerKey`/`oppKey` parameters, with `aiStep`/`runAIFull` defaulting `playerKey` to `'p2'` so every existing call site (`main.js`, `simulate.js`, which never pass a second argument) behaves identically to before.
**Context:** Needed to let `mobile/balance-sim.js` drive p1 with the real AI too (a symmetric AI-vs-AI match), rather than the naive/random strategy it used previously — the project's own prior finding had flagged the object-swap approach as unsafe but hadn't yet built the proper generalized version.
**Implication:** A new `Game.setupAI(game, playerKey, rng)` export now flags an arbitrary seat as AI-controlled (profile + target group), matching what `createGame` already did for p2. Any future headless test needing a second AI-controlled seat should use this rather than reinventing naive-strategy stand-ins.

## 2026-07-28 — Once measured correctly, 4 of 21 politicians (the 3 official celebrities plus "Sivaji Rao") had both an unusually strong agenda kit AND an unusually strong/free special power stacked together
**Finding:** Amitabh Bachchan, Hema Malini, Rajinikanth, and Sivaji Rao all had agenda-kit national averages (seat-weighted avg pts per full tap, summed across their 4 agendas) of 13-16, among the highest in the 21-politician roster, on top of their already-strong special powers. Sivaji Rao's "One-Day Ordinance" (`data/politicians-data.json`) had literally zero cost of any kind while instantly setting a 48-seat state (Maharashtra) to 100% popularity — the single most broken power in the roster.
**Context:** User clarified that their original "celebrities should be OP" intent meant the special power specifically (since these entries "don't have the traditional politician appeal thematically"), not the agenda kit — the double-stacking was an unintentional tuning error from an earlier session, not the design intent.
**Implication:** Rebalancing an over/underperforming politician should first check whether the agenda kit and the special power are both pulling in the same direction before assuming either one alone is the fix — and for personas whose agenda kit is already a deliberate thematic match (confirmed true for all 4 here after a reverted first attempt to swap agendas), the special power's cost/timing/magnitude is the correct lever, not the agenda kit.

## 2026-07-28 — A power-magnitude nerf can appear to backfire when compared across simulation batches where other politicians' kits also changed ("strength of schedule" confound)
**Finding:** Cutting Hema Malini's power from 8%/16% (nationwide/home) to 2%/16% appeared to raise her measured win rate from 60.0% to 64.5% in a batch where 2-3 of her 20 simulated opponents (Sivaji Rao, Rajinikanth) were also weakened in the same run. Re-testing with her power alone toggled, opponent pool held exactly constant, showed the true effect was a genuine -13-point drop (77.5% with the old power → 64.5% with the new one) — the batch-level comparison had been confounded by her opponents getting weaker at the same time.
**Context:** User pushed back that the result "makes no sense" for a change that was strictly weaker in every state except an unchanged home bonus.
**Implication:** Any future single-politician balance change should be verified with an isolated test (only that politician's data toggled, everyone else held at the current committed state) before trusting a before/after comparison across two full batch runs, since `balance-sim.js`'s win rate is relative to the whole 20-opponent pool, not an absolute measure of one kit.

## 2026-07-28 — A magnitude-only nerf hits diminishing returns; breaking through required combining it with a much later `requiresMinPhase` gate in the same edit
**Finding:** Cutting Amitabh Bachchan's "Celebrity Endorsement" nationwide magnitude from 12%→10% dropped his win rate 78%→71% (-7pts), but the next equal-sized cut (10%→8%) only moved it 71%→67.5% (-3.5pts) — diminishing returns on the same lever. An earlier attempt to fix him via cost+timing alone (₹1,000cr→₹2,000cr + `requiresMinPhase: 3`, magnitude unchanged at 12%) moved his win rate by exactly 0%. The combination that finally broke through was magnitude 8%→6% **together with** pushing `requiresMinPhase` from 3 to 6 in the same edit, dropping him 67.5%→62%.
**Context:** User's target was closing the gap between Bachchan and the rest of the top cluster (Rajinikanth 58.5%, Hema Malini 60%) without eliminating his "hardest to beat" status entirely.
**Implication:** For this politician's specific power shape (nationwide, "Others"-sourced, no per-region interaction), a single lever pushed further has diminishing effect — a small early-gate `requiresMinPhase` value (3, i.e. usable almost immediately) contributes far less than a late-game one (6, roughly the midpoint of the 10-phase match) since it cuts off several extra phases of compounding benefit. Test cost/timing/magnitude changes in combination, not just escalating one lever repeatedly, if an earlier single-lever version underperformed expectations.

## 2026-07-28 — The existing single-player engine architecture is already well-shaped for real-time human-vs-human networking, with almost no engine changes needed
**Finding:** `mobile/game.js`'s action functions (`investCash`, `playRallyToken`, `tapAgenda`, `activatePower`, etc.) are already pure, side-effect-free, playerKey-agnostic mutators — `(game, playerKey, ...args) → {ok, reason?}`, no DOM/sound/console access. The AI opponent (`aiStep`) is not a separate code path; it calls these exact same functions with `playerKey='p2'`. Separately, `mobile/engine.js`'s `resolveSimultaneousGain` already exists specifically to reconcile two players' actions landing on the same state in the same phase — the engine already assumes concurrent two-sided play, it has just never been fed by a second live human player.
**Context:** User asked how to extend the game to real-time 2-player-over-internet play. A research pass over `game.js`/`main.js`/`engine.js` (plus pre-existing ADR-0001/0002/0007 on deferred live matchmaking) found these properties already in place rather than needing to be designed from scratch.
**Implication:** A networked multiplayer layer can be built as an additive `mobile/net.js` module that publishes/replays actions through the existing `G.*` functions (an event-sourced action log), without modifying `game.js`/`engine.js` internals. Full architecture written to `design/multiplayer-implementation-plan.md`. The one real gap is determinism: `game.rng` is currently seeded with unshareable `Math.random` and needs a swappable seeded PRNG for two clients to reach identical starting positions.

## 2026-07-28 — Starting-position seat variance: the stopping-rule threshold controls the mean without widening the spread
**Finding:** `mobile/engine.js`'s `generateStartingPosition()` (pre-fix) produced starting national seats averaging ~135-142 with a right tail reaching 180-200+ for one player — confirmed via 3,000-8,000-sample direct simulation, with ~46-47.5% of games putting at least one player over 150 seats (not a rare tail, close to a coin flip). Raising the advantage-draw stopping threshold (`drawStopAt`/`drawBudget`, 100/130 → 124/154) shifted the mean up to ~150 while leaving the standard deviation essentially unchanged (~12.4-12.7 across the whole tested range) — because the stopping rule is self-correcting: a player who draws a run of small states just needs more draws to cross the threshold, which keeps the overshoot (and therefore σ) stable regardless of where the threshold sits.
**Context:** User reported seeing 180-200-seat starts in actual play and initially proposed narrowing the baseline population range (500-2900 bps) as a fix; a parameter sweep showed narrowing the baseline instead just drags the average down without independently controlling the tail. Direct mean/σ measurement (not inferred from percentiles) was needed to find that the advantage-draw threshold is the one knob that moves the mean without stretching the spread, letting a plain reject-and-retry hard cap (tried first) be replaced with pure parameter tuning once the user's actual target — mean 150, σ~12.5 (175/125 as 2σ events) — was made precise.
**Implication:** Any future tuning of this generator should be checked via direct mean/σ simulation, not intuition about which knob "should" raise or lower the total or its spread — several knobs (`baseHi`, `drawStopAt`, `advHi`) all move the mean, but only empirical measurement revealed which one leaves σ untouched.

## 2026-07-28 — Rahul Gandhi, Mamata Banerjee, and Manmohan Singh are empirically the weakest politicians in the 21-politician roster, each for a different structural reason
**Finding:** A 1,260-game batch simulation (`mobile/balance-sim.js`, every politician as p1 with a fixed naive strategy vs. every other politician's real AI as p2, 3 seeds/ordered pair) showed Mamata Banerjee (avg final seats 170.7) and Rahul Gandhi (172.5) as the two worst performers in the roster, with Manmohan Singh (196.5) also bottom-tier — versus e.g. Hema Malini's 261.7. Root causes differ per politician: Manmohan Singh's own special power (Economic Reform) costs -8% popularity in AgriculturalRegion states, which includes his own home state Punjab — a direct self-inflicted tax on his own base, compounded by 3 of his 4 agendas being net-negative on Punjab's own tags. Rahul Gandhi's 4-agenda kit nets slightly negative (-8bps) on his own home state Uttar Pradesh, driven mainly by Secularism's -12/-12 HindiHeartland/Pilgrimage penalty landing on his own turf; UP is also shared as "home" by 8-9 of 20 politicians, so the home-bonus-nullification rule triggers against him unusually often. Mamata Banerjee's home-state popularity is actually healthy (~66% at game end, uncontested West Bengal) but 3 of her 4 agendas (State's Rights, Secularism, Agricultural Reforms) are net-negative across Hindi Heartland/Industrial Corridor states — the country's largest seat blocs — with no offsetting positive-nationwide agenda in her kit.
**Context:** User asked for a thought experiment on why certain politicians felt harder to win with, based on a small manual-play sample. Investigated by hand-computing tag-effect arithmetic per politician's home state against `data/policy-tags.json`/`data/states_data.json`, then building `mobile/balance-sim.js` to get a real empirical sample rather than relying on the small manual sample alone.
**Implication:** `mobile/balance-sim.js` is now available as a repeatable tool for future politician-balance work — rerun after any politician/agenda tweak (`node mobile/balance-sim.js 5` or higher for more confidence) rather than relying on manual small-sample play. No politician rebalance was implemented this session, only the measurement tooling and the diagnosis.

## 2026-07-28 — Modi's Demonetization froze the opponent's funds a full phase late — an off-by-one that directly contradicted the design doc's own explicit ban on delayed-trigger power effects
**Finding:** `mobile/game.js`'s `activatePower()` set `who4.fundsFrozenUntilPhase = game.phase + 1` when applying the `freezeFunds` benefit, so `fundsFrozen(pl, game)` (`pl.fundsFrozenUntilPhase === game.phase`) stayed false for the entire phase the power was activated in — the AI kept investing/tapping normally right after the user used it, only actually freezing the *following* phase. `design/economy-status-map.md` (revised 2026-07-26, the same day Demonetization shipped) explicitly bans this exact pattern: "resolves on a delay (e.g. 'triggers at the start of the next phase')" is called out as banned, with one-phase effects required to "self-clear the moment `game.phase` moves past it" — i.e. apply immediately.
**Context:** User reported "the AI can still click around and I can visually see the funds being deducted for each click" right after activating the power. A diagnostic script (`Game.createGame` + directly calling `investCash`/`tapAgenda`/`aiStep` on the real `mobile/game.js`/`engine.js`) proved the mechanic's gating logic itself was correct in isolation (blocks funds spend exactly during the flagged phase) — the bug was which phase got flagged.
**Implication:** Fixed by setting `fundsFrozenUntilPhase = game.phase` (not `+1`) — the freeze now lands the instant the power activates and expires automatically once the phase advances. Any future duration-based special power in this project must set its expiry field to the *current* `game.phase`, never `+1` — check this specifically against `design/economy-status-map.md`'s explicit delayed-trigger ban whenever a new one-phase-duration power is added.

## 2026-07-28 — Only 6 of 20 politicians have a dedicated power-sound file; the missing-file fallback silently undid background-music ducking for the other 14
**Finding:** `playPowerSound()` (`mobile/main.js`) ducks `bg_music` to volume 0 before playing a politician's `sounds/<Name>.mp3` clip, but its `.catch()` fallback (for the 14/20 politicians with no dedicated file) reset `bg_music.volume` back to full *before* playing the generic `fanfare` sound — undoing the duck right when the fallback needed it most, so fanfare played over full-volume music. Separately, even for politicians *with* a dedicated file (e.g. Rajinikanth), the clip itself was mastered too quietly to hear clearly even with music fully muted — `HTMLAudioElement.volume` caps at 1.0, leaving no headroom to compensate.
**Context:** User reported special-power audio "drowned out by background music" for a politician without a dedicated file, then reported the same for Rajinikanth (who has one) after the fallback fix — the ducking mechanism was confirmed working correctly via design-doc/code cross-check; the actual issue was the clip's own quiet mastering.
**Implication:** Fixed the fallback to keep the duck applied through the fanfare fallback too (restore only fires once whichever sound actually finishes). Added a shared Web Audio `GainNode` (2.5x boost, `mobile/main.js`) on all power-sound playback to push past the `.volume` ceiling for quiet clips — one flat boost across all clips since there's no way to listen and calibrate per-file in this environment; revisit if a specific clip starts sounding distorted/clipped.

## 2026-07-28 — `mobile/index.html`, `index-redesign-a.html`, `index-redesign-c.html` each duplicate their own `<style>` block; CSS added to one doesn't reach the others
**Finding:** All three HTML files load the same shared `engine.js`/`game.js`/`main.js`, but each has its own fully-duplicated `<style>` block rather than a shared stylesheet. Adding CSS for a new JS-driven visual (a special-power activation animation, `.power-burst`) to only `mobile/index.html` left it completely invisible on `index-redesign-a.html`/`index-redesign-c.html` — the shared JS still ran and created the DOM nodes exactly as intended, just with zero matching styles, so nothing rendered and no error was thrown anywhere.
**Context:** User reported "I don't know if you applied any change... I didn't see any flashy animation" twice in a row. A headless Playwright check of `mobile/index.html` showed the animation rendering correctly, which was the tell that the user must be testing a different HTML file — they confirmed they actually play via `index-redesign-a.html`.
**Implication:** Any future CSS-only change to support a shared JS behavior (a new fx/animation, a newly-toggled-visible element, etc.) must be applied to all three HTML files' `<style>` blocks, not just `mobile/index.html` — this is the same "shared JS, unshared markup" trap already documented for `mobile/manifest.json`'s `start_url` in CLAUDE.md, just hitting `<style>` blocks instead of manifest links this time.

## 2026-07-28 — Rajinikanth's power-sound file was misnamed, silently falling back to generic fanfare
**Finding:** `sounds/Rajinikaanth.mp3` (extra "a") didn't match `playPowerSound()`'s key derivation from the politician's actual name "Rajinikanth" (`mobile/main.js`, spaces→underscores + `.mp3`), so it never played — the code fell back to the generic fanfare sound with no error. Newly-added Modi and Sachin Tendulkar sound files matched the convention exactly and needed no fix.
**Context:** Wiring up three newly-added politician sound files (Modi, Sachin Tendulkar, Rajinikanth) requested by the user; checked each filename against the politician's exact `data/politicians-data.json` `name` field.
**Implication:** Any future per-politician sound clip must be named exactly `<Politician Name with spaces→underscores>.mp3` — verify this whenever a new politician voice/power sound file is added, since a mismatch fails silently (falls back to fanfare) rather than erroring.

## 2026-07-28 — Pushing to a second GitHub account (`pradhanmantrielectionsgame`) from this machine fails at every auth path tried so far
**Finding:** Publishing the mobile build to a new dedicated repo (`pradhanmantrielectionsgame/pradhanmantri-mobile`, chosen instead of grafting `mobile/` onto the existing `main`) hit auth failures at every step: (1) default cached git credentials authenticate as the user's main `samitwatve` account, which gets `403` on the second account's repo; (2) forcing the `pradhanmantrielectionsgame` username triggers an interactive browser OAuth prompt that hangs in this sandboxed environment ("Cannot prompt because user interactivity has been disabled"); (3) a Personal Access Token generated under `pradhanmantrielectionsgame` and embedded in the remote URL authenticated correctly (fetch/read succeeded) but was still denied on push — consistent with a fine-grained PAT left at its default read-only Contents permission instead of read-and-write.
**Context:** Multi-turn attempt (this session) to stand up a standalone repo for GitHub Pages hosting of `mobile/`, separate from the live desktop-game Pages site on `main`. Paused mid-troubleshooting at the user's request ("we'll try again tomorrow").
**Implication:** Next attempt needs a token with explicit read-and-write Contents permission (fine-grained) or classic `repo` scope, generated and used from the user's own interactive terminal — this sandboxed environment cannot complete interactive GitHub auth at all. Also: the PAT used in this session was exposed in plaintext (printed via `git remote -v` in tool output) and should be treated as compromised — revoke and regenerate rather than reuse.

## 2026-07-27 — This app's own established Playwright viewport convention (980×2130) only matches real Safari with browser chrome hidden; a normal tab gives ~462px less usable height, hiding real overflow bugs
**Finding:** The project's documented headless-testing convention for this no-`<meta viewport>` app is `viewport:{width:980, height:~2130}`. That figure matches real iOS Safari only when the address bar/toolbar are NOT visible (home-screen-installed or scrolled-to-hide). A separate, already-documented finding says real Safari with chrome visible gives ~664px usable CSS height, not 844px — scaled into this app's ~980px-wide fallback canvas (980/390×664), that's ~1669px, not ~2130px. Testing `index-redesign-a.html`'s politician-select cards at 980×2130 showed 0px overflow for all 21 politicians; re-testing at 980×1669 (the real worst case) showed every card overflowing 187-327px, with Nehru/Ambedkar/Modi/Indira Gandhi worst — exactly matching a user bug report.
**Context:** User reported Modi, (Indira) Gandhi, and Nehru's politician-select cards were visibly cut off on their phone, despite an initial self-check claiming a clean fit.
**Implication:** Any future headless "does this fit / does this overflow" self-check of this app's full-screen overlays must test at BOTH the generous ~2130px canvas height AND the real chrome-visible ~1669px height — the smaller one is the actual worst case for anyone using a normal (non-installed) Safari tab, and the 2130px figure alone can hide real, user-visible overflow bugs.

## 2026-07-27 — `object-fit:cover` has no single crop anchor that works across a set of illustrations with varying composition; `object-position` tuning that fixes one image breaks another
**Finding:** Fixing the politician-card overflow above required shrinking the (previously fixed-height) portrait region to a flexible, content-driven size. With `object-fit:cover` and default centered crop, this cut off Nehru's cap (subject positioned high in frame). Switching to `object-position:50% 0%` (top-anchored) fixed Nehru but then cropped Modi's face out entirely, since Modi's illustration has his face lower with a raised fist above it — different politicians' portraits have different subject placement, so no single crop anchor serves all of them.
**Context:** First attempt (object-position:top) was shown to the user as fixed; user immediately flagged it still looked broken, pointing at a different politician's card.
**Implication:** For any image set with inconsistent internal composition (not just this politician roster — any future art batch), don't try to find one `object-position` value that works for all of them. Switch to `object-fit:contain` instead (already the documented project rule for "cropping might cut off a face" per CLAUDE.md) — it guarantees the full illustration is always visible regardless of container size, at the cost of some letterboxing.

## 2026-07-27 — `mobile/manifest.json`'s `start_url` hijacks Add-to-Home-Screen for every page under its scope, not just the one it was added from
**Finding:** iOS Safari's "Add to Home Screen" honors the linked web-app manifest's `start_url` over the page actually being viewed — any page under `mobile/` that links `manifest.json` gets a home-screen icon that always launches `start_url`'s page (`./index.html`), regardless of which prototype page the user was on when they tapped Add to Home Screen.
**Context:** User reported Safari kept reverting to the old build even after saving a new redesign prototype (`mobile/index-redesign-a.html`) to the home screen.
**Implication:** Any future prototype/experiment page under `mobile/` needs its own dedicated manifest file (e.g. `manifest-redesign-a.json`, `manifest-redesign-c.json`, both added this session) linked via its own `<link rel="manifest">`, or it will silently keep launching whatever the shared manifest's `start_url` points to.

## 2026-07-27 — India's map SVG viewBox in `mobile/index.html` already tightly bounds the landmass (~2-6% margin); the visible NE/SE "dead space" around the map is inherent to India's silhouette, not fixable by scale/transform tuning
**Finding:** Measured directly via Playwright (`document.getElementById('features').getBBox()` against the `viewBox="84 27 833 946"`): actual landmass margins are only 15.9px left / 16.9px right / 18.5px top / 55px bottom out of 833×946 — the SVG itself wastes almost no space. The large empty gray regions visible northeast and southeast of the landmass in the rendered HUD are ocean space within the landmass's own bounding rectangle (India's shape is roughly triangular, not rectangular), not viewBox letterboxing.
**Context:** Investigating visible dead space after reclaiming sidebar/bar screen real estate for the map; a first attempt to fix it by increasing `transform:scale()` on `.india-map` (1.03→1.35 with an offset `transform-origin`) instead clipped the Northeast panhandle off-screen, since scaling up a shape that already fills its box just enlarges the same triangular gaps proportionally.
**Implication:** Don't try to "zoom out" this specific problem again — treat the NE/SE (and to a lesser extent SW) dead zones as permanent geography, and design UI to deliberately occupy them (floating panels/strips) rather than trying to make the map itself expand to fill a rectangle it geometrically can't.

## 2026-07-27 — Headless Chromium screenshots of `mobile/index.html`-family files must use the app's authored ~980px canvas width, not a real device viewport, to mean anything
**Finding:** A Playwright screenshot at a real device viewport (390×844) showed severely oversized, overlapping UI (funds/seats text mashed together, huge icons) — because headless Chromium, unlike real mobile Safari, does not apply the automatic ~0.4x zoom-out this app relies on for its already-documented no-`<meta viewport>` convention. Re-shooting at `viewport:{width:980, height:2130}` (matching the app's own authored canvas) produced layouts that visually matched the user's real-device screenshots.
**Context:** First self-verification attempt this session, checking a redesign change before showing it to the user, came out looking broken until the viewport was corrected.
**Implication:** For any future headless Playwright check of `mobile/index.html` or its `index-redesign-*.html` siblings, screenshot and measure (`getBoundingClientRect()`) at `viewport:{width:980, height:~2130}`, never a real device's literal CSS pixel size. This self-verification approach (screenshot + exact `getBoundingClientRect()` measurements before showing the user) caught several real bugs this session — a flex `order` mistake putting the groups sidebar on the wrong side, and two cases of a floating panel sized far larger than its actual button content — before they reached the user.

## 2026-07-27 — cloudflared is not installed on this machine, and neither is winget or choco
**Finding:** `cloudflared`, `winget`, and `choco` all returned "not found" / "command not recognized" when checked. The official Windows binary (`cloudflared-windows-amd64.exe`) was downloaded directly from `github.com/cloudflare/cloudflared/releases/latest` into the session's scratchpad directory and run from there successfully for phone-testing tunnels.
**Context:** Needed a public tunnel to test mobile redesign prototypes on a phone without the LAN/Windows-network-category path (already documented in an earlier finding).
**Implication:** The downloaded binary lives only in this session's temp scratchpad and will not persist to a future session — any future session needing `cloudflared` on this machine will need to re-download it (or the user installs it permanently at a durable path) rather than assuming it's still available.

## 2026-07-27 — `origin` is a live GitHub Pages site serving `main` directly; `mobile-ui-overhaul` had diverged from `main` at the root level, not just additively
**Finding:** The repo's remote (`pradhanmantrielectionsgame.github.io`) is a live GitHub Pages deployment with no build workflow (`.github/workflows/` is empty) — it serves whatever's on `main`'s root directly. `mobile-ui-overhaul` was 72 commits ahead of `main` with 0 commits behind (a clean fast-forward), which initially looked purely additive. `git diff main mobile-ui-overhaul --stat` on root-level paths told a different story: `index.html` and all 45 files under `js/` had diverged substantially — many old desktop files deleted, replaced with new implementations — not just `mobile/` being added alongside the untouched desktop game.
**Context:** User wanted to test the mobile build on their phone from a different WiFi network. A fast-forward merge of `mobile-ui-overhaul` into `main` plus a push was the initial plan (recommended as low-risk); the user paused it ("We've diverged substantially from it"), which prompted the root-level diff check before proceeding.
**Implication:** Never assume a long-diverged feature branch is purely additive from the ahead/behind commit count alone — check root-level file diffs before merging into `main` on this repo, since `main`'s root is a live public deploy, not a staging area. Used a temporary Cloudflare quick tunnel (`cloudflared tunnel --url http://localhost:8934`) pointed at the local `npm run serve` instead, for remote phone-testing without touching GitHub/Pages at all.

## 2026-07-27 — iOS Safari blocks `.play()` per-`<audio>`-element until that specific element has played inside a real user gesture at least once
**Finding:** `game_over`, `cash_added`, and `phase_reset` (`mobile/main.js`) are only ever triggered from a `setInterval` callback (`doEndPhase`), never a tap — so on strict mobile browsers all three were silently blocked while every gesture-triggered sound (agenda taps, rally deploys, special-power activation) worked fine. This is a per-`<audio>`-element restriction, not a per-page one — unlocking one element via a gesture doesn't unlock others.
**Context:** User reported the end-of-game sound didn't play during a real-device playtest (via a Cloudflare tunnel to the local dev server). An earlier fix that session had already resolved a fanfare/game_over ordering bug, which turned out to be a different, real bug — this was a second, separate cause.
**Implication:** Fixed via `unlockSounds()` (`mobile/main.js`), called from the welcome screen's "Begin Campaign" click handler — the first real gesture in the app — which plays-then-pauses every `Audio` element up front. Any future sound that only ever fires from a non-gesture context (a timer, an AI turn) must be added to that same unlock pass or it will silently never play on iOS.

## 2026-07-27 — The project's ~2.5x UI-scale convention (CLAUDE.md) wasn't applied everywhere on the first pass — settings overlay was still at real-world scale
**Finding:** `.settings-row`/`.settings-help` (`mobile/index.html`) were still authored at literal real-world sizes (15px/13px font, 14px padding) — the same "text too small" failure mode already documented for the ballot-card CSS on 2026-07-24, just in a different, untouched region of the file. The undersized padding/gap also made the overlay look sparse ("ton of whitespace") independent of the font-size issue.
**Context:** User reported the settings menu wasn't rendering full screen; after removing an unrelated `max-width` cap on `.settings-body`, the user immediately flagged the resulting whitespace-plus-small-font combination.
**Implication:** Don't assume the 2026-07-24 scale-up pass caught every region of `mobile/index.html` — check the actual `font-size`/`padding`/`gap` declarations for any UI area about to be touched, rather than trusting that "the convention was already applied."

## 2026-07-26 — `mobile/game.js`'s `startPhase()` applies the per-phase funds refresh unconditionally, including at phase 1 — this is what produces a 7,500 Cr opening balance, and it's correct, not a bug
**Finding:** `makePlayer()` sets `fundsCr` to `cfg.startingFundsCr` (5,000), then `createGame()` immediately calls `startPhase()` for phase 1, which unconditionally adds `fundsRefreshPerPhaseCr` (2,500) on top — giving both players 7,500 Cr before either has acted, not the 5,000 the design doc's prose implies at a glance. A guard (`if (game.phase > 1)`) was implemented to "fix" this, then explicitly reverted after confirming the double-credit produces the intended clean lifetime total: 5,000 + 2,500×10 = 30,000 Cr, matching `design/economy-status-map.md`'s own stated budget.
**Context:** User reported Nitish Kumar starting a game with 7,500 Cr instead of the documented 5,000, suspecting a bug. Investigation traced it to this double-credit; the fix was implemented, then the user clarified they're fine with 7,500 as long as the lifetime total is clean and symmetric for both players — which it is.
**Implication:** Don't re-flag this as a bug in a future session — the 7,500 Cr opening balance is intentional and symmetric across both players. If the design doc's prose is ever read as implying a 5,000 Cr opening balance, that's a documentation clarity issue, not a code bug.

## 2026-07-26 — Sardar Patel and Narendra Modi share `homeState: "Gujarat"`; Indira Gandhi and Atal Bihari Vajpayee share `homeState: "Uttar Pradesh"` — both are the existing "shared home state nullifies bonus for both players" rule firing correctly, not a bug
**Finding:** A screenshot showing Indira Gandhi at only 5% popularity in her own home state (Uttar Pradesh) against Atal Bihari Vajpayee looked like a missing home-state bonus. Both politicians have `homeState: "Uttar Pradesh"` in `data/politicians-data.json` — `mobile/engine.js`'s `generateStartingPosition()` explicitly cancels the home bonus for both players whenever they share a home state (lines ~186-198), which is exactly what fired here.
**Context:** User flagged the screenshot directly; confirmed by grepping both politicians' `homeState` fields and re-reading the cancellation logic in `generateStartingPosition`.
**Implication:** Before treating a low home-state popularity number as a bug, check whether the current match's two politicians happen to share a `homeState` (or an overlapping `secondaryHomeStates` entry) — this roster has at least two such collisions (Gujarat: Modi/Patel; Uttar Pradesh: Indira Gandhi/Vajpayee, and now also Rahul Gandhi/Nehru/Shastri/Ambedkar-adjacent via various UP-homed politicians).

## 2026-07-26 — The "Education" state-group tag (states_data.json) happens to cover exactly the five major regional film industries: Maharashtra, West Bengal, Tamil Nadu, Karnataka, Telangana
**Finding:** Needed a real state-group tag to represent a new "Film and TV" agenda's -ollywood footprint (Bollywood/Tollygunge/Kollywood/Sandalwood/Tollywood) without inventing new tag infrastructure. Checked each of those five states' actual tags directly: all five, and only those five among major film-industry states, carry the `Education` tag — a coincidental but clean match, better than `SouthIndia` alone (which misses Bollywood and Bengali cinema entirely).
**Context:** Designing the new Film and TV agenda's `tagEffects` for `data/policy-tags.json`.
**Implication:** Reuse this exact mapping (`Education` tag ≈ major film/media industry states) for any future entertainment- or media-industry-themed content, instead of adding a new tag to `states_data.json`.

## 2026-07-26 — Agenda taps apply automatically to every state nationwide based on tags (never player-targeted), so a seat-weighted "average national percentage-point swing" is a directly meaningful, comparable strength metric across all agendas
**Finding:** Unlike investment/rally actions, `tapAgenda()` in `mobile/game.js` loops over every state in `game.states` and applies `E.netAgendaEffectBps()` based on that state's own tags — the player never chooses where an agenda's effect lands. Computing `(net bps per state × that state's seats), summed, divided by 543 total seats, divided by 100` gives a genuinely comparable "how much does fully tapping this agenda move my national vote share" number across all 25 agendas. Results ranged from Education (+6.07 pts) down to Secularism (−3.73 pts) — a roughly 25x spread that per-tag magnitude tiers (4/8/12) alone don't predict, because India's real Lok Sabha seat distribution across tags is uneven (e.g. `CoastalIndia`/`AgriculturalRegion`/`IndustrialCorridor` cover far more seats than `EasternBorder`/`TribalLands`/`MinorityAreas`).
**Context:** User asked for a sanity check on all agendas' cost/benefit balance; raw seat-weighted totals (e.g. "329600") weren't legible on their own, so this was reframed as an average-percentage-point metric.
**Implication:** Use this same avg-pts-per-full-tap metric for any future agenda balance question in this project. Also: agendas with zero negative tag (Education, Press Freedom, Film and TV, Women's Empowerment, Healthcare, Anti-Corruption) land far above every agenda with a real positive/negative split, purely as a side effect of seat distribution, not deliberate design — worth knowing before assuming a "contested" agenda's low aggregate strength is a mistake.

## 2026-07-26 — `EasternBorder`/`WesternBorder` tags overlap substantially with `MinorityAreas` — nearly the entire Northeast plus J&K and Punjab carry both
**Finding:** Of the 266 total seats across `EasternBorder`/`WesternBorder`, 85 (~32%) are also tagged `MinorityAreas` — specifically Arunachal Pradesh, Assam, Manipur, Meghalaya, Mizoram, Nagaland, Sikkim, Tripura (the entire Northeast), plus Jammu & Kashmir and Punjab. This isn't scattered coincidence — it's genuinely where India's real security/minority-rights friction concentrates (AFSPA states, Kashmir, Punjab's history).
**Context:** Proposing to add a `MinorityAreas` negative tag to National Defense (replacing an unrelated `CoastalIndia` negative); user asked whether that tag choice would just cancel out the policy's own border-state positive in the same states.
**Implication:** Before adding an "opposing" tag to any policy in this pool, check its co-occurrence with the policy's own positive tags via `states_data.json` directly — this specific overlap doesn't zero anything out (worst case West Bengal nets +12−8=+4 at `MinorityAreas: -4`... wait, actually was tuned to -4 not -8 specifically to keep it a dampener, not a full cancel), but a naive `-8` or `-12` choice could have gutted the exact states the policy is supposed to reward.

## 2026-07-26 — `mobile/game.js`'s `finalizeGame()` checks the win-condition seat threshold in exactly one place, only at the very end of phase 10 — there is no early/mid-game win check anywhere in the engine
**Finding:** The game always runs all 10 phases (`endPhase()` only calls `finalizeGame()` once `game.phase >= game.cfg.totalPhases`); the winner is determined by a single two-line comparison against `game.cfg.seatsToWin` (272) at that point, never checked mid-game. This made implementing a per-player lowered win threshold (P.V. Narasimha Rao's reworked special power, wins at 250 instead of 272) a genuinely small change: one new player field (`seatsToWinOverride`), one new effect-kind case in the existing power interpreter, and reading the override (falling back to the shared constant) at this one comparison site.
**Context:** User asked how complex it would be to implement a "wins at a lower seat threshold" special power for Narasimha Rao (thematically: his real 1991-96 government governed as a minority government).
**Implication:** Any future per-player win-condition variant in this project is cheap to implement via the same pattern — no game-loop restructuring needed, since the check point is already fully centralized.

## 2026-07-26 — Several `specialPower.effect`/`power.description` text fields across the roster were stale or simply wrong relative to the real `power.benefits`/`costs` arrays
**Finding:** Found via a roster-wide text audit: Vajpayee's cost text claimed a funds penalty he doesn't actually pay (`power.costs` is empty); Mamata Banerjee's and B.R. Ambedkar's effect text described an old duration-based mechanic ("immune to opponent's agendas/rallies for rest of game", "permanent, non-decaying popularity floor") that predates the instant-only rework and no longer matches their real home-state flat-boost benefit; Rajiv Gandhi's effect text claimed a permanent ongoing discount his own description says was explicitly reframed as a one-time payout; Jayalalithaa's and Manmohan Singh's text each named an extra/wrong region not present in their actual `benefits`/`costs` tags.
**Context:** Triggered by the user flagging one generic cost string ("Funds hit this phase" for Bachchan), which led to a systematic pass comparing every politician's `specialPower.effect`/`.cost` and `power.description` against their real `benefits`/`costs` arrays.
**Implication:** When editing a politician's power mechanics, always check `specialPower.effect`/`.cost` text against the real `power.benefits`/`.costs` in the same edit — the two are not kept in sync automatically anywhere, and drift is easy to miss since both "look" plausible in isolation.

## 2026-07-26 — An instant before/after `nationalSeats()` diff cannot measure the value of funds/token-transfer power effects
**Finding:** Testing Indira Gandhi's redesigned power (seizes opponent funds + tokens, costs -10% self popularity nationwide) as an instant swing measured -52.7 seats, which looked like proof the seizure itself was harmful. It wasn't — `stealFundsPct`/`stealTokens`/`seizeFundsPct`/`seizeTokens` effects don't touch `game.pop` at all, so an instant diff only ever sees the popularity-cost side of the power; the seizure's actual value (denying the opponent's future investment budget) only shows up over the phases that follow.
**Context:** Discovered while modeling the correct self-cost magnitude for Indira Gandhi's redesigned "National Emergency" power, after the naive instant-swing test kept producing seat values ((-52.7, -22.5, -7.3 at various cost levels) that didn't match manual expectations of a "should-be-worth-it" power.
**Implication:** Any power whose benefit is funds/tokens (not popularity) must be measured by forking an identical game state into "used" vs "skipped" branches (clone via `JSON.parse(JSON.stringify(game))`, reassigning fresh `rng` functions since functions don't survive the clone) and playing both out to the game's end, comparing final seats — never by an instant before/after diff.

## 2026-07-26 — `mobile/game.js`'s `aiStep`/`runAIFull` are hardcoded to act on player key 'p2' only; swapping player objects to reuse them for 'p1' silently corrupts the population data
**Finding:** Attempting to simulate "both sides play normally" for a headless power-value test by temporarily swapping `game.players.p1`/`.p2` object references (so `aiStep`'s hardcoded `'p2'` targeting would act on the human slot) produced a plausible-looking but false result ("Tendulkar's floor-power value increases over time") — because `game.pop[svgId].p1`/`.p2` are fixed string keys independent of which politician object currently sits in which player slot. The swap credited investment gains to the wrong pop bucket (the original p2/opponent's), while the swapped-in player's funds were spent for no real popularity benefit.
**Context:** Found while trying to build a rigorous "both sides invest" test for Tendulkar's 40%-floor power's value over the course of a game; the bug produced numbers (60→77 seats increasing over phases) that looked reasonable enough to almost be reported to the user before being caught and re-derived correctly.
**Implication:** To have p1 invest realistically in a headless Node test, call `investCash`/`tapAgenda` directly with playerKey `'p1'` using a custom (even simple greedy-headroom) strategy — never reuse the AI's own functions via a player-slot swap, since `aiStep`'s hardcoded `'p2'` targeting doesn't follow the swap.

## 2026-07-26 — Testing any power against Jawaharlal Nehru as the opponent is confounded by his own power's opponent-nullify effect
**Finding:** An AI-driven multi-phase test of Tendulkar's floor power against Nehru as the opponent returned exactly 0 seat gain in 300/300 trials at phase 6+, initially read as "the power decays to zero value by midgame." The real cause: Nehru's own special power (Non-Alignment) secretly nullifies whichever side activates their power first against him, and the test's AI-driven opponent had already used it during the simulated phases — a roster-wide interaction that would do this to any of the 20 politicians' powers, not something specific to the power under test.
**Context:** Found by adding a diagnostic check for `res.nullified` on the `activatePower` return value after the "decays to zero" result didn't match a manual sanity check of how many states were actually below the power's floor threshold.
**Implication:** Use a neutral opponent (e.g. Modi) for any power-value modeling test, unless the interaction with Nehru specifically is what's being tested.

## 2026-07-25 — Gemini-generated portrait filenames don't reliably match the expected naming convention
**Finding:** Of 7 new politician-portrait images added this session, 4 needed a filename fix before `politicians-data.json`'s `image` field would resolve: "Bhimrao_Ambedkar.png" (data expected "BR_Ambedkar.png"), "P_V_Narasimha_Rao.png" (data expected "PV_Narasimha_Rao.png"), and two straight typos, "Rajinikaanth.png" and "Laal_Bahadur_Shastri.png" (extra letters).
**Context:** Found by scripting a check that reads every politician's `image` path out of `politicians-data.json` and confirms the file exists on disk, run after each batch of new images the user dropped into `assets/images/`.
**Implication:** After any future Gemini portrait-generation batch, always diff actual on-disk filenames against `politicians-data.json`'s `image` field before calling the task done — don't assume the generated filename matches the `Full_Name_With_Underscores` convention. Fix pure typos by renaming the file; fix legitimate alternate-name choices (e.g. full first name spelled out) by updating the data path instead.

## 2026-07-25 — Gemini image generation confused Rajinikanth with a same-named fictional film character
**Finding:** An earlier Gemini-generated "Sivaji_Rao.png" (wired to the `rajinikanth` roster entry) actually depicts Anil Kapoor's character from the Bollywood film *Nayak*, not Rajinikanth — coincidentally, "Shivaji Rao" is both that film character's name and Rajinikanth's own real birth name (Shivaji Rao Gaekwad), which is almost certainly what caused the mixup. The rest of that data entry (Tamil Nadu home state, "THALAIVAR OF THE MASSES" archetype, South India/Maharashtra special power) was genuinely written for Rajinikanth and needed no change.
**Context:** User spotted the wrong portrait by inspecting the actual generated image rather than trusting the filename/label; resolved by keeping the Nayak character as its own new 21st roster entry ("sivaji-rao", Lokahit party, Maharashtra) and pointing the real `rajinikanth` entry at a not-yet-generated `Rajinikanth.png` (falls back to the initials circle until regenerated).
**Implication:** When a politician's real name, nickname, or birth name coincidentally matches a known fictional character or alternate persona, visually verify the generated portrait's actual content before wiring it into data — a plausible filename is not proof the image shows the intended person.

## 2026-07-25 — object-fit:cover on a full-bleed background image crops noticeably tighter on a real phone than a Playwright `devices['iPhone 14']` screenshot predicts
**Finding:** Building the new welcome-screen poster background (`.welcome-bg`, full-bleed `assets/images/PradhanMantriBg.png`), `object-fit:cover` looked fine in every Playwright screenshot (both the `devices['iPhone 14']` preset and plain narrow-viewport pages) — all faces fully visible with margin. On the user's actual phone, the same CSS sliced the leftmost portrait's face in half at the screen edge, cropping noticeably tighter than any emulated screenshot showed.
**Context:** User reported "the sides get cut off on full screen" after viewing the live mockup over LAN on a real device; a first fix attempt (shrinking the overlay's text/button sizing) didn't address it, since the actual problem was the background image's own crop, not text overflow — confirmed once the user sent a real-device screenshot showing the cropped face directly.
**Implication:** Switched to `object-fit:contain` (with a solid-color background fallback for letterboxing) instead of `cover` for this image — guarantees the full image is always visible regardless of device quirks. Add to this project's existing Playwright-vs-real-device caveat list: don't trust an emulated screenshot alone to judge `object-fit:cover` crop tightness on a full-bleed image where cropping the edges would cut off meaningful content (faces, text) — verify against a real device, or default to `contain`.

## 2026-07-25 — Gemini image generation renders nicknames/idioms literally, not their real-world referent
**Finding:** Prompting Sardar Patel's real-world nickname "Iron Man of India" produced the Marvel superhero Iron Man plus a literal map of India in the background, not a caricature of Patel. Real proper-noun landmark names (Golden Temple, Qutub Minar, Taj Mahal, Statue of Unity) rendered correctly across all 9 politician-portrait prompts that used them.
**Context:** User reported this after trying the first batch of 11 missing-portrait prompts; rewriting the Sardar Patel, Amitabh Bachchan, and Sachin Tendulkar prompts to strip every nickname, metaphor, and parenthetical explanatory aside (e.g. "(site of the 2011 World Cup win)") down to purely concrete physical description fixed it.
**Implication:** Any future Gemini image-gen prompt for this project's politician portraits (or similar assets) must describe only concrete, literal visual content — no honorifics, nicknames, or metaphors, and no parenthetical meta-commentary. Real proper-noun place names are safe and reliably render correctly.

## 2026-07-25 — This session's Gemini image-generation MCP tool is not currently callable — wrong model ID for this API key/project
**Finding:** A test call to `mcp__gemini__gemini-generate-image` errored: `Error generating image: {"error":{"code":404,"message":"models/gemini-2.0-flash-preview-image-generation is not found for API version v1beta, or is not supported for generateContent...","status":"NOT_FOUND"}}`.
**Context:** Attempted to validate a politician-portrait prompt by generating it directly (via a forked agent) before handing prompts to the user manually; the call had already been dispatched when the user asked not to run it themselves.
**Implication:** Image generation from this Claude Code session isn't currently usable — continue handing prompts to the user for manual generation via Gemini's own UI/app rather than attempting the MCP tool, unless the configured model/API access is fixed.

## 2026-07-25 — The real in-browser AI pacing throttle (~20 actions/min) left the AI unable to spend its own phase budget, invisibly to all prior headless testing
**Finding:** `mobile/main.js`'s `scheduleAITick()` gave the AI one action per ~3 real seconds (2-4s randomized cooldown), only ~15 ticks in a 45-second phase — nowhere near enough to spend a 2,500 Cr/phase refresh once agenda taps (which sit at higher priority in `aiStep()`'s decision order) compete for those same ticks. A throttled-vs-unthrottled comparison across 5 seeds showed the AI ending games with anywhere from 5 Cr to 20,560 Cr unspent (one seed: 68% of its entire 30,000 Cr lifetime budget), costing an 84-seat swing (233 vs 317 seats) in that worst case.
**Context:** User reported the AI ends games with a lot of leftover funds after playtesting two earlier AI-strength fixes. This was invisible to all prior Node-based testing (`mobile/simulate.js`, and every simulation run earlier this session) because those always call `G.runAIFull()`, which has no pacing limit — the headless-tested AI and the real browser AI were effectively two different opponents the whole time.
**Implication:** Fixed in `mobile/main.js`: `planAITickPacing()` now dry-runs the AI's remaining turn on a `structuredClone` of the game at the start of every phase (own independent RNG, doesn't touch the real game's randomness) purely to count how many actions it actually needs, then spreads exactly that many ticks evenly across the phase's real duration (clamped 300ms-4000ms/tick). An earlier fix attempt (an instant catch-up burst via `G.runAIFull()` right before each phase closes) was explicitly rejected by the user as an unfair machine-speed advantage a human can't compete with — even pacing was the requirement, not just full budget utilization. Any future AI-behavior testing done via headless Node simulation should be treated as an upper bound on AI strength, not a match for the real browser opponent, unless this pacing constraint is explicitly modeled too.

## 2026-07-25 — mobile/game.js's investment-scoring formula favored tiny states by 30-80x over big states, regardless of tap count
**Finding:** `scoreInvestState()`/`pickAIInvestmentTarget()` scored states by bps-gained-per-Cr (`effectiveGain / cost`), not seats-gained-per-Cr. Since cost scales with a state's seat count but the % boost per tap doesn't, this is inversely proportional to state size: a 1-seat UT scored 20-50 at any tap count, versus 0.25-0.6 for Uttar Pradesh (80 seats) even on its very first, most-boosted tap. Tiny states also have coarse seat-rounding, so cheap % gains there often don't convert to an actual seat at all.
**Context:** Found via headless simulation testing multiple player strategies against the real AI (a bot mirroring a "group-focus + rallies, no agendas" strategy repeatedly got stuck hammering 1-2 cheap states). User separately, independently observed identical behavior in real play ("your bot acts too slow... keeps investing in one or two states"), confirming this matches the shipped AI's own targeting formula, not just a quirk of the test harness.
**Implication:** This was the first of two root-cause AI bugs behind the game feeling both "too hard" (a human avoiding this trap has a structural edge) and, once found, "too easy" (a human exploiting groups+rallies could blow the AI out, e.g. a 340-seat Yogi Adityanath win). Fixed as part of the broader AI rework: investment targeting now commits to one randomly-chosen state group for the whole match (`pl.aiTargetGroup`) instead of round-robining/chasing raw bps-per-Cr nationwide.

## 2026-07-25 — The shipped AI never crafted Nationwide Rally in any tested game, because it always spent rally tokens individually before ever checking whether to save toward the 12-token craft threshold
**Finding:** `aiStep()`'s priority order tried playing a rally token (whenever it had any and hadn't hit the 2/phase spend cap) before ever attempting `craftToken(..., 'nationwide')`. Isolated testing (15 games, opponent passive) confirmed `craftedNationwide` was `false` in all 15 — the AI structurally could never bank enough tokens to reach the 12-token threshold, missing a mechanic the design doc calls out as a +5%-nationwide swing across all 543 seats.
**Context:** Investigated after the user's real Yogi Adityanath game (340 seats, no powers used, "state groups and rallies" strategy) couldn't be reproduced by any bot mirroring that reported strategy — pointed at the AI itself being weaker than expected rather than a politician-specific imbalance.
**Implication:** Fixed per the user's explicit simpler spec: the AI now attempts exactly one rally placement per phase at a random pick from the 10 largest-seat states (`pickAIRallyTarget`); a rejected placement (state already at its shared 2-play cap) is left unspent rather than retried elsewhere, banking toward an unconditional auto-craft-and-deploy of the Special Power the instant 6 tokens accumulate (`specialPowerupCraftCost`), no longer gated by AI personality profile. Verified: win count for a "groups + rallies, no agendas, no powers" exploit strategy dropped from 14/120 to 12/120 across all 20 politicians after this fix, with average seats down across nearly every politician tested (e.g. Yogi Adityanath 223→219.2, Nitish Kumar 286→274.5).

## 2026-07-24 — The politician-select/end-game overlay CSS was authored at real mobile scale, but this app renders on a virtual ~980px canvas zoomed out ~0.4x — the opposite of what an earlier finding this same day assumed
**Finding:** `mobile/index.html` has no `<meta viewport>` tag; both Safari and Chromium fall back to a ~980px-wide virtual layout viewport and zoom the whole page out (measured precisely via `window.visualViewport.scale`: 0.398–0.401) to fit the real device width. The in-game HUD CSS (`.phase-chip`, `.iconbtn`, `.action-btn`, etc.) is authored assuming this ~2.5x inflation — e.g. a 35px phase-chip font renders at a normal ~14px real size. The ballot-card/select-screen CSS (added later, for the redesign) was authored at literal real-world sizes instead (19px name, 13px body text), so after the same 0.4x zoom it rendered at ~40% of intended size — reported by the user as "text too small and unreadable."
**Context:** Diagnosed after fixing an unrelated whitespace/cropping bug on the same screen, when the user reported unreadable text and a huge internal gap. Confirmed with a live Playwright measurement (`visualViewportScale`) rather than guessing, then verified border-widths/box-shadow-offsets are *not* part of the 2.5x convention (compared ballot-card's `border:3px solid` against in-game `.iconbtn`'s identical unscaled `border:3px solid`).
**Implication:** Any future UI added to this app's pre-game/end-game overlays must be authored at ~2.5x the visually-intended size (fonts, padding, margin, gap, icon sizes) to match the rest of the app — this is the established, if unusual, convention here, not a bug to fix by adding a real viewport meta tag (confirmed separately that doing so would overflow/clip the in-game screen's fixed ~755–765px chrome against a true device viewport). Leave border-width, box-shadow offsets, and `:active` press-transform values unscaled. This directly supersedes this same day's earlier finding below ("Chromium is a valid stand-in... authored directly at real mobile scale") — that scale was itself the bug, since fixed in this session.

## 2026-07-24 — Playwright DOM measurements on this app must be converted out of the inflated 980px virtual-canvas coordinate space to mean anything against a real device budget
**Finding:** Playwright's `devices['iPhone 14']` preset (and even an explicit custom `viewport:{width:393,height:852}` matching the user's real phone) still renders this app at `window.innerWidth`/`layoutW` = 980, because the missing viewport tag triggers the same ~980px fallback in Chromium as in real Safari. `page.screenshot()` still captures the true, correctly zoomed-out visual appearance, but any DOM metric (`getBoundingClientRect()`, `clientHeight`, `window.innerHeight`) is reported in that inflated 980-unit coordinate space, not real CSS px.
**Context:** Needed exact overflow/margin numbers (not just "looks about right" from a screenshot) to fit the ballot card on one screen without scrolling, especially for the tightest content case. A raw `getBoundingClientRect()` height looked alarmingly large until multiplied by the real conversion factor.
**Implication:** Any future layout-fit check on this project must multiply layout-space DOM measurements by `(real device CSS width / 980)` to get true px before comparing against a real device's available height — a bare bounding-rect number is meaningless on its own here.

## 2026-07-24 — Jawaharlal Nehru's special-power description (266 chars) is by far the longest of all 20 politicians', making his card the standing worst-case layout test
**Finding:** Queried `data/politicians-data.json` directly: Nehru's `power.description` is 266 characters; the next-longest (Vajpayee) is only 129. Every other politician's card has meaningfully more headroom than his.
**Context:** Needed a concrete worst-case politician to stress-test "does the ballot card fit on one screen without scrolling" before finalizing card-height budget trims, rather than eyeballing only the default (Modi, index 0) card.
**Implication:** Use Nehru's card (carousel index 9) as the standing regression check for any future ballot-card layout change — if his card fits without scrolling, the rest reliably do too.

## 2026-07-24 — Playwright's page.clock.fastForward() only fires a repeating setInterval once, regardless of duration
**Finding:** Trying to fast-forward the mobile build's real phase timer (a plain `setInterval(fn, 1000)`) to reach game end, `page.clock.fastForward(60000)` only incremented the counter by 1, not 60 — confirmed with an isolated minimal repro (`setInterval` counter, `fastForward(60000)` → count stays 1). `page.clock.runFor(60000)` on the identical repro correctly fired it 60 times.
**Context:** Debugging why a Playwright test's fast-forwarded game never reached `game.winner` despite advancing "hours" of virtual time via `fastForward`.
**Implication:** Use `page.clock.runFor()`, not `fastForward()`, whenever a test needs to simulate elapsed time against a `setInterval`-based app timer — `fastForward` appears intended for a single jump, not for firing all timers due within the window.

## 2026-07-24 — Chromium is a valid stand-in for this app's select/end-overlay screens, but not its shared gameplay chrome
**Finding:** `mobile/index.html` has no `<meta viewport>` tag (see 2026-07-23 finding below), so Chromium renders it at the literal CSS viewport width while Safari falls back to a ~980px canvas. This only breaks screens whose CSS assumes that ~980px scale (the topstrip/map gameplay chrome, sized with 33–45px fonts) — a Chromium screenshot of that chrome shows badly oversized, wrapping text. The politician-select carousel and end-of-game overlay were authored directly at real mobile scale (11–23px fonts), so Chromium renders them correctly and matches WebKit+iPhone-profile screenshots of the same screens.
**Context:** Discovered comparing a Chromium screenshot of the old politician grid (correct) against gameplay-chrome screenshots (previously documented as needing WebKit).
**Implication:** Default to Chromium for iterating on the select/end-overlay screens (faster, less flaky in this environment — see next finding); reserve WebKit+`devices['iPhone 14']` specifically for anything touching the shared gameplay chrome.

## 2026-07-24 — Playwright WebKit's page.screenshot() intermittently hangs on "waiting for fonts to load" even when the page itself is fully loaded
**Finding:** Repeatedly, `page.screenshot()` under WebKit hung indefinitely at an internal "waiting for fonts to load" step while iterating on the redesigned select/end screens — even though direct inspection in the same session showed `document.fonts.status === "loaded"`, zero console/page errors, and all expected DOM already present. Not caused by any project change (no new `@font-face`, just CSS `font-family` fallback stacks referencing system fonts like Rockwell/Roboto Slab).
**Context:** Hit across several separate screenshot attempts this session, at various timeouts up to 120s; confirmed via a diagnostic script that checked page/DOM state directly instead of relying on the screenshot call succeeding.
**Implication:** If a WebKit screenshot hangs on "waiting for fonts to load" in this environment, don't just keep raising the timeout — verify page state directly (console listeners + a `document.fonts.status`/DOM evaluate) first, then fall back to Chromium if the screen doesn't depend on Safari's viewport-fallback behavior (see prior finding).

## 2026-07-24 — A flex child with no explicit width/content collapses to zero width when its parent's align-items isn't the default stretch
**Finding:** A decorative tricolor stripe (`display:flex` container of three empty `<span>` segments, no explicit width) rendered correctly on the politician-select ballot card but was completely invisible (0×0 bounding rect) on the new end-of-game "declaration" card — identical CSS rule, different result. Root cause: the select-card's container had no `align-items` override (default `stretch`, so the stripe stretched to full width); the declare-card container set `align-items:center` (needed to center the seal/headline/etc.), which instead shrank the content-less stripe to zero.
**Context:** Found via `getBoundingClientRect()` returning `{width:0, height:0}` for `.tricolor` on the end screen, after the element visually failed to appear in a screenshot.
**Implication:** Any decorative flex item with no intrinsic content (empty-span color bars, spacer divs) needs an explicit `width:100%` — don't assume a rule that worked in one flex container will behave the same in a sibling container with different `align-items`.

## 2026-07-24 — A shared trailing "detail" element breaks tap-to-expand accordions once rows read as a sequential list, not a grid
**Finding:** The politician card's manifesto rows use a tap-to-expand region-effect breakdown. The approved standalone mockup (`design/prototypes/pol-card-mockup.html`) correctly gives each row its own adjacent detail element (CSS `.plank.open + .plank-detail`). When wiring the approved *look* into the real app, the *old* select-screen's single-shared-trailing-detail-div code (left over from when agendas were a 2-column grid, where "detail always at the bottom" wasn't confusing) got reused by mistake — so the breakdown always opened under the last row, not whichever row was actually tapped.
**Context:** Caught by screenshot review after wiring the ballot-card redesign into `mobile/main.js`: tapping "Infrastructure" (first row) visibly expanded chips below "National Defense" (last row) instead.
**Implication:** When porting an approved mockup into production code, port its DOM/interaction structure deliberately, not just its visual styling — re-implementing "the old way" underneath new markup can silently break behavior the mockup already got right. Fixed by giving each agenda row its own adjacent detail div, matching the mockup.

## 2026-07-24 — The 2026-07-24 funds bump (2,500/1,000 → 5,000/2,500 Cr) was never actually recorded inside design/economy-status-map.md itself
**Finding:** The design doc's Core loop, Agenda, and Plausibility-check sections continued citing the old 2,500 Cr start / 1,000 Cr refresh / 12,500 Cr lifetime budget for at least a full day after the real values shipped in `data/game-config.json`'s `mobileEconomy` namespace. The decision itself only ever existed in CLAUDE.md and the live config — never backfilled into the design doc's own prose.
**Context:** Found while building a full project wiki (`docs/wiki.html`) and cross-referencing every mechanic described in `design/economy-status-map.md` against `data/game-config.json → mobileEconomy` directly, rather than trusting the doc's stated numbers.
**Implication:** When a numeric constant changes in code/config, check whether `design/economy-status-map.md`'s prose (not just CLAUDE.md) needs a matching update — CLAUDE.md recording a decision does not guarantee the design doc reflects it. Now fixed: Core loop, Agenda, and Plausibility-check sections all updated to 5,000/2,500/30,000.

## 2026-07-24 — Recomputing the Plausibility-check table against the real 30,000 Cr budget reverses the design doc's "cash alone can never win" claim
**Finding:** At the new budget, the idealized cash-only scenario (no tokens, no agendas, opponent passive, spend spread evenly) clears 272 seats on its own: ~281/543 (51.8%), a ~9-seat margin — versus the old budget's ~203/543 (37.4%), well short of a majority. The combined-strategy margin (cash + full tokens + 2 strong agendas) widened from ~19 seats to ~99 seats over 272.
**Context:** Verified the recompute methodology first by reproducing the design doc's own pre-existing ~203/543 and ~42-seat figures exactly (bit-for-bit) using the same round-cost/decay-curve formula (`costPerTap = seats×10`, boost decaying 500→200 bps over 20 taps, one "round" = tapping every one of the 543 seats once at a flat 5,430 Cr), then applied the identical method to the new 30,000 Cr budget.
**Implication:** The design doc's "cash alone still cannot win under any circumstances — a hard mathematical wall" framing is no longer accurate and has been corrected. More importantly, the unmodeled "two-active-player" case (a real adversarial opponent contesting the same states, not the passive-opponent idealization used throughout this section) is now the single most load-bearing open question in the document — a ~99-seat idealized cushion says much less about real-match balance than the old ~19-seat one did, and is worth prioritizing before further economy tuning.

## 2026-07-24 — Two more "Still not built" design-doc claims were stale beyond the six divergences originally audited: the settings menu and AI personality variety are both already fully shipped
**Finding:** `mobile/main.js:799-823` has a working settings overlay (Sound toggle, Music toggle, Pause/Resume, New Game with a confirm guard) — contradicting the doc's "no Sound/Music/Pause/Help/New-Game controls exist" claim (only Help is genuinely missing). Separately, `mobile/game.js`'s `AI_PROFILES` array already defines exactly the four originally-proposed AI personality profiles (aggressive-investor, policy-rusher, rally-spammer, group-bonus-rusher), and `pickAIProfile()` assigns one at random per match via `createGame()` — contradicting the doc's "not built" claim for AI personality variety.
**Context:** Found while resolving a different, already-flagged divergence (PWA infrastructure) — checking the same "Still not built" list in the Build status section turned up two more stale bullets sitting right next to the one being fixed, neither of which was part of the original six-item audit.
**Implication:** The "Still not built" list had drifted further out of sync with `mobile/` than the targeted six-item review caught. Both bullets have now been corrected (settings menu narrowed to just the missing Help control; AI variety struck as shipped, with the still-true "not adversarially tuned, just varied" distinction preserved). Worth a fresh full pass over the rest of that list rather than assuming everything else in it is current now that these two are fixed.

## 2026-07-24 — check_data_consistency.js's 3 pre-existing failures are confined entirely to the legacy desktop js/*.js build, not mobile/
**Finding:** Direct grep confirmed all 3 stale-field-reference failures (`NortheastIndia`/`BorderLands`) live only in `js/campaign-system.js` and `js/state-manager.js` (the legacy desktop-ported skin) — none in `mobile/`.
**Context:** Ran `check_data_consistency.js` directly while gathering ground truth for the wiki's data-files section, rather than relying on CLAUDE.md's prior note alone.
**Implication:** No action needed on `mobile/` for this; safe to keep citing it as a legacy-only, mobile-irrelevant known issue whenever `check_data_consistency.js` is re-run after a data edit.

## 2026-07-24 — "Modi's home-state bonus isn't working" was the documented shared-home-state tie rule, not a bug — Sardar Patel also has homeState "Gujarat"
**Finding:** User reported Narendra Modi starting at only 25% popularity in Gujarat at phase 1, suspecting the home-state bonus was broken. Direct simulation of `generateStartingPosition` confirmed the bonus code works correctly in general (Modi's Gujarat share was always ≥30% across 10 trials against various opponents — matching the documented "+25% on top of baseline roll" floor). Simulating Modi vs. Sardar Patel specifically (the one other roster politician whose `homeState` is also "Gujarat") reproduced exactly 25% in 2 of 8 trials — matching the user's report precisely.
**Context:** Found by testing the starting-position generator directly with real game data rather than re-reading the (already-correct) bonus code; the roster check (`data/politicians-data.json`) showed Modi and Sardar Patel are the only two of 20 politicians sharing Gujarat as home state.
**Implication:** This is the existing documented tie rule working as intended (`design/economy-status-map.md`'s Home state section: shared home state → bonus nullified for both, no split) — no code change was made. Worth remembering Modi/Patel as the concrete example next time a "home bonus isn't applying" report comes in: check whether the AI opponent that match happened to share the same home state before assuming a code bug.

## 2026-07-23 — mobile/sw.js's cache-first fetch strategy meant a phone's cached build never picked up code changes until the service worker's own bytes changed
**Finding:** The `fetch` handler did `return cached || network` — always serving a cached response first if one existed, and only updating the cache in the background for *next* load. Combined with a static cache name (`pme-mobile-v1`), and browsers only re-running a service worker's `install` when the *byte content of `sw.js` itself* changes, a phone that had cached the build once stayed permanently stuck on that version regardless of what was actually on the server.
**Context:** User reported the AI still showing pre-fix fund behavior (starting at 0, no phase refresh) despite the source already being corrected in `mobile/game.js`. Traced by reading `sw.js` directly rather than re-debugging the already-fixed game logic.
**Implication:** Fixed to network-first (`fetch` preferred, cache only as an offline fallback) and bumped the cache name to force a clean slate. Any future report of "the phone shows old behavior despite the source being fixed" on this project should check `mobile/sw.js`'s fetch strategy and cache version first, not re-debug the already-fixed code.

## 2026-07-23 — The apparent "AI always has ~0 funds" complaint was not a funds-refresh bug — both players get identical funds, the AI just spends its entire pool down to near-zero every phase before the player's first render
**Finding:** Instrumented `createGame`/`startPhase` directly: `startingFundsCr:2500 + fundsRefreshPerPhaseCr:1000` gives both p1 and p2 exactly 3500Cr at phase start. But `runAI()` (at the time) ran synchronously inside `startPhase()`, and its last step looped buying seat-investment until funds dropped below `costPerSeatCr` — so by the time the UI rendered, p2's funds were already spent to ~0, every single phase, by design (a greedy heuristic), not because refresh was broken.
**Context:** User suspected either a leftover economy bug or "the AI just plays too fast" — this trace confirmed the latter and ruled out the former before any code changed.
**Implication:** Motivated the pacing rework (aiStep()/runAIFull() split, ~20 actions/min throttle in main.js) — the actual fix was behavioral/pacing, not an economy-engine correction. Confirms `startPhase()`'s funds-refresh logic itself is symmetric and correct.

## 2026-07-23 — mobile/index.html has no `<meta name="viewport">` tag at all
**Finding:** Grep and a direct `<head>` read confirmed zero viewport meta tag. Under Playwright/webkit with `devices['iPhone 14']`, `window.innerWidth` measured 980 (the classic no-viewport-tag Safari fallback canvas) even when the browser context's own viewport was explicitly forced to 390×664 — the page's CSS layout viewport ignores emulated/physical width entirely without the tag. Only injecting a viewport meta tag via response-body route interception (test-only, not applied to the real file) produced a true 390px layout render.
**Context:** Discovered while verifying whether a new 7-button action row would wrap at real iPhone width, during the "combined action bar" iteration of the corner-buttons redesign.
**Implication:** Not fixed — out of scope for the requesting task, and the user's real device apparently renders it acceptably regardless (confirmed via their own screenshot showing normal proportions). Flagged to the user directly. This project's CLAUDE.md already documents the *prototype* (`pme-mobile-sheet.html`) intentionally omitting this tag for chrome-height reasons; `mobile/index.html` (the real build) has the same omission and it's unclear whether that's deliberate or inherited by accident — worth revisiting if iOS-specific zoom/scaling issues are ever reported.

## 2026-07-23 — AI investment scoring used raw boost/cost with no regard for remaining headroom, so the AI would endlessly reinvest in an already ~100%-owned state instead of moving to the next state in its target group
**Finding:** `scoreInvestState()`'s formula (`boost/cost + tiebreaker`) always favored the cheapest state in a group regardless of how close that state already was to full ownership, since `investCash`'s actual gain caps at `10000 - currentShare` but the score never accounted for that cap. Stress test with unlimited funds (3000 forced `aiStep()` calls): before the fix, the AI stayed stuck on group index 0 for all 2982 successful investment actions and 0 of 15 regional-dominance groups reached full dominance. After changing the score to use effective remaining headroom (`min(boost, 10000-currentShare)/cost`) instead of raw boost, the same test reached full dominance in all 15 groups within 911 actions.
**Context:** Found while stress-testing the newly-added group-based AI investment targeting (round-robin over `game.groups`, skipping already-dominant ones) — the grouping alone didn't produce the intended "invest across the group" behavior until this scoring fix.
**Implication:** This was a real, previously-undetected inefficiency in the investment AI (pre-existing before the group-targeting change, just newly exposed by it) — worth remembering if investment-AI scoring is touched again. The fix is in `mobile/game.js`'s `scoreInvestState()`.

## 2026-07-23 — `.vs-bar[hidden]` did nothing because `.vs-bar{display:flex}` (author CSS) beats the browser's default `[hidden]{display:none}` UA-stylesheet rule at equal specificity
**Finding:** Setting `$('cardVsBar').hidden = true` in JS left the vs-bar still visibly rendered underneath the new group-card LED grid, confirmed via a Playwright screenshot. This codebase already has the fix pattern established in three other places (`.overlay[hidden]`, `.group-readout[hidden]`, `.target-banner[hidden]`) — it was simply missing for the newly-added `.vs-bar`.
**Context:** Found while visually verifying the new group-overview card feature (LED indicators per state in a group) — the vs-bar (only meaningful for a single selected state) should have been replaced by the LED grid but wasn't disappearing.
**Implication:** Fixed by adding `.vs-bar[hidden]{display:none}`. This is now a 4-instance pattern in this codebase: any element toggled via the `hidden` attribute must get an explicit `<selector>[hidden]{display:none}` rule if that same selector's base class already sets its own `display` value — proposed as a CLAUDE.md rule (see checkpoint).

## 2026-07-23 — design/economy-status-map.md's "Known bugs" playtest list was stale; 5 of its items were already fixed in mobile/game.js and mobile/engine.js
**Finding:** Before implementing the 13-task punch list built from the design doc's own "Known bugs" and "Still open" sections, a direct code read plus targeted diagnostics showed 5 of them already working correctly: the AI opponent does spend funds and act every phase (it just greedily spends to ~0 Cr synchronously before any render, which reads as "not acting" but isn't), the starting-position generator never exceeded 187 seats across a 3000-seed stress test (not 200), agenda effects already prorate ¼ per tap, hung-parliament already resolves as a loss vs. the AI, and seat conversion already uses largest-remainder apportionment.
**Context:** Ran diagnostic scripts (`node`-based, reusing `mobile/game.js`/`engine.js` directly) against each reported bug before writing any fix, rather than trusting the doc's bug descriptions at face value.
**Implication:** In this project, a "known bugs" or "still open" list in a design doc can lag real code state significantly — verify against actual code/tests before implementing a fix for any reported bug here, not just before recommending from memory.

## 2026-07-23 — engine.js's loseAt() let a popularity loss vanish when a player held exactly 100% of a state, breaking the p1+p2+others=10000 bps invariant
**Finding:** When `self` (a player's share of a state) is exactly `BPS` (10000) — reachable by tapping a cheap 1-seat UT enough times — `opponent` and `others` are both 0, so `loseAt()`'s `if (denom > 0)` guard skipped the split entirely, leaving `oppGain`/`othGain` at 0 while still unconditionally doing `pop[actor] -= loss`. The freed share disappeared instead of landing anywhere. `gainAt()` has no equivalent bug because a `self`-at-cap boost is clamped to `gain=0` before the same-shaped branch is ever reached — only the loss path has this asymmetry.
**Context:** Surfaced only after adding 4 AI personality profiles (task: "AI difficulty/personality variety") — the `aggressive-investor` profile taps agendas less and invests far more, which was the first thing to actually drive a tiny cheap state to 100% ownership and then apply a negative agenda effect there within the 6-phase power-activation test window. Confirmed via a targeted trace script showing `INAN`/`INLD`'s pop sum shrinking by exactly the agenda's per-tap delta each phase while `p1`/`others` stayed frozen at 0.
**Implication:** Fixed by falling back to giving the freed share to `others` when `denom === 0` (mirrors the existing "round one side, derive the other" discipline's spirit — no proportion to split by, so the neutral default absorbs it), plus a regression test in `engine.js`'s self-check. Any future change to AI investment aggressiveness or to how quickly a state can reach full ownership should re-run `npm test` plus a stress test specifically hammering cheap/tiny states, since this exact denominator-zero shape of bug could recur wherever a "split proportionally" branch is added without a same-shaped clamp to what `gainAt()` already has.

## 2026-07-23 — the local dev server's static handler only resolves an exact `/` request, not a bare directory path like `/mobile/` — silently breaks any service worker precache list that includes `'./'`
**Finding:** `npm run serve`'s inline Node server (`package.json`) only substitutes `index.html` when the request path is exactly `/`; a request for `/mobile/` (no filename) 404s. A new `mobile/sw.js`'s install handler used `caches.addAll(['./', './index.html', ...])` — `caches.addAll` fails atomically if any one URL 404s, so the service worker never installed, `navigator.serviceWorker.ready` hung forever, and `reg.active` was always `false`.
**Context:** Found via a Playwright script that hung on `await navigator.serviceWorker.ready`; isolated by registering the worker directly and inspecting `installing`/`waiting`/`active` state, then confirmed the exact failing request by removing `'./'` from the precache list.
**Implication:** Any future service worker (or other `caches.addAll`-style precache list) on this project must list explicit filenames only, never a bare directory path — this dev server's routing will silently break the whole install otherwise, with no error surfaced anywhere except a hung `serviceWorker.ready` promise.

## 2026-07-23 — Live GitHub Pages deployment still serves the old pre-mobile-rebuild code; the finished mobile engine is unpushed
**Finding:** `origin/main` (which `pradhanmantrielectionsgame.github.io` auto-deploys from) has no `mobile/` directory at all — it's identical to local `main`, still the old desktop-ported `index.html`/`styles.css` skin. All 58 commits containing the finished mobile engine build exist only on the local `mobile-ui-overhaul` branch, which has never been pushed to `origin` (`git ls-remote` shows only `main` on the remote).
**Context:** Found answering a direct question — "how many changes were committed and where is the game deployed."
**Implication:** Don't assume the live site reflects any `mobile/` work until `mobile-ui-overhaul` is actually pushed and merged. User declined to push/merge for now.

## 2026-07-23 — Phone-to-PC LAN dev server access needs both a firewall rule AND a Private network category — neither alone was sufficient
**Finding:** `npm run serve`'s Node server (bound to `0.0.0.0:8934`, confirmed via `netstat`) was unreachable from an iPhone on the same Wi-Fi even after adding an explicit Windows Firewall inbound-allow rule for the port. Root cause: the Wi-Fi adapter was classified **Public** in Windows (`Get-NetConnectionProfile`), and Public-profile implicit restrictions (beyond just the specific port) kept blocking the connection regardless of the explicit allow rule. Fixing it required also running `Set-NetConnectionProfile -InterfaceAlias "Wi-Fi" -NetworkCategory Private`.
**Context:** Debugging "Safari couldn't connect to server" across several turns — ruled out wrong IP, no firewall prompt appearing, third-party AV/firewall (none installed, only Windows Defender), VPN interference (none running), and router client/AP isolation (same-SSID confirmed) before finding the Public-profile cause.
**Implication:** For any future local-dev-server-on-phone session on this machine, check/set the network category to Private first — a port-specific firewall rule alone is not enough while the network stays classified Public.

## 2026-07-23 — mobile/index.html cannot be published as a self-contained Claude Artifact without a bundling step, unlike the frozen pme-mobile-sheet.html prototype
**Finding:** `pme-mobile-sheet.html` worked instantly as an Artifact because it's fully self-contained (inline CSS/JS, no external fetches) — Anthropic's Artifact hosting served it directly, no local network involved. `mobile/index.html` is structurally different: it loads `engine.js`/`game.js`/`main.js` as separate `<script src>` files, and `game.js` `fetch()`s JSON data files (`states_data.json`, `policy-tags.json`, `politicians-data.json`, `game-config.json`) from disk at runtime via a `basePath` — none of which an Artifact's sandbox can resolve.
**Context:** User asked why testing the earlier prototype "just worked" with no server/networking setup, while the real mobile build required all the local-server/firewall/network troubleshooting above.
**Implication:** If an Artifact-hosted version of the real engine is ever wanted, it needs a one-time bundling step (inline the three JS files, inline the four JSON files as embedded JS objects, shim `fetch` to serve them from memory). User explicitly rejected this approach for now and asked for direct local-server access instead — a bundling script was drafted but not applied.

## 2026-07-23 — The plan.md-flagged stale-region-field bug (NortheastIndia/BorderLands) is already fully fixed in data files; only legacy desktop JS still references the old names
**Finding:** `data/states_data.json` and `data/policy-tags.json` contain zero references to the retired `NortheastIndia`/`BorderLands` field names — both already fully migrated to `EasternBorder`/`WesternBorder`. The still-open part of this bug is isolated to legacy desktop code (`campaign-system.js`, `state-manager.js`), already tracked in this file's 2026-07-22 entries.
**Context:** Verified while auditing `design/plan.md` for any content not yet captured elsewhere, before deleting it as part of a documentation consolidation into `design/economy-status-map.md`.
**Implication:** No new action needed — existing 2026-07-22 findings-file tracking of the legacy-code side of this bug remains sufficient and current.

## 2026-07-23 — mobile/main.js has no options/settings menu at all; the old desktop app.js's version (non-functional stub) is not what's missing
**Finding:** `design/plan.md`'s "options menu is non-functional" gap referred to the old desktop-ported `app.js`, where the UI existed but every card just `console.log`'d. `mobile/main.js` has no Sound/Music/Pause/Help/New-Game menu UI at all — not stubbed, simply absent. By contrast, plan.md's other flagged gaps (AI opponent, start/end screens, action log) are all confirmed built: `mobile/main.js` has a working politician-select screen, a win/loss/hung-parliament end overlay (`showEndOverlay`), and the news ticker already covers the action-log role.
**Context:** Verified while auditing `design/plan.md` against the current `mobile/` build before deleting the file, to confirm nothing relevant would be lost.
**Implication:** Options/settings menu is a genuine remaining gap for `mobile/`, now logged in `design/economy-status-map.md`'s Build status section rather than left only in the (now-deleted) plan.md.

## 2026-07-23 — Legacy desktop config/data readers use hardcoded key names with no fallback defaults
**Finding:** `js/config-manager.js` and `js/data-loader.js` read `data/game-config.json`/`data/politicians-data.json` via direct property access on specific key names (e.g. `config.investmentSystem.baseCostPerSeat`, `config.rallySystem.regularTokenBoost`, `config.playerSettings.startingFunds`) with no fallback values — a missing or renamed key doesn't throw, it silently propagates `undefined`/`NaN` into the desktop build's economy.
**Context:** Discovered while building the real mobile engine (`mobile/engine.js`/`mobile/game.js`) against the decided design in `design/economy-status-map.md`, which requires several numbers (rally boost 5% not 8%, token income 2/phase not 1) that differ from the legacy desktop values already in `game-config.json`.
**Implication:** Rather than renaming/restructuring the existing keys, added a new additive `mobileEconomy` namespace to `game-config.json` for the new engine to read, leaving every legacy key untouched. Any future schema change to these two data files must either preserve the exact legacy key names the desktop reader expects, or explicitly accept (and document) breaking that already-deprecated build.

## 2026-07-23 — Playwright's `devices['iPhone 14']` viewport is 390×664, not 390×844
**Finding:** The preset's `viewport` field is `{width:390, height:664}` — 844 is only the `screen` dimension it also reports (`deviceScaleFactor:3`). A screenshot came out 1170×1992px (i.e. 390×664 at 3x) instead of the expected 390×844.
**Context:** Discovered while visually verifying the new `mobile/index.html` build with Playwright's webkit engine, per this project's existing convention of testing Safari-specific mobile behavior with `devices['iPhone 14']`.
**Implication:** 664px, not 844px, is the usable content height this device profile actually models (approximating real Safari's chrome-reduced viewport) — relevant when judging whether Booth Ink's ~755–765px fixed chrome will fit, and a reminder that headless Playwright/webkit still isn't a perfect stand-in for real-device Safari on this specific viewport-fallback behavior; a real device or Capacitor-wrapped build remains the final check.

## 2026-07-23 — data/policy-tags.json and politicians-data.json wrap content in a named top-level key; states_data.json has a leading BOM
**Finding:** `policy-tags.json`'s real content is under a `policyTags` key (not the file root); `politicians-data.json`'s is under `politicians`; `states_data.json` starts with a UTF-8 BOM (`﻿`) that breaks `JSON.parse` if read raw via Node's `fs`.
**Context:** Discovered writing `mobile/game.js`'s data loader and a one-off Node inspection script — the browser's `fetch().json()` path silently tolerates the BOM, which is why this had never surfaced before in any browser-side code.
**Implication:** Any future script reading these three files directly via `fs.readFileSync` (rather than through `fetch`) must strip a possible leading BOM and unwrap the named top-level key before parsing/using the content — `mobile/game.js`'s `stripBOM()` + `normalizeGameData()` now does this once, centrally, for both the browser and Node (test) code paths.

## 2026-07-23 — Seat-conversion rounding (state seats = round(popularity% × seats)) can over-allocate a state's total seats
**Finding:** Computing each player's seat share independently via plain rounding has no reconciliation step, unlike bps popularity redistribution (which guarantees the three shares always sum exactly). Worst case demonstrated: a 3-seat state at P1 50% / P2 25% / Others 25% gives exact quotas of 1.5 / 0.75 / 0.75 — independent rounding gives 2 / 1 / 1, handing out 4 seats from a state that only has 3.
**Context:** Found during a requested structural review of `design/economy-status-map.md`, by analogy to the redistribution engine's own "round one side, derive the other" discipline — the seat-conversion step never got the same treatment.
**Implication:** Fixed by switching to largest-remainder apportionment (Hamilton's method): give each side `floor(quota)` first, then hand out the state's leftover seats one at a time to whoever has the largest fractional remainder. Now documented with pseudocode and a worked example in the Core loop section. Should be re-validated against the plausibility-check seat totals once implemented, since it can shift exact seat counts in edge cases (though not the shape of the ~19-seat passive-opponent margin finding).

## 2026-07-23 — Agenda effect timing (per-tap scaling vs. completion-gated) was never specified, despite every adjacent formula being fully specified
**Finding:** The Agenda section fully specified cost-per-tap (500 Cr), completion-per-tap (25%), and the full-completion effect magnitude (the seat-equivalent ranking), but never stated whether the popularity effect applies prorated per tap or only once the agenda hits 100% completion — a load-bearing gap, since it decides whether a partially-funded agenda has any value at all.
**Context:** Found during the same structural review, cross-referencing the Agenda section's implementation notes against its own "hoarding is a real strategy" worked example, which implied prorated timing but had no formula backing it.
**Implication:** Decided: each of the 4 taps applies exactly 1/4 of the state's net effect immediately, through the redistribution engine — mechanically identical to how investment taps already work (a sequence of discrete, redistribution-capped boosts), just flat instead of decaying. Now documented with pseudocode in the Agenda section's implementation notes.

## 2026-07-23 — Kejriwal's "fixed" Anti-Corruption Raid wording was ambiguous enough to misread as a zero-cost power
**Finding:** The fix wording ("Robs the opponent of 50% of their cash on hand, at a fixed cost of −15% popularity in the opponent's home state") reads on first pass as two effects both hurting the opponent — i.e. no real sacrifice by the activating player, the exact bug pattern already fixed for Tendulkar/Hema Malini. User clarified the intended reading: the −15% is the activating player's own popularity share, lost specifically in the state that happens to be the opponent's home turf — a genuine sacrifice, not a second benefit.
**Context:** Found during the same structural review of the Special Powers section.
**Implication:** Wording tightened in `design/economy-status-map.md` to state explicitly that the cost is paid by the activating player. No behavior change — this was a documentation clarity issue, not an actual design bug.

## 2026-07-22 — Six special powers reworked; each had a genuinely different flaw, not one repeated bug
**Finding:** Auditing the 20-entry special-power roster per the open item flagging Rajinikanth by name. Six powers turned out broken in six distinct ways: Tendulkar's National Icon and Hema Malini's Star Power Rally both had a "cost" that was actually a timing gate or eligibility condition (free whenever usable, same bug class as pre-fix Education's zero-oppose-tags problem). Rajinikanth's Thalaivar Announcement paired an undefined "massive" benefit with a uniquely severe permanent cost (locking all 4 remaining agendas for the rest of the game). Kejriwal's Anti-Corruption Raid had a cost tied to the player's own current token holdings, gameable to zero by spending down first. Nitish Kumar's Alliance Switch and Nehru's Non-Alignment both had value entirely contingent on the opponent's prior actions, capable of being worth nothing depending on timing.
**Context:** Requested directly after the agenda-tag rebalancing work, moving to the next flagged open item (special-power balance).
**Implication:** All six now have concrete effects and costs (see `design/economy-status-map.md`'s Special Powers section for the full before/after table) — Fixed cash+percentage numbers were sanity-checked against Vajpayee's Pokhran Test (+10% nationwide, reviewed and deliberately kept as an accepted outlier/reference point) so the roster doesn't quietly grow a second unchecked outlier while fixing the first six. Nehru's power is now the one deliberate exception to the "matched cost + benefit" rule — zero direct cost, by explicit decision, since its own risk/uncertainty structure (silent block vs. guaranteed 2,000 Cr fallback) already provides the balancing tension. **Update 2026-07-23:** re-confirmed after review — the variable, opponent-contingent payoff is treated as the power's cost in place of a resource sacrifice. 13 politicians' powers remain unaudited, most missing only a magnitude number rather than having a structural flaw.

## 2026-07-22 — Hindi Language had the exact same accidental-cancellation bug as pre-fix National Defense; Digital Transformation was genuinely under-supported, not buggy
**Finding:** Auditing the remaining net-negative agendas per the open item tracking this. Hindi Language (−16.8): Uttar Pradesh (80 seats) and Bihar (40 seats), its two biggest natural `HindiHeartland` supporters, were also tagged `EasternBorder` — which Hindi Language's own `opposeTags` included — canceling their support to exactly zero. Same failure pattern as National Defense pre-fix (a support state accidentally double-tagged with an unrelated oppose region). Digital Transformation (−7.5) had no such accidental cancellation — it was a real support/oppose count imbalance: 2 support tags (`Education`, `IndustrialCorridor`) rarely co-occur in the same state, while 3 oppose tags (`AgriculturalRegion`, `Pilgrimage`, `TribalLands`) often do (Bihar, Andhra Pradesh, Odisha, Punjab each carry two at once).
**Context:** Requested directly after the Economic Liberalization/Education fix, checking whether other negative-ranked policies had the same class of problem.
**Implication:** Fixes applied and verified with `recompute_policy_ranking.js`: Hindi Language's `EasternBorder` reduced from −8 to −4 (−16.8 → −8.9); Digital Transformation's two support tags raised from 4 to 8 (−7.5 → +10.2, flips positive). 8 of 24 policies remain net-negative (down from 9): Land Reforms, Public Sector, Uniform Civil Code, Caste Reservation, Indigenous Rights, Agricultural Reforms, and Secularism haven't had this audit yet.

## 2026-07-22 — Economic Liberalization and Education were the pool's two biggest outliers for a tag-design reason, not a magnitude reason; fixing them shrank the plausibility margin from ~61 to ~19 seats
**Finding:** Ranked #1 (+51.8) and #2 (+49.4), these two weren't just "strong" — they structurally violated the tradeoff pattern every other policy follows. Education had zero oppose tags (pure upside, no losing constituency anywhere). Economic Liberalization had three support tags that co-occur in the same big industrial states (Gujarat carried all three at once, netting +36 alone) against one oppose tag that barely overlapped those states (only Bihar/Punjab penalized). Fix: added `TribalLands: -8` / `NaturalResources: -8` to Economic Liberalization alongside the existing `AgriculturalRegion: -12` (new total +26.2); reduced both of Education's tags from 12 to 8 since it has no real thematic opposition to invent (new total +33.0). Verified with `recompute_policy_ranking.js`.
**Context:** Requested directly after the `tagEffects` schema migration, while looking for what "hugely imbalanced" meant in the newly-migrated ranking.
**Implication:** The plausibility section's combined-strategy scenario used these two policies (+101.2 combined) as its "2 strong agendas" case — with the fix, that drops to +59.2, pulling the modeled best-case seat total from ~333/543 down to ~291/543, and the margin over the 272-seat majority threshold from ~61 seats down to ~19. This wasn't a deliberate margin-tuning move, it's a side effect of fixing a real design bug — but it means the "wide, comfortable passive-opponent margin" this document confirmed as intentional earlier today no longer holds at the same width. Worth revisiting once the two-active-player case (already tracked as open) gets modeled.

## 2026-07-22 — campaign-system.js now references retired policy-tags.json fields (tier, baseMagnitude, supportTags, opposeTags)
**Finding:** `data/policy-tags.json` migrated to the `tagEffects` schema (one signed magnitude per region, replacing `supportTags`/`opposeTags`/`baseMagnitude`, with `tier` dropped entirely now that magnitude is picked per-region instead of once per policy — see `design/economy-status-map.md`'s Agenda section). `js/campaign-system.js`'s `generateCampaignGrid()` is the only code in the repo that read the old fields — it sorts by `tier`, assigns a `tier-N` CSS class, computes tap cost from a `tier`-based multiplier (already flagged elsewhere as contradicting the decided flat 500 Cr/tap cost), and builds support/oppose UI labels from `supportTags`/`opposeTags`/`baseMagnitude`. All of these now read `undefined`.
**Context:** Found while migrating `policy-tags.json` to the new per-region `tagEffects` shape; `check_data_consistency.js` doesn't catch this class of break (it only validates state-group field names, not this file's own JS-level field usage).
**Implication:** `campaign-system.js` is the legacy desktop-ported display code (not Booth Ink, the committed mobile UI direction), already documented as known broken/incomplete and slated for rewiring — following the same precedent as the existing `BorderLands`/`NortheastIndia` stale references, this is left as tracked debt rather than patched now. Whoever next touches this file's campaign grid should: drop the tier-based sort/CSS class, switch to the flat 500 Cr cost, and read `tagEffects`/`nationwideBonus` for the support/oppose labels instead.

## 2026-07-22 — data/policy-tags.json defines 24 policies, not 23; Privatization is orphaned
**Finding:** The data file defines 24 policy entries, but the design doc has referred to "the 23-policy pool" throughout. Cross-referencing every entry against all 20 politicians' assigned `policies` arrays in `politicians-data.json` shows 23 of the 24 are drawn by at least one politician — only **Privatization** (tier 3, +7.0 seat-equivalent, a real positive value) is never assigned to anyone, making it the sole unreachable entry.
**Context:** Found while computing a full seat-equivalent ranking of every policy against the real map, requested to resolve the "full re-ranking" open item.
**Implication:** "23-policy pool" elsewhere in the doc is accurate as a description of what's actually reachable in play; the data file just carries one dead extra. Needs a decision — assign Privatization to a politician, or delete it — tracked as open in `design/economy-status-map.md`. **Update 2026-07-22:** resolved, assigned to Vajpayee.

## 2026-07-22 — Post the National Defense fix, Secularism (−20.3) is now the single worst-ranked policy, not National Defense
**Finding:** Full 24-policy seat-equivalent computation against the real map: range is +51.8 (Economic Liberalization, best) to −20.3 (Secularism, worst). National Defense, the prior worst pick pre-fix (−25.3), now sits at rank 14 (+1.8) after its `opposeTags` correction — nobody had checked whether a different policy was quietly worse once that fix landed.
**Context:** Same full-ranking computation as above.
**Implication:** Secularism, and 8 other net-negative policies found in the same pass (Land Reforms, Public Sector, Digital Transformation, Uniform Civil Code, Caste Reservation, Indigenous Rights, Agricultural Reforms, Hindi Language), haven't had an individual audit for accidental tag-overlap (the National Defense failure pattern) vs. deliberate regional polarization (confirmed by the user as thematically legitimate on its own, not automatically a bug). Tracked as an open audit item.

## 2026-07-22 — 10 of the 20 politicians in politicians-data.json share Uttar Pradesh as homeState
**Finding:** Checked directly against the roster: exactly half of the 20 politicians (Rahul Gandhi, Atal Bihari Vajpayee, Yogi Adityanath, Indira Gandhi, Jawaharlal Nehru, Lal Bahadur Shastri, Rajiv Gandhi, Amitabh Bachchan, Hema Malini) list Uttar Pradesh as their `homeState`.
**Context:** Computed while estimating the expected value of the newly-redesigned random starting-position generator, for a plausibility recompute.
**Implication:** The home-state-tie-nullification rule (both players' politicians sharing a home state → bonus cancels for both, doesn't split) will trigger commonly in real matches, not as a rare edge case. Confirmed by the user as a deliberate stabilizer — it makes the "mega lead from a home-state pick alone" scenario rare on the country's single biggest state (80 seats), not a defect to fix.

## 2026-07-22 — campaign-system.js's generateCampaignGrid() uses a cost formula that contradicts the decided flat 500 Cr/tap agenda cost, and reads UI-display fields that will go stale once the agenda-effect formula migrates
**Finding:** `generateCampaignGrid()` computes agenda tap cost as `baseMagnitude × tierMultiplier` (tierMultiplier 15/15/22.5 by tier) — not the flat 500 Cr/tap already decided in `design/economy-status-map.md`. It also reads `supportTags`/`opposeTags`/`baseMagnitude` directly for its region-effect labels, which will produce wrong output once the effect formula migrates to per-region `tagEffects` (see the tier-redesign decision, same date).
**Context:** Found while assessing consequences of a (later reverted, at user's request) schema migration for the agenda effect formula.
**Implication:** Confirmed this file belongs to the old desktop-ported skin (`index.html`), which CLAUDE.md already documents as superseded by Booth Ink for real mobile UI work — so this document treats fixing it as out of scope rather than part of the eventual migration's critical path. Flagged so the stale labels aren't a surprise later.

## 2026-07-22 — js/rally-system.js's "special" rally token is dead code, disconnected from the live game-config.json
**Finding:** `useSpecialRallyToken()` reads `rallyConfig.specialRallyBoost` and `specialTokenProbability` — neither field exists anywhere in `data/game-config.json`. This is desktop-era code (flat nationwide boost, 5% random chance per phase of receiving one) that the current design's "Nationwide Rally" token-craft mechanic explicitly supersedes ("replaces the old random ⭐ roll entirely").
**Context:** Checked while researching what magnitude to assign the newly-designed Nationwide Rally craft item, to see if any existing precedent value should carry over.
**Implication:** None of this file's values are live or should be treated as a design reference — it only explains the historical lineage of why Nationwide Rally is described as "nationwide" in the first place.

## 2026-07-22 — Recomputing plausibility with the now-decided rally values widened the best-case majority margin from ~6 to ~61 seats
**Finding:** Re-deriving the "cash + tokens + 2 agendas" plausibility scenario with the newly-decided values (5% per rally token, 5% Nationwide Rally, the exact investment decay formula) gives ~333/543 (61.3%), not the prior ~278/543 (51.2%) — a jump from a ~6-seat margin over the 272 threshold to ~61 seats. Driven roughly equally by three things: (1) discovering the seat-optimal token strategy crafts a Nationwide Rally instead of spending all 20 tokens individually (+48 vs +38 seat-equivalent, since the craft reaches states individual per-state caps can't touch), (2) a more rigorous cash-contribution recompute (~42 vs. a prior ~21-seat estimate) now that the exact decay formula is documented, and (3) a slightly higher expected starting position under the new randomized generator (~142 vs. the old fixed table's ~135).
**Context:** Requested by the user specifically to update the token-value dependency flagged when the 5% figures were decided; ended up touching cash and starting-position estimates too since accurate recomputation required them.
**Implication:** Confirmed by the user as intended design, not a balance problem — a real adversarial two-player match (still unmodeled) is expected to compress this margin back toward 272, rather than the passive-opponent ceiling needing to stay razor-thin on its own. The "Still open" two-active-player-ceiling item in `design/economy-status-map.md` was updated to reflect ~61 seats as the new baseline to watch, replacing the stale ~6-seat figure. **Update 2026-07-22:** superseded again — see the Economic Liberalization/Education fix entry above, margin now ~19.

## 2026-07-22 — Rally per-state cap is configured but never enforced in the live code path
**Finding:** `data/game-config.json` sets `maxRalliesPerState: 2`, and one implementation of the rally system checks it (`handleRallyClick()` in `campaign-system.js`) — but that implementation is dead code, never called from anywhere. The version a player actually taps, `useSimpleRallyToken()` in `rally-system.js`, never checks a per-state limit at all. Since a player's simple token fully refills every phase, nothing stops rallying the exact same state every phase for the whole game.
**Context:** Found while auditing the rally system for a game-design analysis piece; verified by reading both rally implementations and confirming `useSimpleRallyToken()`'s call chain has no cap check anywhere.
**Implication:** Of all the rally-system gaps found this session, this is the one a player could actually feel and exploit mid-match, not just a books-balance oddity. Fix needs the cap check added to the live function, not just the dead one.

## 2026-07-22 — Three colliding definitions of resetRallyTokensForPhase(); live behavior is decided by script load order, not a decision
**Finding:** `resetRallyTokensForPhase()` is defined in both `player-manager.js` and `rally-system.js` as two genuinely different designs — the player-manager version hands out a *guaranteed* special token every phase, the rally-system version gives one on a 5% roll. Both files are loaded as plain global `<script>` tags; since rally-system.js loads after player-manager.js in `index.html` (lines 154/156), its declaration silently wins. `game-data.js` also re-exports `window.resetRallyTokensForPhase` but doesn't declare its own version, so it just re-confirms whichever declaration already won.
**Context:** Found while tracing rally-token reset logic for a game-design analysis piece, after noticing `player-manager.js`'s version looked live but was never actually reachable.
**Implication:** Nobody decided the 5%-roll version should be the live one — it's an accident of file ordering in `index.html`. Neither version is marked as retired, so the shadowed one will keep looking like a valid candidate to the next person reading the code.

## 2026-07-22 — A fully dead rally implementation (8% boost, own token pool) still matches the UI's own on-screen instructions
**Finding:** `campaign-system.js`'s `handleRallyClick()` + its module-scoped `rallyTokens` object is a complete, independent rally system (8% boost, hardcoded 2-token pool, per-state cap check) that is never called from anywhere else in the codebase. The live path a player actually reaches — `rally-system.js`'s tray, via `useSimpleRallyToken()` — uses a 4% boost instead. The campaign grid's own instructional text (`generateCampaignGrid()`) still says "Alt+Click for rallies," describing behavior that no longer matches what actually happens (Alt+Click on the UT buttons/map correctly routes to the live 4% tray, not the dead 8% system the copy seems to describe).
**Context:** Found while cross-referencing `game-config.json`'s two rally-boost values (`popularityBoost: 8` vs `regularTokenBoost: 4`) against which functions actually read each one.
**Implication:** The dead system and its stale on-screen copy should either be removed together or reconciled — whichever is decided, both currently point to a rally-boost economy that isn't the one live.

## 2026-07-22 — simulatePopularityChanges() is an uncapturable, never-cleared random-drift timer that contradicts the project's own anti-randomization design decisions
**Finding:** `popularity-manager.js`'s `simulatePopularityChanges()` starts a `setInterval` (every 20 seconds) that nudges 3 random states by a random ±4 points, attributed to a randomly chosen player. It's invoked unconditionally from two separate places (`app.js:457` and `game-data.js:17`), and its interval ID is never stored to a variable, so it can never be cleared — not by `endGame()`, not by anything.
**Context:** Found while checking whether any live randomization systems still existed, after the project's own docs describe deliberately removing exactly this "magnitude randomization dressed as variety" pattern from random events, rally-token odds, and the home-state bonus.
**Implication:** This is a fifth instance of the exact pattern the project already diagnosed and removed elsewhere — it was apparently missed by that earlier audit. Worth deciding whether to delete it outright or gate it behind an explicit setting; either way it's currently running, unnoticed, in production.

## 2026-07-22 — The existing National Defense finding (2026-07-21) understated the self-cancellation: 6 states cancel, not 4, and Himachal Pradesh isn't one of them
**Finding:** Recomputing National Defense's effect directly against `states_data.json` (supports `EasternBorder`/`WesternBorder`, opposed `CoastalIndia`/`HindiHeartland`, `baseMagnitude` 12) shows six states — Uttar Pradesh, Bihar, Gujarat, Rajasthan, Uttarakhand, and West Bengal — net to exactly zero because they carry both a supported and an opposed tag at once. Himachal Pradesh, previously named as one of the four canceling states in the 2026-07-21 finding, actually nets **+1** (it has both `EasternBorder` and `WesternBorder` as support, only one oppose tag), not zero. Full computation: net national effect is −25.3 seat-equivalent, matching the earlier finding's headline number exactly even though its illustrative state list was wrong.
**Context:** Verifying the earlier finding's specifics while explaining National Defense's imbalance in detail; ran the actual per-state math rather than trusting the prior finding's summary at face value.
**Implication:** The earlier finding's aggregate number was reliable; its supporting detail wasn't. Worth treating any finding's specific illustrative examples as worth re-deriving before quoting them further, even when the headline number checks out.

## 2026-07-22 — state-manager.js also references the retired BorderLands field, in addition to campaign-system.js's already-documented NortheastIndia reference
**Finding:** Running `check_data_consistency.js` surfaces a reference to `BorderLands` in `state-manager.js` (property access), on top of the already-known `NortheastIndia` references in `campaign-system.js` (string literal) and `state-manager.js` (property access). Both fields were retired in the 15-group rebalance (replaced by `EasternBorder`/`WesternBorder`) but never fully removed from the code that reads them.
**Context:** Ran the consistency checker after editing `policy-tags.json` (National Defense fix, nationwideBonus field) to confirm the edits introduced no new problems — they didn't, but the checker's pre-existing failure list included this previously-uncaught `BorderLands` reference alongside the known `NortheastIndia` one.
**Implication:** The stale-field cleanup needed in `state-manager.js` is slightly bigger than previously documented — both retired fields need removing there, not just one.

## 2026-07-22 — Campaign grid's display code implies a baseMagnitude%-nationwide convention that would overshoot the newly-decided +2% bonus by ~6x
**Finding:** `generateCampaignGrid()` in `campaign-system.js` already labels a zero-tag policy as "+`baseMagnitude`% Nationwide" for display purposes (pure UI text, no effect attached). Women's Empowerment and Anti-Corruption have `baseMagnitude: 8`, Healthcare has `baseMagnitude: 12`. The actual decided design for these three policies is a flat +2% nationwide effect (recorded today as an explicit `nationwideBonus` field). If a future implementation reached for `baseMagnitude` instead of the new field, Healthcare would compute to ~+65 seat-equivalent (stronger than every other policy in the 23-policy pool, including the current best at +51.8) and the other two to ~+43 each.
**Context:** Cross-referencing the display label logic against the newly-decided nationwide-bonus value while documenting the fix in `design/economy-status-map.md`.
**Implication:** `nationwideBonus`, not `baseMagnitude`, must be what any future effect-calculation implementation reads for these three policies — flagged explicitly in `design/economy-status-map.md` so this isn't rediscovered the hard way during implementation.

## 2026-07-21 — Real starting-position breakdown: ~54% of the map is genuinely contested, not player-vs-player
**Finding:** `popularity-manager.js`'s actual init algorithm gives Player 1 a 2-state stronghold (Uttar Pradesh + Maharashtra, 128 seats), Player 2 a 3-state stronghold (West Bengal + Bihar + Tamil Nadu, 121 seats), and leaves the remaining 31 states (294 seats, 54% of all 543) as "competitive" territory where both players start low (5–29%) and "Others" dominates. Expected baseline seat share at game start: P1 ≈24.6%, P2 ≈24.3%, Others ≈51.1%.
**Context:** Computed against live `data/states_data.json` while modeling whether the decided economy numbers make the game winnable and how adversarial the early game actually is.
**Implication:** Most of the early game is a race to harvest the uncommitted "Others" pool, not direct player-vs-player combat — real zero-sum friction concentrates in the shared competitive-state pool and in attacks on each other's strongholds, not everywhere. **Superseded 2026-07-22** — the fixed stronghold table has been replaced by the randomized starting-position generator.

## 2026-07-21 — Direct cash investment alone cannot win the game under the decided numbers
**Finding:** Simulating the real decay curve against real state-size data, the theoretical best case for cash-only play (full 12,500 lifetime budget, spent optimally spread across all states, opponent does nothing) tops out at ~195/543 seats (35.9%) — well short of the 272-seat majority (50.1%) needed to win.
**Context:** Modeled while assessing whether the newly-decided base economy numbers (2,500 start / 1,000 per phase / seats×10 cost) make the game plausibly winnable.
**Implication:** Rally tokens and agendas are mathematically required to reach a majority under current numbers, not optional flavor layered on top of cash investment — the map-tapping mechanic alone is not a viable win path.

## 2026-07-21 — Agenda seat-equivalent value swings from +51.8 to −25.3 across the real 23-policy pool; 3 policies currently do nothing
**Finding:** Computing `baseMagnitude × Σ(matching support tags − matching oppose tags)` against every real state in `data/states_data.json`, national seat-equivalent value ranges from Economic Liberalization (+51.8, best) to National Defense (−25.3, actively harmful if picked) across the 23 policies in `data/policy-tags.json`. Women's Empowerment, Healthcare, and Anti-Corruption have zero support/oppose tags configured, so completing them currently applies no popularity effect at all under the decided formula.
**Context:** Computed while auditing whether the game is fairly winnable regardless of which of the 20 politicians a player picks, given each is locked into 4 specific agendas.
**Implication:** A single agenda slot can swing ~77 seats between best and worst pick — larger than the ~6-seat margin by which a combined cash+token+2-agenda strategy crosses the majority threshold (see the next finding). Politician/agenda-pick quality is likely a bigger determinant of outcome than player skill under current numbers. The three zero-tag policies are likely an oversight (old `policy-popularity-calculator.js` had a "no tags → flat nationwide bonus" fallback the new formula doesn't implement). Not yet audited: which of the 20 roster politicians got assigned these dead or negative-value agendas. **Update 2026-07-22:** both issues addressed — see the two entries above dated 2026-07-22 for the National Defense fix and the nationwideBonus field added for the three zero-tag policies.

## 2026-07-21 — A combined cash+token+2-agenda strategy just barely crosses the majority threshold (~278/543)
**Finding:** Adding free rally tokens (+20.6 seat-equiv from 20 base tokens on the biggest states) and two strong agendas (Economic Liberalization + Education, +101.2 combined) on top of a reduced cash allocation reaches ~278/543 seats (51.2%) — a majority, with only ~6 seats to spare.
**Context:** Built as the realistic "best case that isn't cash-only" scenario, following the cash-only ceiling finding above.
**Implication:** The game is winnable, but the margin is thin and highly sensitive to agenda-pick quality (see above) — worth confirming the actual 20-politician roster doesn't leave any politician mathematically unwinnable. **Superseded** — see the 2026-07-22 recompute entries above; the margin has moved twice since this finding (~6 → ~61 → ~19).

## 2026-07-21 — Group-dominance payout formula was a 10x-too-small bug; even corrected, cost to dominate any group exceeds the entire lifetime budget
**Finding:** The payout formula was first documented as `0.5 × Σseats-in-group` (matching desktop's `group-rewards.js` literally), but the actual intended formula ties the 0.5 multiplier to the *cost* of a tap on the group treated as one unit: `0.5 × (seats × 10) = 5 × seats` — a 10x correction, confirmed by the user via a worked Maharashtra example. Even with the correction, computing the real cash cost to push every member state of a group to 50% (using the real decay curve and real starting positions) shows every single one of the 15 dominable groups costs more to actually dominate via direct cash than the entire 12,500 lifetime budget — e.g. Agricultural Region costs ~12,480 Cr to dominate for a corrected payout of only 1,365 Cr (~11% ROI).
**Context:** Surfaced when the user walked through a concrete cost-vs-payout example and found the numbers "wildly wrong," then clarified the intended formula.
**Implication:** Fixed the formula shape for whenever group dominance is implemented/ported to mobile (`campaign-system.js`'s `checkRegionalDominanceBonuses()` is stale per the existing 2026-07-21 finding below and doesn't implement any payout yet). Group dominance should be understood and communicated as a bonus on states already being pushed to 50% for the national seat-share race — never as a standalone cash-ROI target, even at the corrected magnitude.

## 2026-07-21 — Rally token system has undocumented caps: 2/phase spend, 2/state lifetime, and a real ceiling of 20 usable tokens (not 28)
**Finding:** Clarified directly by the user: token spending (whether an individual per-state rally play or converting tokens into a Special Powerup/Nationwide Rally craft) is capped at 2 per phase per player, and separately capped at 2 total plays per state for the whole game (shared between both players, Nationwide Rally exempt). Since income is also 2/phase, and the spend cap matches it exactly, the real hard ceiling on tokens ever *usable* across a 10-phase game is 20 (2×10) — regardless of the up-to-28 (20 base + 8 agenda bonus) that can be *earned*. The +8 agenda-completion bonus tokens don't raise this ceiling; they only give scheduling flexibility in when the same 20 get spent, and can go permanently unused if a player already spends their base income at the cap every phase.
**Context:** None of these caps were previously documented anywhere in the project. Surfaced through several rounds of direct clarification after incorrect assumptions (tokens unlimited per state; crafting exempt from the per-phase cap; crafting and base income as separate pools) were each proposed and corrected by the user.
**Implication:** Crafting a Special Powerup (6 tokens) needs a minimum of 3 phases of dedicated commitment; a Nationwide Rally (12 tokens) needs a minimum of 6 — meaning it must be started almost immediately in a 10-phase game or it can't be finished in time. This is one shared token pool, not separate tracks for base vs. bonus tokens. **Update 2026-07-23:** the per-state cap being a *shared* (not per-player) pool was confirmed as a deliberate, symmetric denial-play option, not an oversight.

## 2026-07-21 — phase-system.js had its own independent, drifted config loader; fixed to share config-manager.js's
**Finding:** `js/phase-system.js` fetched `data/game-config.json` independently of the rest of the app, with a hardcoded fallback default that had drifted to 500/phase against the real live config value of 1000/phase.
**Context:** User asked to fix the fallback-default inconsistency; investigation found the root cause was two entirely separate config-loading systems in the codebase — `phase-system.js`'s own fetch vs. the shared `config-manager.js` already used by `campaign-system.js`, `player-manager.js`, `rally-system.js`, and `investment-system.js`.
**Implication:** Fixed at the root, not just the number: deleted `phase-system.js`'s independent config-loading system entirely; it now calls the shared `getGameConfig()` from `config-manager.js`, same as every other module. One config loader in the codebase now, not two that could silently drift apart again.

## 2026-07-21 — totalPhases and base economy scale conflicts (flagged earlier this session) are now resolved by decision
**Finding:** The `totalPhases: 8 vs 10` conflict (see finding below) is resolved: 10 is canonical. `design/plan.md`'s "8" references and Booth Ink's UI still need updating to match — not yet done. The desktop-vs-mobile price-scale mismatch (see the `seats × 1 vs seats × 10` and `50% of seats` findings below) is also resolved: mobile keeps its own numbers (2,500 start / 1,000 per phase / seats×10 cost) and stops comparing to desktop's scale entirely — desktop is no longer treated as the numeric reference for base economy scale, though other formula *shapes* (the redistribution rule, the agenda effect formula) were separately confirmed to still match desktop's.
**Context:** Direct user decisions during this session's economy-plausibility review.
**Implication:** Any future economy work should treat these as closed, not reopen the desktop-comparison question — see `design/economy-status-map.md` for the full current formula set.

## 2026-07-21 — Desktop's real direct-investment cost is seats × 1 (millions), not seats × 10
**Finding:** `campaign-spending.js`'s `calculateBaseCost()` on the desktop app computes cost as `seats × 1`, in millions — a full order of magnitude different from mobile's `data/game-config.json` assumption of `seats × 10` (crores).
**Context:** Auditing the in-game economy per explicit instruction to use the desktop app as reference, not mobile's ported/known-broken code.
**Implication:** Any cost/budget tuning should compare cost-to-lifetime-budget ratios, not raw numbers, when reconciling desktop's and mobile's differently-scaled economies. (Superseded 2026-07-21 — see the resolution finding above: mobile's own scale is now canonical, this comparison is closed.)

## 2026-07-21 — Desktop's home-state bonus is both a cost discount and a flat popularity floor, applied together
**Finding:** `home-state-bonus.js` applies a 20% discount on every campaign cost (`getCampaignCost`) **and** a separate one-time flat +20 percentage-point popularity floor the moment the home state is touched (`applyHomeStateBonus`) — not a per-tap scaling multiplier, and not an either/or choice.
**Context:** Read directly while resolving a design question about whether a politician's regional affinity should discount cost or boost effect.
**Implication:** Desktop's actual precedent combines both mechanisms simultaneously; worth knowing before finalizing mobile's politician-state affinity design.

## 2026-07-21 — Desktop's popularity-gain-per-tap decay formula
**Finding:** Each campaign tap gives `5% × max(0.8, 1 − cumulativeSpendInState × 0.005)` — a mild decay toward an 80%-of-base floor as more money is spent in that specific state, confirmed in `state-info.js`'s `recordStateAction()`.
**Context:** Needed the real formula to model cost-to-dominate accurately for an interactive economy-balance tool.
**Implication:** This is now the reference decay curve used in that tool; any future mobile port of investment decay should match it rather than inventing a new one. (Superseded 2026-07-21 — decided to keep mobile's own existing linear-glide curve instead; see the resolution finding above.)

## 2026-07-21 — Desktop's regional dominance bonus pays 50% of the group's seats, plus recurring carry-forward
**Finding:** `group-rewards.js`'s `awardGroupDominationBonus()` pays `Math.round(totalSeats * 0.5)` in currency once dominance is first achieved, then repeats the same award as a carry-forward bonus while dominance is maintained.
**Context:** Same economy audit, reading `group-rewards.js` and `state-groups.js` directly.
**Implication:** Far more generous than mobile's flat 200+50/phase bonus — worth aligning mobile's numbers to this scale since desktop is the intended reference. (Superseded 2026-07-21 — mobile stopped comparing to desktop's scale; and this exact `0.5 × seats` formula was found to be a 10x-too-small bug when computing real ROI — see the group-dominance-payout finding above for the corrected formula.)

## 2026-07-21 — Desktop's "every state in group must individually exceed 50%" rule is intentional, not a mobile bug
**Finding:** `state-groups.js`'s `checkGroupDomination()` requires `p1DominatingStates === states.length` — every single state in the group individually at ≥50%, not a seat-weighted average. Mobile's ported version uses the identical strict rule.
**Context:** Checked desktop's real threshold logic after finding mobile's version used the same rule, to confirm whether it was a porting bug.
**Implication:** This strictness is desktop's actual design; balance work should treat it as intentional rather than something to loosen without deliberate reason.

## 2026-07-21 — Mobile's regional-dominance check is stale and covers only 4 of 15 real groups
**Finding:** `campaign-system.js`'s `checkRegionalDominanceBonuses()` hardcodes only `['SouthIndia', 'HindiHeartland', 'NortheastIndia', 'CoastalIndia']`. `NortheastIndia` was already deleted from `states_data.json` in the 15-group rebalance (retired in favor of Eastern/Western Border) and can never match again; the other 11 of the current 15 groups aren't checked at all.
**Context:** Auditing whether regional dominance bonuses are actually achievable in the current mobile codebase.
**Implication:** This function needs a real rewrite against the current 15-group list before any group beyond South India/Hindi Heartland/Coastal India can ever pay out on mobile.

## 2026-07-21 — Mobile's investAgenda() has zero funds cost, same bug pattern as the pre-fix investIn()
**Finding:** Booth Ink's `investAgenda()` increments agenda progress with no funds check at all, despite `design/plan.md`'s own D2 decision explicitly calling for agenda commitment to cost "one lump sum per phase."
**Context:** Auditing "how much should agendas cost" as part of the in-game economy walkthrough.
**Implication:** Agenda cost-gating needs the same kind of fix `investInPaid()` just received for direct map-tap investment; not yet implemented. (The cost is now decided — 500 Cr/tap, see `design/economy-status-map.md` — but this remains an implementation gap in Booth Ink's code.)

## 2026-07-21 — Desktop already has a working win condition; it's specifically missing from Booth Ink, not the whole project
**Finding:** `js/ui-manager.js`'s `showElectionResults()` implements a real 272/543-seat majority check, wired through `phase-system.js`'s `endGame()`. This contradicts an earlier assumption in this session that no win condition existed anywhere in the codebase.
**Context:** Traced `endGame()`'s call chain while researching how much money it takes to actually win the game.
**Implication:** Porting/adapting this existing logic into Booth Ink is the real Phase 3 task — not writing a win condition from scratch.

## 2026-07-21 — totalPhases is inconsistent: config says 10, design docs and Booth Ink assume 8
**Finding:** `data/game-config.json` and `phase-system.js` both set `totalPhases: 10`, but `design/plan.md`'s roadmap text ("finishing 8 phases shows a result") and Booth Ink's own UI hardcode 8.
**Context:** Computing total lifetime budget (`startingFunds + refreshFundsPerPhase × totalPhases`) for the economy audit surfaced the discrepancy.
**Implication:** Changes total lifetime budget by a full 2 phases' worth of refresh funds (2000Cr at current mobile values) — needs a decision on which number is canonical before finalizing budget-dependent balance work. (Resolved 2026-07-21 — see the resolution finding above: 10 is canonical.)

## 2026-07-21 — National Defense's own support and oppose tags overlap on 4 real states, netting to zero there
**Finding:** National Defense supports `EasternBorder`/`WesternBorder` and opposes `HindiHeartland`/`CoastalIndia` — but Uttar Pradesh, Bihar, Uttarakhand, and Himachal Pradesh are all tagged both `EasternBorder` and `HindiHeartland` simultaneously. Under the agreed sum-every-matching-tag netting rule, these 4 states get +12 and −12 at once, netting to zero for this policy specifically.
**Context:** Worked a concrete manual-investment-vs-agenda cost comparison for National Defense.
**Implication:** The real "clean" affected footprint for National Defense is smaller than its raw support-tag seat count suggests (~137 of 266 seats, not all 266) — worth checking other policies for similar self-canceling overlap before finalizing agenda balance. (See the full 23-policy seat-equivalent ranking above — National Defense nets negative overall, −25.3 seat-equiv.) **Correction 2026-07-22:** this finding's state list was itself inaccurate — see the 2026-07-22 entry above for the correct 6-state list and the applied fix.

## 2026-07-21 — Direct-investment cost-per-seat-share is size-invariant; rally tokens are not
**Finding:** Under the current cost formula (`cost = seats × costPerSeat`, popularity gain independent of seat count), the cost to gain one percentage-point of seat-share is identical regardless of a state's size — investing in Uttar Pradesh (80 seats) is exactly as cost-efficient, seat-for-seat, as investing in a 1-seat union territory. Rally tokens break this: they cost a flat amount regardless of target state size but deliver the same flat percentage swing, so a token is objectively more valuable spent on a large state than a small one.
**Context:** Derived while auditing the total cost to win the game outright, and encoded directly into the interactive economy-balance tool built this session.
**Implication:** There's no pure-efficiency reason to prefer big states over small ones for cash investment; token-spending strategy, by contrast, should explicitly favor the largest available states.

## 2026-07-20 — Chromium-based testing cannot reproduce real Safari viewport-meta behavior
**Finding:** Playwright's `chromium` engine, even with an explicit `--viewport-size`/`viewport` option, overrides or ignores a page's own `<meta name="viewport">` tag entirely — every automated check run against Chromium this session passed cleanly, yet the real regression (see the viewport-meta finding below) was fully reproducible only once testing switched to Playwright's `webkit` engine with the `devices['iPhone 14']` device profile (`window.innerHeight` measured 664px in real WebKit vs the naive assumption of 844px).
**Context:** Spent several rounds diagnosing a real-device-only layout bug ("everything stacking") that never showed up in headless Chromium screenshots, before realizing the browser engine itself was the blind spot, not the CSS.
**Implication:** Any future mobile-Safari-specific bug in this project (viewport meta, safe-area-insets, dynamic toolbar behavior) must be verified with Playwright's `webkit` engine + an iPhone device profile — Chromium screenshots, however carefully framed, cannot catch this class of bug.

## 2026-07-20 — Adding a viewport meta tag to Booth Ink collapsed the map region on real Safari
**Finding:** `pme-mobile-sheet.html` had no `<meta name="viewport">` tag at all; adding one (`width=device-width, initial-scale=1`) to fix an unrelated double-tap-zoom bug caused the map to collapse to a near-zero-height sliver on a real iPhone in Safari. Root cause: without a viewport meta tag, Safari falls back to its classic ~980px-wide virtual canvas (confirmed via real WebKit: `window.innerHeight` was 1668px in that fallback mode vs 664px with the tag), which gave `.map-wrap`'s `flex:1` region far more absolute pixel budget than a true 390×844 device-width viewport does. The file's fixed-height chrome (topstrip + news ticker + groups bar + info panel ≈ 755–765px) does not actually fit within a true device-width viewport at all.
**Context:** User reported "everything stacking on top of each other" after the viewport meta tag was added; initial hypothesis (that Safari's real usable viewport, ~664px with browser chrome, was simply less than the assumed 844px) was factually correct but was the wrong explanation — the user correctly pointed out the same setup had worked for 2 days prior, proving the viewport tag itself was the regression trigger, not a previously-hidden constraint.
**Implication:** The viewport meta tag was reverted; the original zoom-on-rapid-tap bug was instead fixed with `touch-action:manipulation` CSS, which doesn't touch viewport/layout math at all. If a real viewport meta tag is ever needed here, the fixed-chrome elements (topstrip/newsticker/groups-bar/info-panel) will need a real density trim first, or the page needs to rely on standalone/home-screen-installed mode (`apple-mobile-web-app-capable`, already present) rather than a plain Safari tab.

## 2026-07-20 — Booth Ink's map renders 3 states as `<circle>` overlays, not `<path>` — selectors must target both
**Finding:** Uttarakhand (`INUT`), Ladakh (`INLA`), and Himachal Pradesh (`INHP`) each have two SVG elements sharing the same `id` — a real traced `<path>` boundary plus a `<circle>` marker with no `r` attribute (renders as literally zero-radius/invisible on its own). Every map interaction function (`paintMap()`, `selectState()`, the map click handler, `applyGroupHighlight()`) originally only queried `path[id]`, so these three states never got leader-color fills, group-highlight opacity, or click handling via their circle element, and any styling applied only to the `<path>` half of the duplicate-ID pair.
**Context:** User reported these three states appearing to be excluded from group-highlight taps; traced to the selector gap after confirming the underlying group-membership data was correct.
**Implication:** Any future map-interaction code in `pme-mobile-sheet.html` must query `path[id], circle[id]` together (not just `path[id]`) or it will silently skip these three states.

## 2026-07-20 — SVG `opacity` dims stroke along with fill; use `fill-opacity` to keep borders visible
**Finding:** `applyGroupHighlight()` originally set `element.style.opacity` to dim non-member states when a group filter is active — this fades the stroke (border) along with the fill, since `opacity` scales the whole element. Switching to `element.style.fillOpacity` keeps the black border fully visible on every state regardless of group membership, dimming only the fill.
**Context:** User reported specific states (Himachal Pradesh, Ladakh) "should show black border like the other ones" after a group filter was tapped — traced to the whole-element opacity dimming, not a state-specific bug.
**Implication:** Any future highlight/dim treatment on the map SVG should default to `fill-opacity` (or `stroke-opacity` if the intent is the reverse), not plain `opacity`, unless fading the border along with the fill is actually the intended effect.

## 2026-07-20 — Booth Ink prototype has no real game logic behind its UI
**Finding:** `design/prototypes/pme-mobile-sheet.html` (759 lines, one inline `<script>` block, 20 functions) is UI/interaction-only — its `seed()` function fabricates mock popularity numbers and `investIn()`/`renderTokens()` etc. operate on that fake data. There is no real popularity math, campaign-fund accounting, phase timer, or policy-tag resolution; none of it calls into or matches the real engine in `js/*.js`.
**Context:** Investigated while evaluating the user's proposal to "abandon the old mobile project" and restart from just `design/plan.md` + Booth Ink, to check whether that would actually preserve working game logic.
**Implication:** Booth Ink is the UI/markup layer only. Any work wiring it into the real game must rewire its stub functions to call the actual engine functions in `investment-system.js`, `rally-system.js`, etc. — it is not a functional replacement for `index.html` + `js/*.js`, which stays as the engine regardless of skin changes. **Update 2026-07-22:** Booth Ink now fetches real state/group *reference* data live from `states_data.json` (per CHANGELOG's Live Data Binding entry), but its *game* state (popularity, funds, phase timer) is still entirely fabricated — the two engine halves remain disconnected.

## 2026-07-20 — Service worker registration needs a secure context; LAN IPs don't qualify, only localhost or real HTTPS
**Finding:** The secure-context exemption that lets service workers register over plain HTTP only applies to `localhost`/`127.0.0.1` on the same machine. Reaching a local dev server from a phone over LAN (e.g. `http://192.168.x.x:8000`) is a plain-HTTP, non-localhost origin from the phone's perspective — not a secure context — so service worker registration (and by extension real PWA install/offline behavior) will not work there.
**Context:** User asked whether a local server is sufficient for testing the planned PWA scaffolding (Phase 0 of `design/plan.md`'s roadmap), or whether GitHub Pages is required.
**Implication:** Desktop-only iteration on manifest/service-worker code can use `localhost` freely. Testing actual "Add to Home Screen" + offline caching on a physical iPhone requires real HTTPS reachable from the phone — GitHub Pages (already in the stack, zero extra setup) or a tunnel (ngrok/Cloudflare Tunnel) for faster iteration than push-to-Pages each time.

## 2026-07-19 — CSS Grid `1fr` tracks silently override declared pixel sizes when the grid container's width is undefined
**Finding:** `.groups-box` used `grid-template-columns:repeat(8,1fr)` with no explicit `width`, relying on CSS shrink-to-fit. The `.gchip{width:63px}` declared on each grid item was silently overridden — actual rendered icon size came from an accidental shrink-to-fit computation against the viewport, not the CSS value. It happened to look reasonable on the device it was screenshotted on, but nothing guaranteed that on a narrower or wider phone.
**Context:** Investigated after the user asked "what's the solution" for a floating icon bar being "problematic for some screen sizes."
**Implication:** Any future CSS Grid layout with `1fr` tracks in this project must give the grid container an explicit, screen-relative width (e.g. `min(calc(100% - Npx), cap)`) or use `aspect-ratio` on items — never leave sizing to shrink-to-fit when a declared pixel size needs to actually hold.

## 2026-07-19 — Standalone HTML prototypes need an explicit `<meta charset="UTF-8">`
**Finding:** The mobile-first prototype files rendered correctly via Claude's Artifact hosting (which sets a proper `Content-Type: text/html; charset=utf-8` header) but produced full mojibake — every emoji, the ₹ symbol, and em dashes — when served via Python's built-in `http.server`, which sends no charset header at all. The file had no `<meta charset>` tag, so the browser had to guess and guessed wrong.
**Context:** User hit this testing the prototype on their phone via a local LAN server (`python -m http.server`) set up so they could preview it at true device proportions outside the Artifact viewer's chrome.
**Implication:** Every standalone HTML file in this project (prototypes or the real game) should declare `<meta charset="UTF-8">` as the first tag, regardless of intended hosting — don't rely on the host to set the header correctly.

## 2026-07-19 — Claude Artifact viewer chrome makes on-device proportions look wrong in screenshots
**Finding:** Claude's Artifact viewer wraps published pages in its own title-bar UI (~150–200px: page title, "Artifact by you", share/flag icons) sitting above the actual page content. Since the mobile prototype's map region uses `flex:1` to fill all remaining vertical space, a screenshot taken inside the Artifact viewer under-represents how much taller the map (and therefore how much smaller the chrome looks by comparison) will be in a true fullscreen context — several rounds of "make the UI bigger" feedback traced back to this rather than any single wrong pixel value.
**Context:** User compared the same prototype rendered inside the Artifact viewer vs. added to the iPhone home screen from a local server, and the proportions looked very different.
**Implication:** When sizing chrome for a mobile-first prototype meant to be judged against real device proportions, size against actual iPhone 14 viewport assumptions (390×844, this project's stated default target) rather than against how it looks inside the Artifact preview panel — the preview systematically under-represents available map height.

## 2026-07-19 — Claude Artifacts cannot be true installable PWAs
**Finding:** The Claude Artifact runtime only exposes two capabilities (`downloads`, `mcp` — confirmed via the artifact-capabilities skill, contract 0.1.12). There is no way to host a separate `manifest.json` or register a service worker for a published artifact, so real installability (Android install banner, offline caching) is not achievable there. iOS Safari's legacy meta-tag-only standalone mode (`apple-mobile-web-app-capable`, `apple-touch-icon`, no manifest/service worker required) is the only way to get a full-screen home-screen launch from an artifact.
**Context:** User asked whether a published artifact could be made into a PWA for full-screen mobile testing of visual/interaction prototypes.
**Implication:** Fine for prototyping (already applied to the interactive mockups), but a real installable PWA still requires the actual project repo's own hosting (e.g. GitHub Pages) with a real manifest + service worker — this is unrelated to and doesn't substitute for Phase 0 of the mobile parity/PWA plan.

## 2026-07-19 — Real India map SVG wastes ~27% of its rendered area
**Finding:** `assets/icons/INDIA_V3_smaller_viewbox.svg` declares `viewBox="0 0 1000 1000"` (a square), but parsing every path's `d` attribute with a proper path-command parser (handling M/L/H/V/C/S/Q/T/A/Z, absolute and relative) shows the actual content only spans x:100–900, y:45.5–955 — a ~0.88 width:height aspect ratio baked inside a claimed 1:1 square, wasting roughly 10–19% margin per side.
**Context:** Investigated after the map still looked undersized on a phone-width mockup even after removing all UI overlays that had been covering it.
**Implication:** A tightened viewBox (`"84 27 833 946"`, verified against parsed coordinates) renders ~27% more map area with zero risk of cropping any state. Worth applying this same fix to the real game's map asset, not just prototypes — the underlying SVG file itself carries the bug, not any particular layout.

## 2026-07-19 — Small-UT click pattern already exists in production and should be reused
**Finding:** Both the desktop and mobile games already solve "some UTs are too small to tap directly on the map" with a dedicated button cluster (mobile's `index.html` → `union-territories-container`, grouped with the timer-pill overlay) covering exactly 6 UTs: Delhi, Chandigarh, DNH & DD, Puducherry, Lakshadweep, A&N Islands. Note `states_data.json` flags 8 total UTs (also J&K and Ladakh), but those two are large enough to tap directly — only the 6 above need the button-cluster workaround.
**Context:** Needed a way to make small UTs clickable in an interactive prototype; checked the real app's existing approach before inventing a new pattern.
**Implication:** Any future UT-interaction work (real game or further prototyping) should extend this existing button-cluster pattern rather than diverging from it — it's already validated in production.

## 2026-07-19 — Mobile's likely replayability root cause: static regional-dominance payoffs
**Finding:** The regional dominance bonus (>50% popularity across a whole state group → lump sum + recurring per-phase payout) has fixed group membership and fixed payout every match. Random starting popularity changes *where* a player happens to be leading but not *which lever is worth pulling* — so a learned opening ("rush South India") stays optimal in every game, forever.
**Context:** Reasoned from the actual bonus logic in `campaign-system.js` (`checkRegionalDominanceBonuses()`) plus `ROADMAP.md`'s group-bonus description, in response to the user's complaint that the game converges to 2–3 strategies after a few plays.
**Implication:** Highest-leverage replayability fix is randomizing which groups/values are "live" per match and giving random events real strategic teeth — not adding more static content. (Superseded by the agenda/special-power/token redesign — see `design/plan.md`.)

## 2026-07-19 — Mobile has no AI opponent; "Player 2" is same-device hotseat
**Finding:** `app.js:53` — `const playerId = event.shiftKey ? 'player2' : 'player1'`. Player 2 has no decision-making logic at all; it's a second local input path via Shift+Click on the same device. Desktop's `ai-player-controller.js` (956 lines, the largest module in either codebase) was never ported to mobile.
**Context:** Discovered while auditing feature parity between desktop and mobile JS modules.
**Implication:** Any "vs AI" or matchmaking work must build/port real AI decision logic from scratch — current mobile behavior is not a stand-in for anything more than a second local input path.

## 2026-07-19 — Mobile has no session start/end; options menu is a non-functional stub
**Finding:** No welcome/setup screen exists (players hardcoded to Modi/BJP vs Rahul/INC in `index.html`), no game-over/results screen exists, and every option card in `app.js`'s `initOptionsModal()` (New Game, Toggle Sound, Toggle Music, Pause/Resume, Help, Random Events, Hard Mode) just calls `console.log` and closes the modal.
**Context:** Found via reading `app.js` and `index.html` while auditing what's implemented vs. stubbed for the desktop/mobile feature-parity audit.
**Implication:** The mobile build currently has no defined session boundary — treat it as a sandbox, not a completable game, until these are built (tracked as Phases 1, 3, 4 in the mobile parity/PWA plan artifact).

## 2026-07-19 — Regional dominance bonus is ported; random events, home bonus, action log are not
**Finding:** `checkRegionalDominanceBonuses()` in `campaign-system.js` correctly replicates desktop's group-bonus system. However, desktop's `random-events.js`, `home-state-bonus.js`, and `actions-log.js` have no mobile equivalent at all.
**Context:** Cross-referenced every desktop JS module against mobile's module list during the feature-parity audit.
**Implication:** Random events and home-state bonus work is purely additive (not blocked on anything else) and doubles as the main replayability lever — see the entry above. (Superseded — see `design/plan.md` Replayability section for the actual replacement design.)
